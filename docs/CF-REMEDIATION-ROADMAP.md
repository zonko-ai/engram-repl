# Engram — Cloudflare Remediation Roadmap

> Workflow `engram-cf-remediation-research` (run wf_8c63008a-6fc). 53 agents, 3.3M tokens, ~16min.
> Maps the 89 stub-unacceptable + 3 dangerous-cliff findings -> fixes; adversarially CF-API-verified.

**Verdict tally:** 3 ship-now · 15 feasible · 0 research-needed · 0 infeasible · 0 rejected (of 18 themes).

**Thesis confirmed:** most 'unacceptable' gaps are engram impl-gaps / unwired capabilities / determinism choices — NOT CF platform limits. Only crypto, timers, vm-isolation need CF primitives.

---

All VERIFY corrections are confirmed against the live code: `registry_invoke` is a frame-level handler (lib.rs:1757), `FsStageOp` has no Rename variant (only Write/Delete at :1208/1221), the 24MB ceiling guard is at kernel-glue.ts:2581, and the 4-budget drain loop is at :1667. I have enough to write the roadmap with corrected effort/feasibility per the verifications.

---

# ENGRAM REMEDIATION ROADMAP

*Canonical. Every effort/feasibility below reflects the adversarial VERIFY corrections, validated against live code (`set_clock` absent; `__rand` is a Rust closure; host-call boundary is async-park-only; `FsStageOp` ∈ {Write, Delete}; `registry_invoke` is frame-level; 24MB guard at kernel-glue.ts:2581).*

## 1. Headline

Of the 89 unacceptable + 3 dangerous findings, **~85 are fixable and ~7 stay hard-limited** — but the means is more conservative than the per-theme self-assessments claimed. The dominant lever is **pure in-VM / host-side JS** (console, buffer encodings, util.inspect/TextDecoder, streams, fetch/http fidelity, fs-tree in-heap semantics, TS reject-gate, fetch-cap typed-reject, process.env, vm same-realm hardening) — these need **no CF primitive and no determinism change**, and clear the largest block of findings. A second tier is **wiring already-bound CF primitives**: Worker Loader (`LOADER.load`) for *real* VM isolation, DO Alarms (supervisor-owned, since facets can't alarm) for cross-hibernation timers, R2 streaming GET→PUT for atomic large-file rename, and the Tier-2 container as the escape hatch for real-Node TLS/mTLS and full-ICU. A third tier is the **determinism-vs-realism axis** (`config.realtime`): unifying ~15 findings, but **NOT the "S, glue-only" job the menu claimed** — real entropy must be plumbed through the Rust `__rand` closure (an engine edit + rebuild), and `set_clock` *does not exist in the shipped engine* (the "clock:real already works" premise is false). **Three findings are genuinely immovable** (the snapshot incompressible cliff's in-kernel ceiling, raw TCP keepalive/NODELAY, in-VM TLS cert-chain/ALPN/mTLS) — these become document-only honest-signal fixes plus a container escape hatch. **Zero new WASM imports** are needed anywhere; the 12-import ceiling is untouched by every fix.

## 2. Quick wins — ship-now, XS–S, no CF, no determinism break

| Theme | Fix | Effort | Findings cleared |
|---|---|---|---|
| **Console format** | Hoist existing `__format`/`formatWithOptions` to global; route all 5 console levels through it (lazy guard); real ASCII `console.table` | **S** (Rust rebuild → not XS) | printf %-substitution, console.table garbage, util.format parity |
| **Buffer encodings** | base64url + ucs2/utf16le in toString/write; lenient `__b64dec` (kills strict-atob throw); brand-symbol `isBuffer`/`instanceof` | **S** | base64 strict-throw, base64url, ucs2/utf16le, isBuffer false-positive |
| **TS reject-gate** | `rejectUncompilableTS(src)` before sucrase — typed `TypeScriptError` for `namespace`/`module`/decorators; **use sucrase `getFormattedTokens` (isType + @-label), NOT a char scanner** (avoids false-rejecting `module`/`namespace` as plain identifiers); fix stale comment (kernel-glue.ts:1529-1533), docs | **S** (token-based, not the menu's XS char-scan) | namespace silent ReferenceError, decorator SyntaxError; const-enum/enum/param-props confirmed false-positives (do NOT gate) |
| **Fetch egress cap** | Drain loop (kernel-glue.ts:1667): replace silent bail with synthesized **per-budget** typed reject (`HostCallLimit`/`StreamOp`/`WsOp`/`FsOp`) via `eval_resume`; mandatory slack-break + **glue-synthesized final frame** if still parked; drop the wrong "use fetchStream for fan-out" advice | **S** | silent stale-result on 65th host call (3 sibling budgets too) |
| **util.inspect / deepStrictEqual / TextDecoder** | opts-bag in `__preview` (**duck-type 2nd arg for backward-compat across 9 callsites**); Symbol/non-enum keys; `__deepEqual` Symbol+boxed+cycle+element-wise Set; single-byte/UTF-16 decoder paths (real CP-1252 table, not latin1) | **M** (signature blast-radius) | inspect opts ignored, deepStrictEqual Set/Symbol holes, TextDecoder non-UTF8 |
| **Streams** | Complete WHATWG (WritableStream/TransformStream/tee/pipeTo/pipeThrough/strategies); fix `stream/web` alias → WHATWG not `__stream`; real hwm/drain/unpipe in Node `__stream`; fromWeb/toWeb bridges | **M–M+** | WHATWG incompleteness, hwm no-op, unpipe no-op, web alias wrong, no backpressure |
| **process.env / memoryUsage** | `config.env` → `_seedProcessEnv()` via existing eval_begin/scratch; re-seed missing-keys on restore; `memoryUsage()` reads `used_heap()`/`buffer_bytes()` (glue re-seed, no recompile path) | **XS–S** | process.env always empty, memoryUsage hardcoded zeros |
| **VM isolation (Part A)** | One-time warn on `runInNewContext`; harden shim with `with(sandboxProxy){…}` (has⇒true) to kill ambient-global reads | **XS** | casual prototype-pollution / global-read in vm shim |
| **FS tree (in-VM only)** | renameSync prefix-move over `V.files`/`V.dirs`; `V.links` map for symlinks; lstatSync/readlinkSync/Dirent.isSymbolicLink | **S** (default `vfs` provider; durable layer is separate — §3) | renameSync not recursive, fake symlinks (in-heap path) |
| **TLS/socket honesty** | `getProtocol()`→null (was hardcoded 'TLSv1.3' tls.js:55), `getPeerCertificate()`→{}, `alpnProtocol`→false honestly; SNI already-correct (doc) | **S** | fabricated TLS protocol/cert/ALPN lies |

## 3. CF-primitive fixes (grouped by primitive)

| Theme | CF primitive | Mechanism | Determinism / snapshot cost | Effort | Risk |
|---|---|---|---|---|---|
| **Wall-clock timers — TIER A** | None (DO event-loop `setTimeout`) | `host.sleep(ms)` effect in `_serviceHostCall`; `await setTimeout`; resume via eval_resume. **Own budget bucket** (not the 64 guard); **cap single-digit seconds** (a pending timer pins the kernel resident + bills + blocks hibernation — the "15min" cap is unsafe) | Opt-in `config.realtime`; default unchanged. Snapshot: zero (parked like in-flight fetch) | **M** | medium |
| **Wall-clock timers — TIER B** | **DO Alarms** (supervisor-owned; facets can't alarm) | Cross-hibernation sleep: persist `{wakeAt,contId}` to SQLite min-heap, set alarm, hibernate, resume parked continuation on `alarm()` | New "parked" eval protocol status (SDK/CLI must handle); mid-host-call heap snapshot **unproven** | **L** (NOT M; do not bundle with A) | high |
| **VM isolation — Part B** | **Worker Loader `LOADER.load`** (fresh isolate; engram currently only uses `.get` warm-cache) | New async `vm.runInNewContextAsync(code,ctx)` → park → `LOADER.load({…,globalOutbound:null,limits:{cpuMs}})` → JSON-marshalled result. **`_serviceHostCall` does NOT hold `env.LOADER`** (arrives as raw JsValue at the frame handler only) → must thread LOADER into GlueKernel (`setLoaderEnv`-style) OR route the parked effect back to `registry_invoke` | Child isolate non-deterministic (own Math/Date/crypto) → kept OFF contract as a NEW opt-in API; `opts.seed` for reproducibility. Snapshot: **cliff RELIEF** (heavy work runs outside WASM linear memory; only JSON result lands) | **M+** (binding plumbing, not the "clean drop" claimed) | low–med |
| **FS large-file + atomic rename** | **R2 streaming GET→PUT** (already-bound SNAPSHOTS) | `MAX_FS_FILE_BYTES`≈20MB guard in r2-write branch → typed `EFBIG` reject (was silent >24MB WS-frame drop). New **`FsStageOp::Rename`** + new `host.__fs op:'rename'` (the menu's "no new host op / imports=NONE" is **FALSE** for the r2 path); **staged & applied in `flush_staged_fs` at checkpoint, NOT mid-cell** (mid-cell R2 mutation breaks staged-commit coherence). R2-origin body has known length → streaming PUT works (generic stream needs `FixedLengthStream`). Rename touches 2 paths → must survive the single-path collapse map | Determinism-neutral. Snapshot: removes read-whole-body-into-heap; **body-first ordering** (not transactional atomicity — call it that) | **M** | low–med |
| **FS metadata (mtime/Dirent)** | None (DO SQLite column + R2 HEAD fallback) | `fs_files ADD COLUMN mtime_ms` (idempotent ALTER, lib.rs:387 pattern). **Wire `r2_fs_op`'s 'stat'/'list' arms (lib.rs:4479/4515), NOT `vfs_stat`** (menu mis-located it; hStat hits r2_fs_op which emits no mtime today → always 0). Stamp from **VM seeded `Date.now()`** threaded through staged op (NOT host `js_sys::Date::now` = real wall-clock = poisons determinism); requires extending FsStage payload+struct+flush path. Container/direct-R2 files → R2-HEAD real upload time, flagged non-deterministic | Seeded path stays byte-identical IF VM-seeded clock used. `existsSync`-over-R2 is **structurally impossible** (sync-over-async); only async `access`/`exists` fixable | **M-high** | low |
| **FS tree (durable layer)** | DO SQLite + R2 | `fs_files ADD COLUMN ftype,link_target`; durable dir-rename = manifest path-column UPDATE (R2 keys stable via r2_key indirection). **`reconcile_fs_files` (lib.rs:2048) MUST skip `ftype='symlink'` rows** or it deletes every bodyless symlink on the next gateway/hash-worker invoke (silent loss) | Deterministic (string fns). Negligible snapshot | **M** | medium |
| **Console / TextDecoder ICU / TLS-mTLS escape** | **Tier-2 Container** (engram-sandbox, already bound) | Full-ICU legacy multibyte (shift_jis/gbk/big5/euc) and real-OpenSSL mTLS/ALPN/cert-pinning routed to `s.sandbox.exec`, capability-gated, shared /workspace | Opt-in, outside determinism contract (like host.fetch) | **S** (doc + pointer) | low |

## 4. `config.realtime` — the unified realism switch

A single coherent two-axis switch replacing the scattered partial flags. **All OFF by default; seeded byte-identical restore stays the default contract.**

```
config.realtime: true              // umbrella → clock + crypto + timers all real
config.realtime: { clock, crypto, timers, mtime }   // granular override
```

**What it enables:**
| Axis | Real behavior | Implementation reality (corrected) |
|---|---|---|
| **clock** | `Date.now`/`performance.now`/`hrtime`/`uptime`/`dayjs.now`/`mtime` track real wall time; real inter-cell elapsed | ⚠️ **`set_clock` does NOT exist in the shipped engine** (kernel-glue.ts:1571 try/catch silently no-ops). Today clock:real only sets a create-time base then drifts +1ms/call. **Real fix = build the `set_clock` export** (engine edit + rebuild). The menu's "already shipped" is false. |
| **crypto/entropy** | `Math.random` + node:crypto + nanoid/uuid + `crypto.getRandomValues` → true workerd CSPRNG | ⚠️ **NOT glue-only.** `__rand` is a Rust closure inside WASM (lib.rs:2932); every user-facing consumer routes through it, NOT through WASI `random_get`. Swapping `random_get` alone achieves nothing visible. Real fix = gate `__rand` by a `cryptoReal` flag and have it read real bytes (per-turn-refilled scratch ring written by glue via `crypto.getRandomValues`, chunked ≤64KB/quota). **Unbounded-draw problem unsolved**: a `Math.random()`-heavy loop exhausts any fixed per-turn buffer with no mid-cell host re-entry (sequential host-call boundary). Needs a new `set_entropy_mode` export. |
| **timers** | `setTimeout`/`AbortSignal.timeout`/socket idle fire on real wall time (TIER A, single-digit-sec cap) | §3 — own budget bucket, pins kernel resident |
| **mtime** | fs timestamps from real clock | rides the clock axis + fs_files column (§3) |

**EXACTLY what it costs:**
- **Byte-identical restore is forfeited** for the enabled axes — by design, identical trade to today's `clock:"real"`. Two runs of the same source from the same session diverge in the heap.
- **Replay/comparison becomes meaningless** for that session — but the kernel *never replays* (heap-snapshot, no journal), so this costs **nothing operationally** except: engine-migration replay (the oplog path on engine-hash change) can no longer reproduce identical bytes for a realtime session.
- **Snapshot/restore mechanism unaffected** — still an ArrayBuffer blit; flags persist in `config_json` (same path as rngSeed/clock), re-armed on restore.
- **Observability:** add an AE `configRealism` blob (`none`/`clock`/`crypto`/`timers`/`all`) per op so determinism status is queryable.

**Honest effort:** the menu said **S/ship-now**; it is **M with an engine rebuild** — both real axes (clock via new `set_clock`, crypto via `__rand` reroute + new `set_entropy_mode`) are Rust engine work, plus a re-test of seeded byte-identical restore in the default path. The CF *primitive* (`globalThis.crypto.getRandomValues`, `Date.now`) is trivially available (no binding, sync, no quota beyond 64KB/call) — that was never the hard part.

**Findings unified (~15):** all crypto-entropy items, all wall-clock-timer items, Date/performance/hrtime/uptime non-realism, mtime=0, dayjs.now, and the determinism-policy meta-finding.

## 5. The snapshot cliff (the dragon)

**Best achievable mitigation = three honest layers; the in-kernel ceiling is immovable.**

- **Layer 1 — IN-KERNEL CEILING IS INFEASIBLE (hard limit, document it).** WASM linear memory is monotonic; dlmalloc does no downward compaction. The OOM is the **dump's transient resident expansion** — `new Uint8Array(memory.buffer.slice(0))` (full 2nd copy) + zstd (3rd buffer) inside the ~128MB DO RAM budget — **NOT the R2 write**. Therefore R2 multipart/streaming, D1, KV offload of the *stored image* **cannot raise the in-kernel ceiling**. True in-place compaction needs value-serialization that loses promise/closure/exotic-namespace fidelity (the whole thesis). **~24MB incompressible is the immovable in-heap ceiling.**

- **Layer 2 — ALREADY FAIL-SAFE for the snapshot path; harden the residue.** The `INCOMPRESSIBLE_BUFFER_CEILING_BYTES=24MB` guard (kernel-glue.ts:2581) **already throws a typed `SizeAdmissionError` before the slice/compress** — socket alive, reset recovers. So **yes, the snapshot path is a typed reject, NOT WS-1006.** The genuinely uncatchable residue is narrow: a single synchronous **native-builtin alloc burst** that crosses ~28MB before the cell yields. Hardening: clamp `ArrayBuffer`/`TypedArray`/`Buffer.alloc/.fill` length args in injected JS (precedent: scryptSync clamp engine:1839) to throw `RangeError` pre-growth. **Honest claim downgrade:** this catches the **canonical single-giant-alloc** (`new Uint8Array(40MB)`) but is **bypassed** by `String.prototype.repeat`, `Array(n).fill().join`, `ArrayBuffer.prototype.resize/transfer`, `%TypedArray%`/`Reflect.construct` subclassing, huge-string `JSON.parse` — rquickjs has **no native alloc-size hook**, so it's a global-constructor shadow (fragile, long tail remains). The post-cell growth tripwire **already exists** (engine finalize_cell:3243). So: "closes the residue" is **too strong → "clamps the common case, residue remains."**

- **Layer 3 — RAISE EFFECTIVE WORKING-SET via offload (the real ceiling-raiser, opt-in).** For legit >24MB working data, don't hold it in the QuickJS heap — push to the **already-bound Tier-2 container** (256MB–20GB, shares R2 /workspace) or R2 directly under `fs/<doId>/offload/`, keep only handles/cursors in the snapshottable heap. Thin `host.offload(key,blob)`/`host.fetchOffloaded(key)` over existing `__hostCall` + R2/sandbox fetch. **Zero new imports.** The snapshot becomes a small index over large external state → big-working-set sessions snapshot/restore fast and never approach the cliff. R2-only offload is deterministic; container offload is opt-in non-deterministic.

**Honest ceiling:** in-heap live state stays bounded at ~24MB forever. **Can it be made fail-safe? — YES, already is, for the snapshot path** (typed `SizeAdmissionError`). The only WS-1006 residue is the narrow native-alloc burst, partially (not fully) closed by Layer 2 clamps.

## 6. Hard limits that stay (document-only)

No CF primitive fixes these:

1. **Snapshot in-heap ~24MB incompressible ceiling** — WASM monotonic memory + dump transient expansion. (Mitigate via offload §5-L3; cannot raise in-kernel.)
2. **Raw TCP keepalive / `setNoDelay` (TCP_NODELAY)** — `cloudflare:sockets` `SocketOptions` exposes only `{secureTransport, allowHalfOpen}`. No knob exists. (No-op returning `this`; Node tolerates.)
3. **In-VM TLS introspection / mTLS** — `getPeerCertificate`/`getProtocol`/`getCipher`/`alpnProtocol`/`authorized`/client-certs/`rejectUnauthorized:false`. TLS terminates inside workerd; neither host JS nor VM ever sees the chain. (Honest-signal: null/{}/false; **container escape** for real mTLS/ALPN/pinning.)
4. **Full ICU legacy-multibyte decoders** (~100: shift_jis/gbk/big5/euc) — multi-MB ICU tables would push images toward the cliff. (Container escape; single-byte + UTF-16 done in-VM.)
5. **Synchronous `existsSync`/`require('./x')` over R2-only files** — sync-over-async is structurally impossible (explicitly not-built). Async `access`/`use('./x')` only.
6. **`import()` native syntax through a parking loader (vm-isolation c1 / module-system c1)** — QuickJS module resolution inside `Module::eval` is **synchronous**; the host-call boundary is a full async unwind. A `set_loader` Loader **cannot park/await** (proven by `__esmEval`'s `WouldBlock`). **Redesign required** (pre-resolve all imports before eval, or a sync in-heap module-source cache) — the parked-loader design is reject-as-described, not "follow-up."
7. **Facets cannot set alarms** — TIER-B cross-hibernation timer scheduling must live on the supervisor DO, not the facet.

## 7. Prioritized sequence

| Phase | Contents | Effort | Findings cleared (~) |
|---|---|---|---|
| **P0 — Pure-JS quick wins (no CF, no determinism, no engine rebuild where possible)** | Fetch-egress typed-reject (4-budget) · process.env/memoryUsage · vm Part-A `with`-proxy + warn · TLS honesty edits (null/{}/false) · FS in-VM rename prefix-move + symlinks · TS reject-gate (token-based) · stale-comment/doc fixes | **S each** | **~22** |
| **P1 — Pure-JS fidelity (engine rebuild; run in-thread, deploy via alchemy)** | Console formatter + table · Buffer encodings (base64url/ucs2/lenient/brand) · util.inspect opts + deepStrictEqual + TextDecoder single-byte/UTF-16 · Streams WHATWG completion + Node hwm/drain/unpipe + bridges · fetch/http fidelity (multipart deterministic-boundary, redirect mode, getSetCookie, IncomingMessage stream, >2MB un-cap) | **M each** | **~26** |
| **P2 — FS durable layer + atomic large-file (CF: R2 streaming + SQLite columns)** | `MAX_FS_FILE_BYTES` EFBIG reject · `FsStageOp::Rename` staged-in-flush + R2 streaming GET→PUT · `fs_files` mtime_ms/ftype/link_target columns · wire `r2_fs_op` stat/list (Dirent + mtime) · `reconcile_fs_files` skip-symlink fix · async exists/access durable-aware | **M–M-high** | **~12** |
| **P3 — Realism axis `config.realtime` (CF: crypto.getRandomValues + DO Alarms; engine rebuild)** | Build `set_clock` export (clock axis) · `__rand` cryptoReal reroute + `set_entropy_mode` (per-turn scratch ring, document unbounded-draw limit) · TIER-A `host.sleep` (own budget, single-digit-sec cap) · mtime real-clock wiring · AE `configRealism` blob | **M (engine)** | **~15** |
| **P4 — Real isolation + working-set escape (CF: Worker Loader `.load`, Tier-2 container)** | `vm.runInNewContextAsync` via `LOADER.load` (thread LOADER into GlueKernel) · `host.offload`/`fetchOffloaded` (snapshot-cliff relief) · Layer-2 alloc-clamp (canonical-case only) · container escape pointers (mTLS, full-ICU, legacy-multibyte) · module-system (A)+(B-sync-in-heap)+(c2 rewrite); **drop c1** | **M+** | **~10** |
| **P5 — TIER-B durable timers (CF: supervisor-owned DO Alarms) — deferred/research** | Cross-hibernation sleep: SQLite min-heap wake queue, new "parked" eval protocol status (SDK/CLI), mid-host-call snapshot resume (**unproven — de-risk first**), supervisor-owned alarms for facets | **L** | **~3** |
| **Document-only** | §6 hard limits: snapshot ceiling, keepalive/NODELAY, in-VM TLS introspection, full-ICU, sync-over-async, native `import()`, facet-no-alarms | **XS** | **~7 (reclassified, not "fixed")** |

**Sequencing logic:** P0/P1 clear the majority (~48) with zero CF-primitive risk and no determinism exposure — ship first. P2 is self-contained durable-fs work on already-bound R2/SQLite. P3 is the one phase that touches the determinism contract (gated, opt-in, default-safe) and needs the honest engine work the menu under-scoped. P4 unlocks *real* isolation and the cliff escape valve. P5 (TIER-B) is the only genuinely high-risk, research-gated item — **do not bundle with TIER-A**, and de-risk mid-host-call snapshot resume before committing.

Key file anchors: `apps/kernel/engine/src/lib.rs` (BOOTSTRAP shims, `__rand`:2932, `create`:3073, no `set_clock`), `apps/kernel/src/kernel-glue.ts` (drain loop:1667, `_serviceHostCall`:1766, 24MB guard:2581, `set_clock` no-op:1571), `apps/kernel/src/lib.rs` (`FsStageOp`:236, `flush_staged_fs`:1177, `reconcile_fs_files`:2048, `r2_fs_op` stat/list:4479/4515, `registry_invoke`:1757, ALTER pattern:387).


---
## Appendix — Per-Theme Fix + Verification


### CRYPTO ENTROPY

- **findings:** Predictable VM tokens: crypto.getRandomValues, crypto.randomUUID, node:crypto randomBytes/randomFillSync/randomFill/randomInt, and Math.random all route through seeded RNGs (Rust lcg __rand at engine/src/lib.rs:184,2923; JS mulberry32 in WASI random_get at kernel-glue.ts:253-261,317). Bootstrap crypto shim engine/src/lib.rs:367-378; node:crypto shim captures __seededRandom once at :1799-1810. Determinism is an engram CHOICE (clock:real precedent at kernel-glue.ts:1232); workerd has real CSPRNG host-side.
- **root cause:** engram-determinism-choice · **fix:** Add opt-in config.crypto:"real" (default unset = "seeded"), mirroring the existing config.clock:"real" pattern. In "real" mode, crypto.getRandomValues/randomUUID + node:crypto randomBytes/randomFillSync/randomFill/randomInt draw from the host's real crypto.getRandomValues (workerd CSPRNG) instead of the seeded LCG. Keep Math.random on the seeded __rand so non-crypto determinism is untouched. Wire via a glue-injected globalThis.__cryptoRandomFill primitive that the engine bootstrap crypto shim (engine/src/lib.rs:367) and the node:crypto __seededRandom capture (:1800) use INSTEAD of __rand — seeded in default mode, real-entropy in "real" mode. Resolve the flag BEFORE bootstrap runs at BOTH create and restore (the crypto-shadow ADR captures the entropy ref once). Persist config.crypto via configJson across cold restore (same path as clock/rngSeed/typescript).
- **CF primitive:** workerd real Web Crypto (globalThis.crypto.getRandomValues) — already available host-side, NO wrangler binding, NO env entry
- **mechanism:** (1) Client sets config.crypto:"real" at create. (2) _applyConfig (kernel-glue.ts ~:1221) reads it into a this.cryptoReal flag, persisted in configJson (round-trips through the snapshot like clock/rngSeed). (3) Glue injects globalThis.__cryptoRandomFill: seeded mode = existing seeded path; real mode = fills via globalThis.crypto.getRandomValues(buf). crypto.getRandomValues is SYNCHRONOUS host-side (CSPRNG, no Promise) so it needs NO host-call park/resume round-trip (the menu's async warning is about crypto.subtle, not getRandomValues). (4) The engine bootstrap crypto shim and node:crypto shim use __cryptoRandomFill instead of __rand for entropy; because both capture their entropy ref once at bootstrap, the glue seeds __cryptoRandomFill before BOOTSTRAP at create AND restore. Math.random stays on seeded __rand. Thinner alt: make WASI random_get (kernel-glue.ts:317) real-fill in cryptoReal mode and route only crypto/node:crypto entropy through it — but the dedicated __cryptoRandomFill global cleanly isolates crypto from Math.random.
- **determinism:** Default DOES NOT break seeded byte-identical restore: config.crypto unset/"seeded" keeps the exact current LCG path (Math.random AND crypto.* stay seeded, byte-identical across restore). config.crypto:"real" is strictly OPT-IN and consciously forfeits byte-identical replay for crypto.*/node:crypto random fns only (Math.random left seeded) — identical trade to the existing clock:"real". No change to the default determinism contract. · **snapshot:** Minimal: adds only a tiny config.crypto flag to the persisted configJson manifest — same mechanism as clock/rngSeed/typescript, a few bytes, no heap-layout change, no extra serialized state. Already-consumed real bytes live in the heap like any value and snapshot-persist normally; on cold restore the flag re-resolves to "real" and subsequent fills draw fresh real bytes (consistent with clock:real re-seeding). No effect on snapshot-size cliff or restore path. · **imports:** ZERO new WASM import. The fix changes the JS body of the already-existing WASI random_get import (kernel-glue.ts:317) and injects a plain JS global (__cryptoRandomFill) via the normal globalThis seeding path. Host crypto.getRandomValues is a host-side JS global, not a WASM import. The 12-engine-WASM-import ceiling is untouched.
- **effort:** S · **risk:** low · **feasibility(self):** ship-now
- **VERIFY [revise→feasible]:** CORRECT on the CF/workerd facts. globalThis.crypto.getRandomValues IS available host-side in a Durable Object, is SYNCHRONOUS (CSPRNG, returns the filled buffer, no Promise), needs NO wrangler binding and NO env entry, and needs NO fetch round-trip. Only crypto.subtle.* is async. workerd's getRandomValues enforces the standard 65536-byte-per-call quota (QuotaExceededError above that), so a real-fill helper must chunk large buffers; the menu's async warning genuinely concerns subtle, not getRandomValues. config-persistence claim holds: _applyConfig (kernel-glue.ts:1216-1252) re-resolves config on every create AND restore, so a persisted config.crypto flag re-resolves to 'real' on cold wake, matching the clock:'real' precedent. determinism/snapshot/imports framing is otherwise honest: zero new WASM import is achievable, default path stays byte-identical, Math.random untouched.
  - blockers: The MECHANISM targets the wrong entropy source — load-bearing error. (1) The VM's crypto.getRandomValues (engine/src/lib.rs:367) and node:crypto's __seededRandom capture (:1800-1802) draw from globalThis.__rand, a RUST-side seeded LCG closure (inject_host_fns :2923-2932, RNG cell :30, lcg :184) running INSIDE the WASM engine — NOT the glue's rnd() and NOT WASI random_get. The glue's WASI random_get (:317/:253) is a SEPARATE mulberry32 the crypto shims never touch, so the proposal's 'thinner alt' (make random_get real-fill) would NOT change crypto.getRandomValues/randomUUID/node:crypto output at all. (2) There is NO glue->VM JS-global injection path (grep confirms); the glue cannot inject globalThis.__cryptoRandomFill into the VM. The only synchronous host->VM channel is a Rust Function::new closure inside the engine; the __hostCall bridge (:2936) is ASYNC/parked (HOSTCALL_REQ + suspend/resume), unusable for a synchronous fill inside the bootstrap crypto loop. CORRECT FIX: plumb real-entropy through the WASM boundary into the Rust __rand path — gate __rand (or a new Rust crypto closure) by a cryptoReal flag passed into create(), and have it call OUT via an import for real bytes. To stay under the 12-import ceiling, reuse the existing WASI random_get import: re-route Rust __rand to call WASI random_get when cryptoReal is set, and branch random_get's JS body (seeded mulberry vs host crypto.getRandomValues, chunked at 64KB). That is an ENGINE edit (engine/src/lib.rs: create() signature or a mode cell + __rand routing) plus a glue import-body branch — NOT the claimed glue-only change. (3) The crypto-shadow ADR captures the entropy ref ONCE at bootstrap (__seededRandom bind at :1800), so mode must be fixed BEFORE bootstrap at both create and restore — satisfiable (config resolved pre-create), but means real mode is create-time only, not toggleable on a live session (acceptable, matches clock). Net: feasible and architecturally sound goal, but effort is NOT S/ship-now — it requires an engine (Rust) change and a re-test of seeded byte-identical restore in default mode; call it M, research-light.
  - evidence: apps/kernel/engine/src/lib.rs:2923-2932 (__rand = Rust seeded LCG closure, the real entropy source for VM crypto), apps/kernel/engine/src/lib.rs:184-188 lcg + :30 RNG thread-local + :3073-3079 create() seeds RNG, apps/kernel/engine/src/lib.rs:366-373 (VM crypto.getRandomValues/randomUUID uses globalThis.__rand, NOT random_get), apps/kernel/engine/src/lib.rs:1800-1810 (node:crypto __seededRandom captures crypto.getRandomValues once -> __rand), apps/kernel/src/kernel-glue.ts:317-321 + 253-261 (WASI random_get is a SEPARATE mulberry32, independent of engine __rand)

### WALL-CLOCK TIMERS

- **findings:** setTimeout/setImmediate ignore delay (engine lib.rs:865-869); setInterval drains to 10k microtask iters (lib.rs:874); AbortSignal.timeout(ms) aborts ~immediately (lib.rs:953); IncomingMessage.setTimeout/ClientRequest.setTimeout no-ops (lib.rs:2048,2056); the documented "no wall-clock timers" caveat (lib.rs:863, 2242, 2261-2264). socket.setTimeout maps to the same gap in the net/tls shim.
- **root cause:** unwired-existing-capability · **fix:** Two-tier fix, both reusing the EXISTING __hostCall park/resume substrate (no new WASM import, no engine rewrite). TIER A (in-turn real delay, the common case): add a DO-side host effect `host.sleep(ms)` routed in `_serviceHostCall` (kernel-glue.ts:1766) that `await`s a real `setTimeout(resolve, ms)` on the DO event loop, then resumes the VM via eval_resume. Re-point the engine timer shims at it: when `config.timers === 'real'`, `setTimeout(fn,ms)` becomes `host.sleep(ms).then(fn)` (the cell must be async/top-level-await, which the eval loop already supports), `AbortSignal.timeout(ms)` arms via the same effect, and node http/socket `setTimeout(ms)` race the in-flight fetch/socket against a `host.sleep(ms)` abort. Cap ms at the platform per-turn CPU/wall ceiling (~30s default, hard 15min) and clamp; longer requests reject with a typed TimerTooLongError. Keep SEEDED-clock default semantics: even in real mode, `Date.now()` stays the seeded monotonic clock (delay is real, the VM's observed clock is not — documented). TIER B (cross-hibernation sleep, opt-in, larger lift, can ship later): for delays that should survive idle eviction, `host.sleep` with ms beyond a threshold persists a wake record to DO SQLite, sets a DO Alarm (already wired at lib.rs:1026; supervisor-owned for facets since facets can't alarm), lets the VM park and the DO hibernate; alarm()/reconnect re-enters and resumes the parked continuation. Default-off behind config.timers:'real'.
- **CF primitive:** TIER A: NONE (pure DO-side setTimeout on the host event loop, inside the existing eval turn) — no CF binding. TIER B: Durable Object Alarms (already bound, this.ctx.storage.setAlarm) for cross-hibernation wake; Queues delaySeconds is a viable alternative for the supervisor-side coarse wake but Alarms are already in the kernel and need no new binding.
- **mechanism:** Park/resume already exists and is proven by host.fetch: engine JS calls __hostCall(name,args) -> Rust stages HOSTCALL_REQ and eval_begin/eval_resume returns STATUS_HOST_CALL (engine lib.rs:3426-3473) -> glue while-loop (kernel-glue.ts:1667) reads the req, dispatches in _serviceHostCall, and feeds the result back via ex.eval_resume. TIER A adds one branch: `if (name==='sleep') { const ms=clamp(args[0]); await new Promise(r=>setTimeout(r,ms)); return {ok:true,value:null}; }`. Because the whole eval is one async DO turn and the DO event loop is live while the VM is parked, a real wall-clock delay elapses, then the VM resumes and the timer callback fires AFTER ms. The engine timer shims (lib.rs:865-875, 953) gain a real-mode branch that calls `globalThis.host.sleep(ms)` instead of queueMicrotask. ms is clamped to the per-turn budget; AbortSignal.timeout and http/socket setTimeout become `Promise.race([inflight, host.sleep(ms).then(abort)])`. TIER B: when ms exceeds the in-turn threshold, host.sleep writes {wakeAt, parkedContinuationId} to SQLite, the glue returns a special PARK_HIBERNATE status that lets evalCode return a 'parked' envelope; the DO sets an alarm for wakeAt; on alarm()/next connect the DO restores the snapshot and calls eval_resume with the sleep result, the continuation runs. Single-alarm-per-DO means concurrent long sleeps need a min-heap in SQLite (the next-wake earliest); for facets the supervisor owns the alarm.
- **determinism:** Default UNCHANGED and byte-identical: shipped behind config.timers:'real' (opt-in), exactly mirroring the existing config.clock:'real' precedent. In default seeded mode setTimeout stays immediate-microtask and determinism/restore is untouched. In real mode the DELAY is real but Date.now()/the VM clock remain the SEEDED monotonic clock (host.sleep adds zero entropy to the VM — like host.fetch which is documented as adding 0 entropy), so a real-mode session is still reproducible in its observed values; only wall-time pacing differs, which is non-reproducible BY DEFINITION (same trade already accepted for clock:real). So: does NOT break seeded byte-identical restore; opt-in flag gates the real-delay behavior. · **snapshot:** TIER A: ZERO snapshot impact — the sleep is entirely DO-side; the VM is parked exactly as it is for an in-flight host.fetch, no timer state lives in the heap, nothing new is serialized. A snapshot taken while a TIER-A sleep is in flight is identical to one taken mid-fetch (the parked continuation re-drives on resume). TIER B: adds a small wake-record row to DO SQLite (wakeAt, continuation id) and relies on snapshotting the parked VM heap (same heap the existing per-cell snapshot already captures) so the continuation can resume post-hibernation; the heap image is unchanged in shape — only a manifest/wake-queue row is added. No effect on the ~24-28MB monotonic snapshot cliff. · **imports:** ZERO. Reuses the single existing __hostCall WASM rendezvous import already counted in the 12-ceiling (same path as host.fetch/ws.*/socket.*/sandbox.*). No new wasm import slot consumed — host.sleep is just another string-named effect dispatched DO-side in _serviceHostCall.
- **effort:** M · **risk:** medium · **feasibility(self):** feasible
- **VERIFY [revise→feasible]:** TIER A core mechanism is REAL and already proven in this codebase. workerd does NOT count setTimeout/I/O wait against the 30s-default CPU limit (configurable to 5min via limits.cpu_ms); wall-clock is effectively unlimited while a request processes, so `await new Promise(r=>setTimeout(r,ms))` inside the live eval turn genuinely elapses real wall time then eval_resume fires the callback. CONFIRMED by engram's own WS pump (kernel-glue.ts:2437-2491 _wsRecv): the DO event loop is live while a cell is parked in an await and setTimeout macrotasks resolve mid-eval-turn. host.sleep needs no new CF binding and no new WASM import — just another string-named effect in _serviceHostCall (dispatch chain verified: __hostCall -> STATUS_HOST_CALL=1 (engine lib.rs:3191,3631) -> glue while-loop (kernel-glue.ts:1667) -> await _serviceHostCall (1682) -> eval_resume (1687)). TIER B Alarms claim is real (ctx.storage.setAlarm; engram uses set_alarm at lib.rs:1026) and 'facets cannot set alarms' forces supervisor-owned wake — acknowledged. WRONG/omitted: (a) an active timer / in-flight request PREVENTS DO hibernation and BILLS duration — a TIER-A sleep pins the kernel resident for its full ms, opposing the idle-eviction thesis; the 'hard 15min' TIER-A cap is unsafe. (b) WS idle timeout is 100s on Free/Pro (engram Paid, partially mitigated) — a multi-minute TIER-A park risks the client WS dropping mid-sleep with no resume in TIER A.
  - blockers: 1) GUARD-CAP COLLISION (concrete, missed): host.sleep as a generic effect counts against maxHostCalls (default 64, kernel-glue.ts:1646). A cell arming many real timers — especially setInterval (currently up to 10000 microtask ticks, engine lib.rs:874) — exhausts the 64-cap and aborts. Needs its OWN budget bucket like streamOps/wsOps/fsOps; real-mode setInterval needs a hard re-arm count. 2) HIBERNATION/BILLING CONTRADICTION (TIER A): pending setTimeout blocks hibernation and bills duration the whole sleep — fine for sub-second delays but 'common case ~30s default' pins a kernel resident 30s. Cap should be single-digit seconds, not 15min. 3) config.timers is net-new plumbing: must thread through create AND persist across cold-wake like config.clock (snapshot manifest) or a restored session silently loses real-timer mode. 4) Promise.race([inflight, host.sleep]) for http/socket timeout works because the inflight side is its own awaited host effect — but a single long sleep park does NOT pump other I/O macrotasks (the exact bug at kernel-glue.ts:2444); must not be used as a generic poll. 5) TIER B is under-scoped as 'M, ship later' — it is an L: new 'parked' eval protocol status the SDK/CLI must handle, mid-continuation heap snapshot (existing snapshots are BETWEEN cells not mid-host-call; the parked __hostCall state lives in JS heap so it CAN survive but resuming the eval_resume rendezvous post-hibernation is unproven), a SQLite min-heap wake queue (single-alarm-per-DO), supervisor-owned alarms for facets. Do NOT bundle TIER B feasibility with TIER A.
  - evidence: apps/kernel/src/kernel-glue.ts:1667-1688 (async host-call pump: await _serviceHostCall then eval_resume — TIER A adds one branch here), apps/kernel/src/kernel-glue.ts:1646 (maxHostCalls default 64 guard — host.sleep counts against it), apps/kernel/src/kernel-glue.ts:2437-2491 (_wsRecv proves DO event loop live while cell parked + single-long-setTimeout-doesnt-pump-other-IO caveat at :2444), apps/kernel/engine/src/lib.rs:865-875,953 (current immediate-microtask setTimeout/setInterval/AbortSignal.timeout shims to re-point in real mode), apps/kernel/engine/src/lib.rs:3191,3631,3458 (STATUS_HOST_CALL=1, eval_resume rendezvous reused)

### VM ISOLATION

- **findings:** Audit row #1 (engine/lib.rs:2147,2240); catalog entries "vm module" (lib.rs:313), "vm.runInNewContext [stub-unacceptable]" (lib.rs:1660); systemic finding #5 (single-heap serial eval makes runInNewContext un-isolatable); security-critical honorable-mention (line 166). Does NOT cover vm.runInThisContext (correctly stub-acceptable: same realm IS intended Node semantics).
- **root cause:** unwired-existing-capability · **fix:** Two-part; ship the cheap part now. PART A (ship-now, XS): stop the silent footgun. Keep runInThisContext/Script.runInThisContext same-realm (correct Node semantics). For runInNewContext/runInContext/createContext: (1) one-time console.warn that engram's vm provides NO context/security isolation (same QuickJS realm) and to use s.invokeWorker for a real isolate; (2) harden the shim to deny ambient globalThis by running the body inside `with(sandboxProxy){...}` where the proxy traps has=>true and resolves only ctx keys — kills casual prototype-pollution/global-read escapes for template-engine use while staying explicit it is not a security boundary. PART B (real isolation, M): add a NEW async API vm.runInNewContextAsync(code, ctx, opts) that routes to the already-live Worker-Loader registry (LOADER.load one-shot isolate, globalOutbound:null), evals there, returns JSON-marshalled result via the existing __hostCall park/resume path — genuine V8-isolate isolation. Do NOT silently make the sync runInNewContext async (breaks every existing caller and Node's sync contract).
- **CF primitive:** Dynamic Worker Loader (LOADER binding) — already bound and in production via registryInvoke/registerWorker/invokeWorker. PART B reuses it via LOADER.load() (always-fresh isolate). PART A is pure-impl (NONE).
- **mechanism:** PART A (no host call, in-VM pure JS at engine/src/lib.rs:2147): rewrite runInNewContext to build `const p=new Proxy(ctx,{has:()=>true,get:(t,k)=>k in t?t[k]:undefined}); return Function('__sb','with(__sb){return eval(code)}')(p)` so every bare identifier resolves against the sandbox proxy first (unqualified globals read undefined instead of escaping); ctx mutations stay on the passed object; one-shot console.warn on first call. Still same realm so `globalThis.x` and Function.prototype reflection escapes remain (documented), but the casual-read footgun is closed with zero new machinery. PART B (real isolation): new in-VM vm.runInNewContextAsync calls __hostCall('vm.runIsolated',{code,ctx}) → parks the QuickJS request and returns a Promise via the same park/resume slot used by host.fetch/invokeWorker (lib.rs:740). Host glue (new branch near registryInvoke in kernel-glue.mjs) does env.LOADER.load({mainModule:'m.js', modules:{'m.js':WRAPPER_SRC,'user.js':code}, env:{__ctx:ctxJson}, globalOutbound:null, limits:{cpuMs:opts.timeout||5000}}).getEntrypoint().fetch(), returns the JSON result string, resolves the parked Promise. Wrapper reconstructs ctx from JSON, evals user code with those as the only bindings (fresh isolate = real globalThis isolation; globalOutbound:null = no egress), JSON-stringifies the completion value back. Marshalling is structured-JSON only: primitives/arrays/plain objects cross; functions/closures/Promises/class instances do NOT (documented hard limit — same constraint registryInvoke already lives with; security-sensitive callers pass data contexts, not live function graphs).
- **determinism:** PART A: zero entropy added (pure JS rewrite); seeded byte-identical restore unaffected. PART B: the child isolate has its own runtime (workerd-default Math.random/Date/crypto = NON-deterministic) unless the wrapper re-seeds from opts.seed; keep it OFF the determinism contract by making runInNewContextAsync OPT-IN (new API, not the sync path) and documenting isolated eval as non-deterministic by default, with opts.seed to inject the parent LCG seed for byte-identical behavior when needed. The sync runInNewContext path keeps the parent's seeded clock/RNG, so existing deterministic sessions are untouched. Net: determinism-by-default preserved (no existing path changes semantics). · **snapshot:** PART A: none — pure in-heap JS, already snapshotted. PART B: the child isolate is ephemeral (one-shot LOADER.load, not snapshotted); only the small JSON result lands back in the QuickJS heap, so snapshot size is bounded by result size, not the isolate's working set — actually a snapshot-cliff RELIEF valve (heavy computation runs outside the monotonic WASM linear memory). No new monotonic growth. A runInNewContextAsync in flight at eviction is lost exactly like an in-flight host.fetch (same existing semantics); cold-restore otherwise unaffected. · **imports:** Zero. Both parts are host-side JS / in-VM JS. LOADER is a Worker-Loader binding accessed from the DO glue layer, not a WASM import — does not touch the 12-import ceiling (already bound and used by registryInvoke). PART A is pure QuickJS-heap JS. No new WASI/WASM imports.
- **effort:** M · **risk:** low · **feasibility(self):** ship-now
- **VERIFY [revise→feasible]:** CF Worker Loader binding is real and exposes BOTH methods the proposal conflates: `.load(code)` = fresh isolate every call (no ID cache), `.get(id, cb)` = warm-cached by ID (CF docs confirmed). globalOutbound:null is real and documented ("totally cut off from the network", fetch()/connect() throw). The live engram path (registryInvoke, kernel-glue.ts:523) uses `.get(codeId,cb)` (WARM-cached), NOT `.load()` as the proposal repeatedly states — for Part B's "always-fresh isolate / genuine isolation" goal `.load()` is the correct call, which engram does NOT currently use (new call, not a blocker). limits:{cpuMs} is already passed in the live registryInvoke path so CPU caps work in practice. A DO CAN invoke Worker Loader — engram does it today via the JS glue (LOADER is JS-shaped; Rust passes env as raw JsValue). JSON-only marshalling is honest: same constraint registryInvoke already lives with.
  - blockers: PART A: none — ship-now, accurately XS. Shim is exactly at engine/src/lib.rs:2147; pure in-VM JS, no host call, zero entropy, snapshot-safe; the with(sandboxProxy) rewrite is valid QuickJS JS that closes the casual ambient-global READ footgun (globalThis.x / Function.prototype reflection escapes honestly remain same-realm). PART B: real ARCHITECTURAL PLACEMENT ERROR. The proposal says add the LOADER branch 'near registryInvoke in kernel-glue.mjs' via the eval park/resume path — but registry_invoke runs at the DO FRAME level (lib.rs:1757, worker_invoke handler dispatched as a separate {t:'worker-invoke'} frame OUTSIDE an active eval), NOT inside the evalCode host-effect loop. The GlueKernel's _serviceHostCall (where a new vm.runIsolated host effect would land, kernel-glue.ts:1766) does NOT hold env.LOADER or ctx.exports.VfsGateway — those arrive as raw JsValue from Rust ONLY in the frame handler. The closest in-eval analog (_doSandbox) reaches its target via a plain fetch() to an injected URL, not via a binding. So Part B needs MORE plumbing than stated: either thread env.LOADER + ctx.exports into the GlueKernel (via a setLoaderEnv-style installer mirroring setSandboxConfig), or route the parked effect back out to lib.rs to call registry_invoke. The park/resume slot (lib.rs:740, __hostCall) IS real and re-entrant-safe, so once env is reachable the mechanism works — but effort is M+ (binding plumbing), not the clean drop the writeup implies. Secondary (sound): child-isolate non-determinism is correctly flagged and correctly kept OFF the determinism contract by making it a NEW opt-in async API rather than mutating the sync runInNewContext.
  - evidence: apps/kernel/engine/src/lib.rs:2147 (runInNewContext shim — Function(ks).apply, same realm, exactly as proposal claims), apps/kernel/engine/src/lib.rs:2240 (vm self-describes 'NO security/context isolation boundary'), apps/kernel/src/kernel-glue.ts:523 (registryInvoke uses LOADER.get(codeId,cb) WARM-cache, NOT .load(); globalOutbound:null; limits:{cpuMs} live), apps/kernel/src/lib.rs:1757 (registry_invoke called from worker_invoke FRAME handler, env.as_ref()+ctx as JsValue — outside the eval loop), apps/kernel/src/kernel-glue.ts:1766 (_serviceHostCall name-keyed switch — where vm.runIsolated would slot, but NO env.LOADER ref here)

### SNAPSHOT INCOMPRESSIBLE CLIFF (THE DRAGON)

**2026-06-15 status:** local fault-injection gates landed. `docs/CHAOS-FAULT-INJECTION-VERDICT.md`
records the fixed-tree run. The snapshot path now has a measured raw restore ceiling constant
(`MEASURED_RESTORE_RAW_CEILING_BYTES = 60MiB`) in addition to the existing 24MiB incompressible
guard. The remaining work is a throwaway Cloudflare R2-overflow run if we need production R2
read-after-write timing evidence; prod `engram-kernel` was not touched.

- **findings:** The ~28MB incompressible-heap OOM cliff (audit line 170); the separate ~256MB monotonic-buffer single-session unrecoverable DO-kill (audit lines 126,170); "uncatchable WS-1006" framing in the audit exec summary (line 24). Also touches: zlib large-input in-memory path tripping the size-admission guard (line 1269 div 4), 500KB stdlib cap driven by the same cliff (line 2023).
- **root cause:** hard-platform-limit · **fix:** Split into three honest layers. (1) RAISE-THE-CEILING-IN-KERNEL = INFEASIBLE, declare hard limit: WASM linear memory is monotonic and dlmalloc does no downward compaction; the dump's transient ~2-3x gz/copy expansion (memory.buffer.slice(0) at kernel-glue.ts:2600 + zstdCompress) is what OOMs the DO, NOT the R2 write — so R2 multipart/streaming, D1, and KV offload of the STORED image cannot raise the in-kernel ceiling. True in-place compaction needs value-serialization that loses promise/closure/exotic-namespace fidelity (the whole thesis). Document ~24MB incompressible as the immovable in-heap ceiling. (2) ALREADY FAIL-SAFE for the SNAPSHOT path — close the audit's stale 'uncatchable' wording: the INCOMPRESSIBLE_BUFFER_CEILING_BYTES=24MB guard (kernel-glue.ts:920,2581) already throws a typed SizeAdmissionError BELOW the ~28MB cliff, socket alive, reset recovers. The genuinely uncatchable residue is narrow: native-builtin alloc bombs (.fill on huge TypedArray — no bytecode interrupts, outruns mid-cell tripwire) and a single cell that grows the buffer past the guard in one synchronous native burst before any checkpoint. Pure-impl hardening: add a buffer_bytes() re-check inside the mid-cell tripwire that ALSO fires after returning from a native host shim / large typed-array constructor, and clamp ArrayBuffer/TypedArray/Buffer.alloc/.fill length args in the engine to (INCOMPRESSIBLE_BUFFER_CEILING_BYTES - current incompressible) so a giant single alloc throws RangeError in-VM instead of growing the buffer past the catch point. This converts most native-bomb kills into catchable in-VM RangeErrors with ZERO new imports. (3) RAISE EFFECTIVE WORKING-SET via offload to existing muscle = the real ceiling-raiser, opt-in: for sessions that legitimately need >24MB of working data, do NOT hold it in the QuickJS heap — push it to the already-wired Tier-2 sandbox container (real Linux, 256MB-20GB, separate process, shares the R2 /workspace) or a Tier-1 Worker-Loader isolate, and keep only handles/cursors in the snapshottable heap. This is the documented 'snapshot-cliff escape hatch' in the CF menu and needs no kernel rewrite — it is a usage pattern + a thin host.offload(key, value) -> R2/sandbox helper.
- **CF primitive:** NONE for the in-kernel ceiling (hard limit). For the working-set escape valve (layer 3): already-bound Containers/@cloudflare/sandbox (Tier-2, ENGRAM_SANDBOX_URL) + R2 (SNAPSHOTS bucket / shared /workspace) + Worker Loader (LOADER, Tier-1) — all already bound, no new binding. Layer-2 hardening is a pure-impl engine fix (NONE).
- **mechanism:** Layer 1 (declare limit): no code change; doc update in SYSTEM-LIMITS.md + correct NODE-FIDELITY-SCALE-AUDIT.md line 24 to say 'fail-safe-rejected for the snapshot path, uncatchable only for the narrow native-alloc-bomb residue'. Layer 2 (pure-impl, host-call/engine): in engine/src/lib.rs intercept the TypedArray/ArrayBuffer/Buffer allocation builtins to compute requested-bytes + current incompressible-extent and throw a JS RangeError if it would cross INCOMPRESSIBLE_BUFFER_CEILING_BYTES; and have the existing interrupt-tripwire read buffer_bytes() not only on bytecode interrupts but also immediately after any __hostCall return, so a native shim that grew memory is caught on the next instruction. No park/resume, no host-call round-trip added to the hot path (the cap check is a synchronous in-engine comparison). Layer 3 (binding-backed escape valve): add host.offload(key, blob) and host.fetchOffloaded(key) that route through the existing DO-side fetch to the sandbox (or a plain R2 put/get under fs/<doId>/offload/<key>); the QuickJS heap keeps only the string key. Large arrays/buffers live in R2 or the container, the heap stays small, snapshot stays under the cliff. Mechanism = existing host-call dispatch (__hostCall) + existing R2/sandbox fetch — no new WASM import.
- **determinism:** Layer 1: none. Layer 2: none — clamping a giant allocation to throw RangeError is deterministic and identical across seeded restore (the throw is a pure function of requested size + current heap extent, both already deterministic). Does NOT break byte-identical seeded restore; no opt-in needed, safe by default. Layer 3: offload to R2 is deterministic-neutral for reads of previously-written deterministic data; offload to the sandbox container introduces real (non-seeded) compute/IO and MUST be opt-in (capability-gated like the existing s.sandbox surface) — a session that uses it forfeits byte-identical replay for that data, exactly like the existing host.fetch/sandbox capabilities already do. R2-only offload of pure data keeps determinism. · **snapshot:** Layer 1/2: snapshot path unchanged; layer 2 strictly REDUCES the chance of an over-cliff buffer reaching the dump, so MORE sessions stay snapshottable. Layer 3: dramatically shrinks the snapshotted heap for big-working-set sessions (only handles/keys persist), so they snapshot/restore fast and never approach the cliff; the offloaded blobs live in R2 under fs/<doId>/offload/ (durable, already in the same data plane as the unified /workspace) — they survive eviction independently and are re-fetched lazily on restore. Net: the snapshot becomes a small index over large external state. · **imports:** Zero new WASM import slots consumed in any layer. Layer 2 is in-engine Rust (no import). Layer 3 routes through the existing __hostCall dispatch and the existing DO-side R2/sandbox fetch — host calls are a single multiplexed import already present, not per-capability imports. The 12-import ceiling is untouched.
- **effort:** M · **risk:** medium · **feasibility(self):** feasible
- **VERIFY [revise→feasible]:** All three layers are consistent with real CF behavior. (1) WASM linear memory IS monotonic and dlmalloc does no downward compaction — confirmed by code + docs; the OOM is the DUMP's transient resident expansion, not the R2 write: kernel-glue.ts:2600 does `new Uint8Array(ex.memory.buffer.slice(0))` (a FULL second copy) then :2621 `zstdCompress(raw)` (a third buffer), all inside the same ~128MB DO RAM budget. So R2 multipart/streaming/D1/KV offload of the STORED image genuinely cannot raise the in-kernel ceiling — Layer 1 is correct. (2) The existing INCOMPRESSIBLE_BUFFER_CEILING_BYTES=24MB guard at kernel-glue.ts:2581 throws a typed SizeAdmissionError BEFORE the slice/compress — so the snapshot path is ALREADY fail-safe-rejected, exactly as the proposal claims. (3) Layer-3 offload: a DO calling R2 put/get is a normal async fetch round-trip (already done verbatim by sandbox_frame at src/lib.rs:1873 and the R2 fs path) and routes through the existing single multiplexed __hostCall import (engine line 746/2934) — ZERO new WASM imports, 12-import ceiling untouched. CF reality holds for all three.
  - blockers: REVISE drivers (not rejection): (1) Layer 2's NEW engine code is OVERSOLD. The post-cell buffer-growth tripwire ALREADY exists (engine finalize_cell:3243-3262) and ALREADY converts a single native alloc into a typed MemoryLimitError WHEN THE CELL RETURNS. The true residue is narrower than stated: one synchronous native burst that crosses ~28MB BEFORE the cell yields. The proposed fix (clamp ArrayBuffer/TypedArray/Buffer.alloc length args to throw RangeError pre-growth) is feasible — precedent exists (scryptSync param-clamp at engine:1839; stack-guard catchable RangeError) — but QuickJS/rquickjs has NO native alloc-size hook, so it must SHADOW the global constructors in injected JS (like the EngramDate/EngramRS prototype patches), which is fragile: it catches the canonical `new Uint8Array(40MB)` but is bypassed by `String.prototype.repeat`, `Array(n).fill().join`, `ArrayBuffer.prototype.resize/transfer`, `%TypedArray%`/`Reflect.construct` subclassing, and huge-string `JSON.parse`. So 'converts MOST native-bomb kills into catchable RangeErrors' is too strong — it converts the textbook case, leaves a long tail. (2) The 'buffer_bytes() re-check after __hostCall return' is largely redundant: host calls are async-parked (Promise), and the microtask pump (engine:3576) already runs the interrupt handler with the growth tripwire — marginal value. (3) Layer 1/2 audit-correction is LOW value: NODE-FIDELITY-SCALE-AUDIT.md line 24/124/170 ALREADY says 'fail-safe-rejected... uncatchable only for the narrow native-alloc-bomb residue' — the 'stale uncatchable wording' is already nuanced; this is a no-op doc touch-up. (4) Effort 'M' is honest for Layer 3 + docs but UNDERSTATES the constructor-clamp's sibling-coverage fiddliness; realistic to land it as 'catches the common case' not 'closes the residue'. Net: ship Layers 1+3 as-is, ship Layer 2 but DOWNGRADE its claim to 'clamps the canonical single-giant-alloc bomb, does NOT close the residue'.
  - evidence: apps/kernel/src/kernel-glue.ts:2581 (INCOMPRESSIBLE guard throws before dump), apps/kernel/src/kernel-glue.ts:2600 (memory.buffer.slice(0) full copy), apps/kernel/src/kernel-glue.ts:2621 (zstdCompress(raw) third buffer), apps/kernel/src/kernel-glue.ts:883,888,890 (SCRATCH_DELTA 31MB, MAX_USED 50MB, SAFE_SERIALIZE 76MB), apps/kernel/engine/src/lib.rs:3243-3262 (post-cell buffer-growth tripwire ALREADY exists -> typed MemoryLimitError)

### FS metadata fidelity

- **findings:** (1) statSync/stat() always returns mtimeMs/atimeMs/ctimeMs=0; (2) readdir withFileTypes returns bare strings (not Dirent) on the ASYNC/R2 host path (hReaddir); (3) existsSync semantics on R2-only paths (returns false rather than reflecting the durable R2 object → downstream ENOENT, the "existsSync throws" symptom). Confirms determinism implication of stamping mtime.
- **root cause:** pure-impl-gap · **fix:** Three-part pure-JS + Rust-glue fix, NO new CF primitive:

(A) IN-HEAP VFS (default provider, apps/kernel/engine/src/lib.rs ~2374-2395): change the file record shape from `{data, mtime:0}` to `{data, mtime, ctime, birthtime}` and stamp it from the VM's ALREADY-SEEDED clock (`Date.now()` inside the VM, which routes through the seeded `__now`/clock facade — NOT the host wall-clock). writeFileSync/appendFileSync/copyFileSync/symlinkSync set mtime=ctime=Date.now(), birthtime preserved on overwrite. statSync returns mtimeMs/ctimeMs/atimeMs/birthtimeMs from the record (atime aliases mtime; we do not track real access time), plus mtime/ctime/birthtime Date objects, nlink/dev/ino synthesized deterministically. This is the default code path and the one the finding's stat()=0 hits → it becomes non-zero AND deterministic. Directory mtime = max child mtime or dir-creation stamp held in a parallel `V.dirMeta`.

(B) ASYNC R2 HOST PATH (hReaddir/hStat ~2447, and DO-side vfs_stat/vfs_list ~1473-1556): (1) extend host.__fs `list` to return `entries:[{name,isDir}]` alongside the legacy `names:[]`; hReaddir maps entries→Dirent[] when withFileTypes, falling back to names→strings only if entries absent (back-compat). (2) hStat already forwards r.mtimeMs — wire vfs_stat to return mtimeMs from the new persisted column (see C). (3) hExists/existsSync-over-R2: make the async `exists`/`access` consult the committed fs_files row + R2 HEAD fallback (the path vfs_stat already uses), so existsSync-equivalent on an R2-only/container-written file is TRUE, not a downstream ENOENT.

(C) DURABLE MTIME (apps/kernel/src/lib.rs fs_files table): add `mtime_ms INTEGER` via the same `ALTER TABLE fs_files ADD COLUMN` idempotent-migration pattern already used for etag/origin (line 387). Populate it NOT from host `now_ms()` (js_sys::Date::now, real wall-clock = non-deterministic) but from a per-cell mtime value PASSED DOWN from the VM in the staged fs op (the VM's seeded clock), so a seeded session's fs_files.mtime_ms is byte-identical across restore. Container-written / direct-R2 files (no fs_files row) fall back to the R2 object's real uploaded timestamp via HEAD — explicitly flagged non-deterministic (origin='frame'/'container'), acceptable because those are real external I/O already outside the determinism envelope.
- **CF primitive:** NONE (pure-impl fix). Reuses already-bound R2 (SNAPSHOTS) HEAD for the container/direct-R2 fallback timestamp and existing DO SQLite for the manifest column — no new binding.
- **mechanism:** In-heap VFS: file/dir records grow mtime/ctime/birthtime fields stamped from the in-VM seeded clock at write time; statSync/Dirent read them back — pure JS, one realm, no host call. R2 host VFS: host.__fs gains an entries[] field (Dirent source) and an mtime_ms passthrough; the DO Rust glue persists mtime_ms in fs_files (new ALTER column, committed atomically with the heap manifest in the same staged-commit txn as r2_key/size/origin), and vfs_stat/vfs_list serve it. For files with no fs_files row (sandbox container wrote straight to R2), the DO does an R2 HEAD and uses the object's uploaded timestamp — clearly an external-I/O value. existsSync-over-R2 is fixed by routing the async exists/access through the same committed-row + R2-HEAD lookup vfs_stat already performs, so a durable file reads as present.
- **determinism:** Does NOT break seeded byte-identical restore IF mtime is sourced from the VM's seeded clock, not the host wall-clock. CRITICAL: the existing host `now_ms()` = js_sys::Date::now() is real wall-clock and would poison determinism — the fix MUST stamp fs_files.mtime_ms from the VM-supplied seeded time (carried in the staged op), and the in-heap path uses the in-VM seeded Date.now() directly. With that, a seeded replay produces identical mtime bytes in both the heap snapshot and fs_files. The ONLY non-deterministic mtime is for container/direct-R2 files (no fs_files row → R2-HEAD real upload time): these are already real external I/O outside the seeded envelope, so no regression; they should carry a deterministic=false marker. No opt-in flag needed for seeded sessions — it stays deterministic by default. (atime is aliased to mtime, not real access-time tracking, to avoid every read mutating snapshot state and bloating deltas.) · **snapshot:** In-heap VFS: each file record gains ~3 numbers (mtime/ctime/birthtime) → trivial heap growth (~24 bytes/file), well inside the snapshot envelope; survives hibernate/restore natively as part of the heap blit (no new machinery). fs_files: one new INTEGER column, committed in the SAME staged-commit txn as existing columns → no new commit ordering, no atomicity change, no torn-manifest risk. Directory mtime tracking (V.dirMeta) adds a small parallel map. Net snapshot-size impact: negligible; no effect on the 24-28MB monotonic cliff. · **imports:** Zero. Consumes NO WASM import slot — the in-heap path is pure JS in the existing realm; the R2 path reuses the existing host.__fs round-trip channel and the already-bound R2/SQLite host bindings on the DO (JS/Rust side, not WASM imports). Stays well under the 12-import ceiling.
- **effort:** M · **risk:** low · **feasibility(self):** feasible
- **VERIFY [revise→feasible]:** No new CF API is invoked — this is a pure-impl fix over already-bound primitives (DO SQLite, R2 SNAPSHOTS), so there is no CF-limit/consistency/async surprise to clear. The ONE CF-reality claim that matters is determinism: the proposal correctly identifies that fs_files.created_ms is stamped from now_ms() = js_sys::Date::now() (apps/kernel/src/lib.rs:3997-3998, real wall-clock), which would poison seeded byte-identity. Verified true. The R2-HEAD fallback for container/direct-R2 files (no fs_files row) is real and already used by vfs_stat (lib.rs:1500-1508) and r2_fs_op read (lib.rs:~4345) — R2 HEAD on a DO is a normal async bucket.head() call, no fetch round-trip needed, bucket binding present. The in-VM seeded Date.now() (engine lib.rs:198, __now facade) is the correct deterministic mtime source and is real. So the CF/determinism story holds — but ONLY if the fix is wired to the correct handler (see blockers), which the proposal partially mis-locates.
  - blockers: (1) HANDLER MIS-LOCATION — the proposal's part (B)(2) says 'hStat already forwards r.mtimeMs — wire vfs_stat to return mtimeMs'. But hStat (engine lib.rs:2447) calls host.__fs op='stat', which is served by r2_fs_op (apps/kernel/src/lib.rs:4479-4494), NOT vfs_stat. r2_fs_op's 'stat' arm sets ok/isFile/isDirectory/size and emits NO mtime field at all → hStat's `r.mtimeMs||0` is always 0 today. vfs_stat (lib.rs:1473) is a SEPARATE 'vfs-*' RPC path used by the sandbox/SDK, not by the VM's fs.promises.stat. The fix must add mtime_ms to r2_fs_op's 'stat' arm (and 'list' for Dirent), not (or in addition to) vfs_stat. (2) FsStage HAS NO mtime FIELD — the staged write op (FsStageOp::Write(Vec<u8>), lib.rs ~struct) carries only bytes; r2_fs_op 'write' (lib.rs:4442) reads only path+bytes+chunk from the payload. Passing a VM-seeded mtime down requires: extending the host.__fs write payload, extending FsStage/FsStageOp, threading it through flush_staged_fs (lib.rs:1177-1217) into the INSERT (replacing `now` for the mtime_ms column). This is more than 'one ALTER column' — it's a payload+struct+flush-path change. Effort is honest-ish at M but skews toward M-high. (3) r2_fs_op 'list' returns ONLY `names:[]` (lib.rs:4515-4526), no per-entry isDir → hReaddir(withFileTypes) over the host provider genuinely cannot build Dirent today; part (B)(1) is real, necessary work (vfs_ls already has entries[] but is the wrong handler). (4) existsSync-over-R2: existsSync (engine lib.rs:2375) is SYNC and in-heap-only; under a host provider it can't consult R2 (the whole provider split is sync-throws/async-only). The fix can only make the ASYNC fs.promises.access/exists durable-aware (hExists, lib.rs:2453, already calls stat) — calling it 'existsSync-over-R2' is a misnomer; sync existsSync over an R2-only file is structurally impossible without sync-over-async (explicitly listed as NOT built in CLAUDE.md). (5) MINOR determinism: in-heap directory mtime = 'max child mtime' makes a dir's mtime mutate on every child write — fine for seeded replay (deterministic) but adds heap delta churn; acceptable. atime-aliases-mtime (no read-time mutation) is the correct call and the proposal makes it.
  - evidence: apps/kernel/engine/src/lib.rs:2395 (in-heap statSync returns mtimeMs:0 — the finding's default-path bug), apps/kernel/engine/src/lib.rs:2447 (hStat reads r.mtimeMs from host.__fs op=stat), apps/kernel/src/lib.rs:4479-4494 (r2_fs_op 'stat' arm emits NO mtime — the real host handler hStat hits, NOT vfs_stat), apps/kernel/src/lib.rs:4515-4526 (r2_fs_op 'list' returns names[] only, no isDir → no host-path Dirent today), apps/kernel/src/lib.rs:1217 (flush_staged_fs INSERT stamps created_ms from `now`)

### FS TREE SEMANTICS

- **findings:** (1) renameSync on a directory moves only the dir's own V.dirs marker and orphans every V.files['old/...'] / V.dirs['old/...'] child at the dead prefix (engine/src/lib.rs:2393). (2) symlinkSync writes the target as a plain regular file (lib.rs:2400) so lstat().isSymbolicLink() is false. (3) readlinkSync always throws EINVAL even for a path created by symlinkSync (lib.rs:2399) — broken round-trip. (4) lstatSync aliased to statSync (lib.rs:2522), no link semantics. (5) Durable fs_files manifest (apps/kernel/src/lib.rs:373) and engram-fs FsEntry (packages/engram-fs/src/types.ts:37) are flat path->body rows: no parent, no type, no symlink_target — same orphaning/symlink gaps on the durable + container/gateway-reconcile path and the directory rename does not rewrite child R2 keys.
- **root cause:** pure-impl-gap · **fix:** Pure-impl fix in two layers, no CF primitive — the store (DO SQLite fs_files) already exists; the gap is schema + algorithm. (A) IN-VM facade (engine/src/lib.rs, the hot path libs actually hit): (A1) Fix renameSync to be a prefix-move: when V.dirs[a] exists, iterate Object.keys(V.files) and Object.keys(V.dirs), and for every key === a or starting with a+'/', re-key to b + key.slice(a.length); delete the olds. This is the load-bearing correctness fix (isomorphic-git, bundlers, mv). (A2) Model symlinks with a third map V.links (path -> targetString) instead of faking a file: symlinkSync sets V.links[p]=target; readlinkSync returns V.links[p] or throws EINVAL only if absent; lstatSync returns {isSymbolicLink:()=>true,size:target.length,...} when V.links[p], else statSync; statSync/readFileSync resolve one hop through V.links (bounded, cycle-guarded) so following a link reads the target. existsSync/readdirSync count links. (B) DURABLE manifest (fs_files + engram-fs FsEntry) — additive columns: ALTER TABLE fs_files ADD COLUMN ftype TEXT DEFAULT 'file' (file|dir|symlink) and ADD COLUMN link_target TEXT (same exec_ignore idempotent-migration pattern already at lib.rs:387). Directory rename in the durable path becomes a manifest UPDATE path=replace(path, old, new) over all rows under the prefix PLUS an R2 copy-key+delete loop (or, cheaper, keep r2_key stable and only rewrite the logical path column — the r2Key indirection in FsEntry was designed exactly so 'no data migration ever needed': rename only touches the manifest path column, R2 bodies stay put). Symlink rows store ftype='symlink'+link_target with no R2 body. parent is derivable from path (no stored parent column needed — readdir already prefix-scans), so 'model a tree' = add type+symlink_target + make rename prefix-correct, not a full inode rewrite.
- **CF primitive:** NONE (pure fix). Manifest already lives in the already-bound DO SQLite (this.ctx.storage.sql); no new binding. D1 from the menu is the WRONG choice here (per-session tree is single-DO-local; a network hop per fs op would wreck the hot path and add no value — explicitly rejected).
- **mechanism:** End-to-end: a cell calls fs.renameSync('/workspace/a','/workspace/b'). IN-VM: the fixed renameSync rewrites all V.files/V.dirs keys under the 'a' prefix to 'b' in the live QuickJS heap — children move atomically within the single-threaded turn. fs.symlinkSync(target,p) records V.links[p]=target; a later readlinkSync(p) returns target; lstat distinguishes the link; stat/readFile resolve one hop. At checkpoint, flush_staged_fs / reconcile_fs_files (apps/kernel/src/lib.rs) write fs_files rows; symlink entries get ftype='symlink', link_target=<target>, no R2 body; a durable directory rename issues an UPDATE rewriting the path column for every row under the old prefix (R2 keys untouched thanks to the r2_key indirection in FsEntry). On cold restore the QuickJS heap blit restores V.files/V.dirs/V.links verbatim (it's in linear memory); the fs_files tree columns rehydrate the durable view for the container/gateway. The container (s3fs mount) and VfsGateway see the corrected logical paths via the manifest; reconcile_fs_files keeps the ftype column ('reconcile' origin rows default ftype='file' since R2-only writers can't carry link metadata — documented limitation, container can't create in-VM-visible symlinks anyway).
- **determinism:** NONE — does not break seeded byte-identical restore, and needs no opt-in. fs is a pure in-heap byte/path store with no clock/RNG/entropy input; rename and symlink are deterministic functions of their string args. Two seeded sessions replaying the same fs ops produce byte-identical V maps and identical fs_files rows. Default-on is safe (it is a correctness fix, not a behavior toggle). mtime stays 0 as today (already deterministic). · **snapshot:** Negligible/positive. The in-VM fix changes nothing structural in the heap: V.links is one more small map in the same linear memory already snapshotted; rename re-keys existing entries (no net byte growth — same bodies, moved keys), so the heap snapshot size is unchanged and the ~24-28MB monotonic cliff is untouched. The durable side adds two nullable TEXT columns to fs_files (a few bytes/row in the coalesced SQLite flush, same commit txn as today). The r2_key-indirection means a directory rename does ZERO R2 data movement (only manifest path-column rewrites), so it does not inflate the R2 overflow path or restore latency. · **imports:** NONE — consumes zero WASM import slots. The entire fix is JS inside the QuickJS heap (engine fs facade) plus Rust/SQL on the DO host side; no new WASI/host import is added, so the hard 12-import ceiling is unaffected. No new host.__fs op is required for the vfs provider (rename/symlink are in-heap); the host-backed provider's existing host.__fs effect already carries op+path and can carry op:'symlink'/link_target without a new import.
- **effort:** M · **risk:** low · **feasibility(self):** ship-now
- **VERIFY [revise→feasible]:** No new CF primitive is invoked — correct, this is a pure fix over already-bound DO SQLite (this.ctx.storage.sql via KernelStore) + R2. The exec_ignore idempotent ALTER TABLE migration pattern is real and live (lib.rs:387-388 already added etag/origin to fs_files the exact same way), so adding ftype/link_target columns is mechanically sound and the D1-rejection reasoning is right (per-session tree is single-DO-local; a D1 hop would wreck the hot path). BUT the proposal's central durable-rename claim does not match the actual code. There is NO durable rename op path. The durable fs side (flush_staged_fs, lib.rs:1177-1232) knows ONLY two staged ops — Write(bytes) and Delete — and these are staged ONLY under config.fs.provider=='r2'. Under the DEFAULT 'vfs' provider host.__fs is set to NULL (lib.rs:2627) and NOTHING is staged: durability is 100% the heap blit of V.files/V.dirs in linear memory. So the proposed 'durable directory rename = UPDATE path=replace(path,old,new) over the prefix, R2 keys untouched via r2_key indirection' SQL does not exist and is not reachable: a renameSync only mutates in-heap V maps, which the r2 provider never flushes (no rename in the staged-op enum). Wiring it would require a NEW FsStageOp::Rename plumbed through a NEW host.__fs op:'rename' — directly contradicting the proposal's 'No new host.__fs op is required'.
  - blockers: (1) DURABLE RENAME IS VAPOR for the r2 provider: flush_staged_fs has only Write/Delete (lib.rs:1207-1224); the engine's promise-mode rename (lib.rs:2533) is a single-file read+write+unlink, no directory variant, and sync renameSync touches only the in-heap V maps the r2 provider never flushes. The 'UPDATE path column' rename needs a new staged FsStageOp::Rename + a new host.__fs op:'rename' — so imports=NONE and 'no new host op' are FALSE for the r2 path. (2) RECONCILE DELETES SYMLINKS: reconcile_fs_files (lib.rs:2107-2115) deletes every committed fs_files row whose path is not a live R2 object. Symlink rows carry NO R2 body (proposal says so), so after any VfsGateway/hash-worker invoke that triggers reconcile, ALL symlink rows are destroyed — silent data loss in exactly the unified-fs scenario this targets. Fix needs reconcile to skip ftype='symlink' rows. (3) PARTIAL: the IN-VM layer (A1 prefix-move rename, A2 V.links map) is genuinely correct AND fully sufficient under the DEFAULT vfs provider (durability via heap blit, no schema change needed there) — that is the load-bearing, ship-now portion. The durable-manifest layer (B) is where the design is under-specified and breaks. (4) lstatSync currently aliases statSync (lib.rs:2522) and Dirent.isSymbolicLink is hardwired false (lib.rs:2387/2395) — both must change for link visibility; the proposal covers this for the in-VM path but readdir withFileTypes Dirent still reports links as files.
  - evidence: apps/kernel/engine/src/lib.rs:2393 (renameSync: V.dirs[a] rename only re-keys the dir bit, ORPHANS children — confirmed bug), apps/kernel/engine/src/lib.rs:2399-2400 (readlinkSync always throws EINVAL; symlinkSync fakes a file body — confirmed bugs the fix targets), apps/kernel/engine/src/lib.rs:2522 (lstatSync aliases statSync), 2387/2395 (isSymbolicLink hardwired false), apps/kernel/src/lib.rs:1207-1224 (flush_staged_fs: ONLY Write/Delete ops — no Rename), apps/kernel/src/lib.rs:2626-2628 (default vfs provider sets fs_handler NULL — nothing staged, durability is heap-only)

### FS large-file silent-loss + non-atomic rename

- **findings:** (1) per-file host-backed write >~24MB = SILENT LOSS — DO-side _serviceFs/staged write (lib.rs 4442-4470) accumulates unbounded chunks via buf.extend_from_slice into one Vec<u8> with NO total-size guard; flush_staged_fs (1208) PUTs it as a single bucket.put; an oversize body rides the WS_FRAME_MAX_BYTES=24MB machinery and is dropped with no typed reject to the cell. (2) vfs_write offset/append (1321-1349) does full GET->splice->PUT read-modify-write, doubling RAM, no guard. (3) rename non-atomic + RAM-doubling — engine line 2533 rename = hReadFile(a) -> hWriteFile(b) -> hUnlink(a): pulls the whole body through the 32KB-chunked host boundary into the VM heap, three round-trips, no atomicity; there is NO rename/copy op in the DO fs handler at all.
- **root cause:** pure-impl-gap · **fix:** Three pure-impl changes, no new CF primitive. (A) SIZE GUARD turning silent-loss into typed-reject: add MAX_FS_FILE_BYTES (recommend 20MB, mirroring the MAX_DUMP_BUFFER_BYTES 18-20MB envelope culture) checked in the DO-side _serviceFs write branch (lib.rs ~4442). On chunk>0 append, if the accumulated staged buffer length + new_bytes would exceed the cap, drop the staged Write for that path and return {error:"FsError: EFBIG file exceeds <N>MB fs write limit"} so the in-VM fs.promises.writeFile/appendFile REJECTS (engine hostThrow already turns {error} into a thrown FsError). Same EFBIG check in vfs_write (1351, on `full.len()` after splice) returning ok:false. This converts the >24MB silent WS-frame drop into a deterministic typed reject, socket alive. (B) ATOMIC SERVER-SIDE RENAME/COPY: add new "rename" and "copy" ops to the DO-side fs handler match (lib.rs 4318) that operate entirely on R2 keys + the fs_files SQLite rows — never pulling the body into the VM or even the DO buffer. rename = R2 server-side copy(srcKey->dstKey) then delete(srcKey) + UPDATE fs_files SET path=dst,r2_key=dstKey WHERE path=src (one SQL row move, staged in the same commit). Re-point the engine rename/copyFileSync (engine 2533/2394) for the host-backed provider to a single host.__fs({op:'rename'|'copy'}) call instead of read+write+unlink. R2 has no native server-side copy in the workers-rs binding, so "copy" is implemented DO-side as a streaming GET(srcKey).body piped to a PUT(dstKey) WITHOUT materializing the full Vec (use the ReadableStream body directly), keeping peak RAM bounded; the row move stays atomic-with-the-commit. The in-heap 'vfs' provider rename (engine 2393) is already an O(1) object-key move and is fine.
- **CF primitive:** NONE for the guard (pure size-check). For the atomic rename: R2 streaming GET->PUT (already-bound SNAPSHOTS R2 bucket, no new binding) — the menu's "R2 multipart/streaming + conditional writes" capability, used only via the existing env.bucket().get()/put() ReadableStream path. No new wrangler entry, no new import.
- **mechanism:** Write guard: engine chunks a body at 32KB over host.__fs; the DO write branch (4442) already appends chunks into one staged Vec<u8>. Insert a running-total check there: track accumulated bytes for the path's staged Write; if it crosses MAX_FS_FILE_BYTES, replace that path's staged op with a sentinel and return {error:"EFBIG"} on the offending chunk — the engine's hostThrow converts {error} into a thrown FsError inside fs.promises.writeFile, the cell rejects cleanly, the eval mutex releases, socket stays alive. Mirror in vfs_write after the splice (1351). Atomic rename: engine rename for host-backed provider becomes one host.__fs({op:'rename',from,to}) call; DO handler resolves src/dst canonical keys (fs/<doId>/<norm>), does R2 GET(src).body -> PUT(dst) streaming (or, if a future workers-rs server-copy lands, a single copy call), then bucket.delete(src) and a staged fs_files row move (DELETE src row + INSERT dst row, or UPDATE path) committed in the SAME flush_staged_fs transaction as the heap manifest — preserving the staged-commit coherence invariant (no dangling reference: dst body durable before its row, src row+body removed together). A crash mid-rename leaves either the old name (copy not yet committed) or both names briefly (copy done, delete pending) — never data loss, and the next checkpoint reconciles; idempotent re-apply is safe because keys are content-path-addressed.
- **determinism:** ZERO. The size guard is a pure byte-count comparison (no clock/RNG). The rename moves R2 keys + SQLite rows with no entropy. Both are byte-identical across seeded restore. Not opt-in — the guard is on-by-default (it only ever turns a previously-silent loss into a typed error; it cannot change a successful write's bytes). No new non-determinism enters the heap. · **snapshot:** Positive/neutral. The guard caps a single staged fs body at ~20MB BELOW the ~24MB incompressible-buffer WS-1006 cliff, so an oversized write can no longer balloon DO RAM into the dump-OOM zone — it fails closed before the checkpoint. Atomic rename removes the read-whole-body-into-heap step, so a rename no longer transiently doubles the VM/DO heap (was a hidden contributor to the snapshot high-water-mark). fs_files rows and the manifest already participate in the staged-commit; the row-move rides the existing transaction. No change to snapshot format or restore path. · **imports:** NONE. All changes are in the DO host (Rust lib.rs) + engine JS glue, above the WASI import boundary. R2 GET/PUT/delete are DO-side worker bindings, not WASM imports. The 12-import ceiling is untouched.
- **effort:** M · **risk:** low · **feasibility(self):** ship-now
- **VERIFY [revise→feasible]:** Mostly real, with one CF gotcha the proposal got right and one it under-specified. (1) workers-rs 0.8.3 R2Bucket (verified at /Users/umang/.cargo/registry/.../worker-0.8.3/src/r2/mod.rs) has NO server-side copy method — only head/get/put/delete/delete_multiple/list/multipart. The proposal CORRECTLY anticipates this and falls back to DO-side streaming GET->PUT. (2) The streaming copy is genuinely RAM-bounded: `Data` enum (mod.rs:494) includes `ReadableStream(web_sys::ReadableStream)` and `Object::body()`/`.stream()` yields a ByteStream, so `bucket.put(dst, obj.stream())` pipes without materializing a Vec. CRUCIAL CF reality the proposal didn't flag: R2.put of a ReadableStream THROWS `Provided readable stream must have a known length` unless the stream carries a content-length. An R2 GET body DOES carry known length (it came from R2 with a known size — the "image/pdf works" case), so streaming an R2-origin body into a PUT is the supported happy path. But if the future relies on a generic stream it would need FixedLengthStream(size) wrapping. A DO CAN call all of these (R2 bindings are DO-side worker bindings, above the 12-WASM-import boundary — confirmed, untouched). (3) R2 has no native rename and put/delete are eventually-consistent-but-strongly-read-after-write for a single key; the crash-window reconciliation story holds.
  - blockers: (1) STAGED-vs-IMMEDIATE inconsistency — the load-bearing bug in the proposal. The "Atomic rename" para says the row move is "committed in the SAME flush_staged_fs transaction," i.e. STAGED to checkpoint; but the "Mechanism" para describes the DO handler doing GET->PUT->delete->row-move at host-call time, i.e. IMMEDIATE/mid-cell. These are incompatible. Mid-cell immediate R2 mutation BREAKS the staged-commit coherence invariant this kernel depends on (lib.rs:4262-4267, flush_staged_fs:1177): if the cell rename()s then throws, an immediate rename is already durable in R2 while the heap rolls back — exactly the dangling-reference the model forbids. CORRECT design: add a `FsStageOp::Rename{from,to}` (or Copy) variant, stage it in `staged` (lib.rs:4453 area), and apply the R2 copy+delete+row-move inside flush_staged_fs (1205 loop) at checkpoint, after collapsing last-op-per-path. The proposal must commit to staged. (2) "atomic-with-the-commit" is OVERSTATED — even existing writes do R2 body PUT first (non-transactional, 1211) THEN the SQLite row; the R2 copy/delete in a rename is likewise NOT in the SQLite txn. The real invariant is only "body durable before row," giving the old-name / both-names-briefly / never-loss window the proposal describes — that part is correct, but call it body-first-ordering, not transactional atomicity. (3) Collapse interaction: flush_staged_fs collapses to BTreeMap last-op-per-path keyed on the path. A Rename touches TWO paths (from+to); it does not fit the single-path collapse map and must be handled as an ordered op that survives collapse, or a write-after-rename to the same dst in one cell will mis-order. (4) Size guard chunk-accounting: the guard at chunk>0 append (4454) must track a per-path running total; when it trips it must replace the staged Write with a sentinel/drop AND the engine's hWriteFile loop (engine 2429) will then throw on the next hostThrow and stop sending chunks — verified the loop breaks on {error}. But a sentinel left in `staged` must be skipped by flush_staged_fs (a dropped/sentinel op must NOT write a partial body). (5) SCOPE: silent-loss only afflicts the OPT-IN host-backed `config.fs.provider=="r2"` path; the DEFAULT `vfs` provider is in-heap and already bounded by the snapshot size-admission guard (engine renameSync/copyFileSync at 2393/2394 are O(1) heap moves, correctly left untouched). The 20MB cap should also be reconciled with the 24-28MB incompressible WS-1006 cliff — 20MB is a reasonable below-cliff value but the host-backed body lives in DO RAM, NOT WASM linear memory, so it does NOT hit the monotonic-buffer snapshot cliff directly (it transits the 32MB SCRATCH only on the read-back path). The guard's real value is bounding DO RAM and turning silent >scratch-ceiling truncation into a typed reject. (6) READ-PATH for copy via engine is fine because the proposal moves copy DO-side, avoiding the engine's whole-body single-call read (scratch ~32MB ceiling, kernel-glue.ts:590) — good, that's the right call.
  - evidence: apps/kernel/src/lib.rs:4442 (r2_fs_op write branch — NOT _serviceFs; function is r2_fs_op, not the name in the proposal), apps/kernel/src/lib.rs:4453-4468 (chunk>0 append into staged Vec — where size guard inserts), apps/kernel/src/lib.rs:4262-4267 + 1177-1232 (flush_staged_fs staged-commit coherence: body-to-R2 first, then SQLite row; collapse last-op-per-path), apps/kernel/src/lib.rs:1351 (vfs_write full.len() — mirror EFBIG site), apps/kernel/engine/src/lib.rs:2533-2534 (host-backed rename = read+write+unlink, copyFile = read+write — the inefficiency targeted)

### CONSOLE FORMAT

- **findings:** (1) console.log/error/warn/info/debug do NOT apply printf substitution for %s/%d/%i/%f/%j/%o/%O/%c/%% — they map each arg through __fmt (string passthrough else __preview) and space-join, so console.log('%s=%d', 'x', 5) prints literally "%s=%d x 5" instead of "x=5". (2) A complete Node-compatible util.format/formatWithOptions already exists in-VM (engine/src/lib.rs __util, lines 1554-1574) but is unwired from console. (3) console.table (line 1332) is a stub that just dumps __preview(rows,3) instead of rendering a tabular layout.
- **root cause:** unwired-existing-capability · **fix:** Route console through the already-implemented formatter, and add a real table renderer. In apps/kernel/engine/src/lib.rs: (A) inside the __util IIFE, before `return`, hoist the printf engine to a global: `globalThis.__format = format; globalThis.__formatWithOptions = formatWithOptions;` (format is the multi-arg printf already handling %s %d %i %f %j %o %O %c %%, with leftover args inspected+space-appended and non-string-first-arg fallback — exactly Node util.format semantics). (B) Rewrite the console object (lines 725-731) so each level builds its message via the formatter at CALL time with a lazy guard: `const __cfmt = (...a)=> (globalThis.__format ? globalThis.__format.apply(null, a) : a.map(__fmt).join(' '));` then `log:(...a)=>{ globalThis.__logs.push({level:'log', msg:__cfmt(...a)}); }` for all five levels. The lazy `globalThis.__format ?` guard is belt-and-suspenders: BOOTSTRAP is one eval so by the time any cell calls console.* the assignment at __util has already run; the fallback only matters if console fires during bootstrap itself. Keep __fmt as the documented fallback. (C) console.table (line 1332): replace the __preview passthrough with a real fixed-width ASCII grid builder — derive columns from union of row keys (array-of-objects) or index/value for primitives/arrays, compute per-column max width, emit header + separator + rows, push as a single console.log line. ~30 lines, pure JS. Also align console.dir/group already fine; leave console.assert as-is. No new Rust, no host call, no module changes — util module export of format is unchanged so require('util').format keeps working (now shares the global).
- **CF primitive:** NONE (pure in-VM fix)
- **mechanism:** All of console, __util, and __preview live in the single BOOTSTRAP string (apps/kernel/engine/src/lib.rs:197) eval'd once per fresh QuickJS runtime (WIRED-guarded at lib.rs:3195-3217) and re-eval'd verbatim on every cold restore. Functions resolve lazily at call time, so console (defined at L725) can invoke a formatter assigned later at L1574 via globalThis. The formatter is the existing `formatWithOptions` closure: it scans the first string arg for /%[sdifjoOc%]/g, consumes positional args (%s String/inspect-object, %d|%i parseInt, %f parseFloat, %j JSON.stringify with [Circular] guard, %o|%O inspect via __preview, %c->'', %%->'%'), then space-appends any extra args (inspected if non-string) — byte-for-byte the Node util.format contract. console.* pushes the formatted string into globalThis.__logs; Rust drains via __drainLogs (L732) per cell unchanged. table builds an ASCII grid string and pushes it as one log line. End to end: cell calls console.log('%s=%d',k,v) -> __cfmt -> globalThis.__format -> substituted string -> __logs -> __drainLogs -> existing capture/preview path -> SDK. Zero new code paths in Rust/glue.
- **determinism:** None — does NOT break seeded byte-identical restore, NOT opt-in. The formatter is pure-of-input (string scan + JSON.stringify + __preview, no clock/RNG/crypto/entropy). Identical inputs yield identical output bytes across restore. Default-on is safe; no determinism flag needed. (Output bytes for existing console.log calls that happened to contain a literal '%s' in a string will CHANGE — they now substitute rather than print literally — but that is the correct Node-parity behavior, not a determinism regression; replay of the same session with the same code is still byte-identical.) · **snapshot:** None. The change is entirely within BOOTSTRAP, which is re-eval'd verbatim on every fresh instance and cold restore (not snapshotted as data — it is re-installed code). __logs is drained per cell and not part of the persisted heap state of interest. globalThis.__format adds two function refs to the heap (negligible, identical on every restore since BOOTSTRAP re-runs). No manifest/column/size-admission change. Snapshot bytes unaffected beyond the trivial constant heap delta that is reconstructed deterministically. · **imports:** None — consumes zero WASM import slots. Pure JS inside the QuickJS heap; no new host call, no __hostCall round-trip, no WASI function. Stays at the existing import count well under the 12 ceiling.
- **effort:** XS · **risk:** low · **feasibility(self):** ship-now
- **VERIFY [confirmed→feasible]:** CF primitive is NONE (pure in-VM QuickJS-heap fix) and that claim is accurate — zero new WASM imports, zero host-call round-trips, no WASI fns, no fetch, no DO/R2/AE/Loader involvement. The 12-import ceiling, monotonic-memory cliff, single-realm, sequential-host-call boundary, and WS-hibernation are all untouched; this is pure JS inside the existing BOOTSTRAP string. The ONE platform reality the proposal mis-states as 'snapshot=None': BOOTSTRAP is Rust engine source, so ENGINE_HASH = SHA-256(engine.wasm) (engine-hash.ts) CHANGES on this edit. Per apps/kernel/src/lib.rs:2971, an engine_hash mismatch does NOT wedge — it triggers a one-time 'engine-migration-replay' (oplog journal replayed into a fresh instance, pure cells re-run, host effects fed from recorded oplog so no re-fire). So EXISTING live sessions survive but pay a heavier first-wake-after-deploy. That is the designed upgrade path, not a regression, but it is NOT 'None'. Snapshot bytes themselves are unaffected; the constant heap delta (2 fn refs on globalThis) is reconstructed deterministically on every BOOTSTRAP re-eval as claimed.
  - blockers: 1) LINE NUMBERS PARTIALLY WRONG — verify before editing: console object IS at lib.rs:725-731; console.table IS at L1332. BUT __util IIFE starts at L1551 (not 1574) and its `return` block is L1599-1606; L1574 is only where the local `format` fn is defined. Hoist the globals at the return (~L1599), and __util sits AFTER console (L725) AND after console.table (L1332) — the forward reference is real. 2) FORWARD-REFERENCE IS SAFE and idiomatic: the lazy `globalThis.__format ?` guard mirrors what the file already does — existing __fmt (L724) and util.inspect (L1552) already forward-ref globalThis.__preview defined far later at L2701; all resolve at cell-call time, long after the single BOOTSTRAP eval (L197-2907) completes once. Bootstrap-time console calls hit the documented __fmt fallback. 3) EFFORT IS NOT XS — call it S: editing Rust engine source forces a full Rust->WASM rebuild (package.json build:worker = build-engine + worker-build --release + engine-hash regen). CLAUDE.md states cargo/worker-build exceed the 180s workflow watchdog and MUST run in-thread; deploy via `alchemy deploy` NOT wrangler (DO-migration 10074). 4) Existing sessions take a one-time engine-migration-replay on next wake (see cf_api_reality). 5) console.table multi-line ASCII grid as one __logs {level,msg} entry with embedded \n preserves the drain contract (L732, apps/kernel/src/lib.rs:3411) — verify SDK/UI renders embedded newlines (low risk). Determinism claim VERIFIED: __preview (L2701) and formatWithOptions (L1554) are pure-of-input (string scan + JSON.stringify, no clock/RNG/crypto) — byte-identical across restore; default-on is safe.
  - evidence: apps/kernel/engine/src/lib.rs:725-731 (console object, a.map(__fmt).join(' ') — the call sites to rewrite), apps/kernel/engine/src/lib.rs:724 (__fmt fallback already forward-refs globalThis.__preview), apps/kernel/engine/src/lib.rs:1551-1607 (__util IIFE; formatWithOptions L1554-1573 with /%[sdifjoOc%]/g handling %s/%d/%i/%f/%j/%o/%O/%c/%% + trailing-arg append; format L1574; return L1599-1606), apps/kernel/engine/src/lib.rs:1329-1333 (console.dir/group/table/assert; table=__preview passthrough at L1332 is the target), apps/kernel/engine/src/lib.rs:2701 (globalThis.__preview — pure string-inspection, no entropy)

### BUFFER ENCODINGS

- **findings:** (1) buf.toString(enc,start,end) drops start/end — always decodes the whole buffer; (2) toString missing base64url/ucs2/utf16le → falls through to TextDecoder UTF-8 = garbled; (3) Buffer.from + buf.write missing base64url (no -/_ remap, no pad-tolerance) and ucs2/utf16le on write → garbled; (4) Buffer.from base64 uses strict atob → throws on Node-lenient input (whitespace/invalid chars/missing padding); (5) Buffer.isBuffer returns true for ANY Uint8Array (not just Buffers) and `x instanceof Buffer` is broken (__B is a plain function, real Uint8Array instances are not instanceof it).
- **root cause:** pure-impl-gap · **fix:** Pure-JS patch to the existing Buffer block in apps/kernel/engine/src/lib.rs (the `if (typeof globalThis.Buffer === 'undefined')` region, lines ~1354-1511). Five edits, all in-VM, no host calls, no new globals:
1. toString slice + encodings (line 1357): read `(enc, start, end)`; compute `var b = (start!==undefined||end!==undefined) ? this.subarray(start|0, end===undefined?this.length:end|0) : this;` then decode `b`. Add e==='base64url' (btoa then .replace(/\+/g,'-').replace(/\//g,'_').replace(/=+$/,'')), and e==='ucs2'||'utf16le' (DataView getUint16 LE loop → String.fromCharCode, honoring byteOffset via __dv(b)).
2. Add a shared lenient base64 decoder `__b64dec(s)` = strip whitespace, map base64url -/_ back to +/, re-pad to length%4, then atob; wrap in try and fall back to a manual table decode that skips invalid chars. Route Buffer.from(...,'base64'|'base64url') (line 1465) and write/StringDecoder through it. This kills the strict-atob throw.
3. write (line 1442): add e==='base64url' (via __b64dec) and e==='ucs2'||'utf16le' (DataView setUint16 LE) branches; default stays utf8.
4. Buffer.from base64url already remaps but uses strict atob — switch to __b64dec.
5. isBuffer/instanceof: make __B carry a proper prototype so `Buffer.from(x) instanceof Buffer` is true (set __B.prototype = Uint8Array.prototype OR brand instances), and gate __B.isBuffer on a non-enumerable brand symbol (e.g. set Object.defineProperty(out,Symbol.for('nodejs.buffer'),...) on Buffer.from/alloc outputs; isBuffer checks the brand) so a plain Uint8Array is NOT mis-reported as a Buffer. Simplest brand-only path avoids reshaping the prototype chain and keeps Buffer === Uint8Array structurally.
- **CF primitive:** NONE (pure-impl fix, in-VM JS over already-resident QuickJS builtins atob/btoa/TextDecoder/DataView)
- **mechanism:** All logic lives in the create-time-eval'd Buffer shim block that patches Uint8Array.prototype and defines globalThis.Buffer. No host round-trip (__hostCall untouched), no WASI fn, no fetch. Encodings are computed purely from input bytes/strings using atob/btoa (QuickJS C builtins), TextDecoder/Encoder, and DataView — all already in the realm. base64url is a pure char-remap + tolerant re-pad before atob; ucs2/utf16le is a DataView LE u16 loop. The lenient decoder is a try/atob-with-cleanup then a manual skip-invalid fallback. isBuffer becomes a brand check (a Symbol property set on Buffer.from/alloc outputs); instanceof is satisfied by branding rather than reshaping the Uint8Array prototype chain (lowest-risk). The whole block re-evaluates identically on cold restore (engine re-instantiates and re-evals the same engine source BEFORE the heap blit), so behavior is stable across hibernate/resume.
- **determinism:** NONE — does not break seeded byte-identical restore, not opt-in. Encodings are pure functions of their input; they touch zero entropy sources. The seeded path (__rand/__seededRandom/seeded crypto.getRandomValues/randomUUID/WASI random_get) is entirely separate code and is not modified. No clock, no RNG, no host call introduced. A seeded session produces the same bytes before and after this fix's only observable change being correct (vs garbled) output, which is deterministic either way. · **snapshot:** Neutral. No new persistent state, no new globals beyond the existing Buffer shim. The patched prototype methods and __B are closures created by evaluating the same engine source string at create and at restore; the heap-snapshot blit restores the same function objects. The optional brand Symbol (Symbol.for('nodejs.buffer')) is a registered symbol, stable across realms/restore. Snapshot size unchanged (a few hundred bytes of extra source in the engine bundle, not in the per-session heap delta). No effect on the ~24-28MB monotonic cliff. · **imports:** NONE — consumes zero WASM import slots. Uses only in-realm JS builtins (atob/btoa/TextDecoder/TextEncoder/DataView/Symbol) already present; adds no new WASI/host import. Stays well under the 12-import ceiling (no change).
- **effort:** S · **risk:** low · **feasibility(self):** ship-now
- **VERIFY [confirmed→ship-now]:** No CF API is invoked — this is correctly scoped as a pure in-VM JS patch. The "primitives" it relies on (atob/btoa/TextDecoder/TextEncoder/DataView/Symbol) are QuickJS-ng / rquickjs C builtins already resident in the single realm; none of them are in lib.rs (confirmed: atob/btoa have no Rust definition, they're engine builtins). The proposal's read of CF/QuickJS behavior is accurate on the load-bearing point: QuickJS-ng atob throws on truly invalid base64 (forgiving-base64 strips whitespace but rejects non-alphabet chars), whereas Node Buffer.from(s,'base64') silently skips invalid chars — so the strict-atob throw at lines 1449/1465 is a real divergence and the __b64dec(re-pad + try/atob + skip-invalid fallback) wrapper is the correct fix. No DO round-trip, no fetch, no WASI fn, no host call (__hostCall untouched).
  - blockers: No hard blockers; the fix is feasible exactly as described. Caveats to land carefully: (1) BEHAVIORAL TIGHTENING — current isBuffer (line 1493) returns true for ANY Uint8Array; gating it on a brand Symbol makes plain Uint8Arrays return false. That is correct Node semantics but some bundled libs call Buffer.isBuffer(someRawUint8Array) expecting true — a minor compat regression to accept knowingly. (2) instanceof loosening — setting __B.prototype=Uint8Array.prototype makes EVERY Uint8Array `instanceof Buffer`, which is loose but consistent with the existing Buffer===Uint8Array structural-identity choice; the proposal's preferred brand-only path (don't reshape prototype, just brand outputs) is the lower-risk option and should be the one shipped. (3) write() must clamp the decoded src to the (length, this.length-offset) window for ucs2/base64url just as utf8 does (line 1452-1454) — easy to miss on the new branches. (4) toJSON/round-trip outputs (line 1473) and concat outputs are NOT branded under the brand-only approach, so isBuffer on a JSON-revived Buffer would be false — decide whether those paths also need the brand. (5) base64url toString must use btoa-then-char-remap-then-strip-padding; verify atob/btoa availability is unconditional in this build (it is — used elsewhere at lines 879-880, 2360, 2417).
  - evidence: apps/kernel/engine/src/lib.rs:1357 toString(enc) reads only enc, ignores start/end slice, apps/kernel/engine/src/lib.rs:1362-1364 base64 only; base64url/ucs2/utf16le fall through to TextDecoder = garbled, apps/kernel/engine/src/lib.rs:1442-1456 write() handles hex/base64/latin1/utf8 only — no base64url/ucs2/utf16le, apps/kernel/engine/src/lib.rs:1449,1465 strict atob() — throws on lenient/invalid base64 (no skip-invalid), apps/kernel/engine/src/lib.rs:1493 isBuffer = x instanceof Uint8Array → plain Uint8Array mis-reported as Buffer; __B has no .prototype so Buffer.from(x) instanceof Buffer is false (asymmetry)

### FETCH/HTTP FIDELITY

- **findings:** (1) FormData body serialized as application/x-www-form-urlencoded with a fabricated '[blob]' placeholder, never multipart/form-data with a boundary (engine lib.rs L970-972, L981). (2) VM Request.redirect mode (L1016) is parsed but never forwarded into the host fetch init (fetch() L1071-1140 builds sendInit without redirect) -> 'manual'/'error' callers silently get follow semantics. (3) getSetCookie() missing on the Headers shim (engine L516-538) AND set-cookie is already collapsed at the host boundary by r.headers.forEach (kernel-glue _doFetch L2115 / _doFetchStream L2171) into one comma-joined string -> multiple Set-Cookie unrecoverable. (4) http/https IncomingMessage is a fake Readable that emits the whole body in one 'data' event (engine L2043-2076), not a true incremental stream. (5) buffered _doFetch truncates response bodies at FETCH_MAX_BODY_BYTES=2MB (kernel-glue L2100-2106).
- **root cause:** pure-impl-gap · **fix:** Five surgical fixes, all in the two shim files (engine/src/lib.rs JS shim + kernel-glue.ts host), no new CF binding. (a) MULTIPART FormData: in bodyToBytes (L970), when body is a FormData, emit a real multipart/form-data payload with a DETERMINISTIC boundary (derived from a per-cell counter / content hash, NOT random — see determinism note), inlining Blob/File bytes with Content-Disposition + filename + per-part Content-Type; have inferContentType return 'multipart/form-data; boundary=<b>'. Keep urlencoded only as an explicit fallback if a caller opts in. (b) REDIRECT MODE: forward req.redirect into sendInit.redirect in fetch() (L1080); host-side, in ssrfSafeFetch/_fetchPrep honor 'manual' (return the raw 3xx, no hop-follow) and 'error' (reject on any 3xx) — 'follow' keeps today's SSRF-rechecked manual-hop loop. (c) getSetCookie + set-cookie survival: PRESERVE set-cookie as an ARRAY across the host boundary — in _doFetch/_doFetchStream replace the lossy forEach with `const sc = typeof r.headers.getSetCookie==='function' ? r.headers.getSetCookie() : []` (workerd Headers.getSetCookie is available host-side), ship it as a separate `setCookie:string[]` field in the result; in the engine Headers shim store set-cookie internally as an array and add HP.getSetCookie() returning it, and have mkResp/Response seed it from the new field. (d) IncomingMessage real stream: drive its 'data' events from the existing host streamRead chunk loop (the same primitive Response.body already uses at L1050-1056) instead of one r.arrayBuffer() blob — emit per chunk, honoring pause()/resume() backpressure. (e) >2MB body: the WHATWG fetch() already routes through host.fetchStream which chunks unbounded bodies, so the 2MB truncation only bites the LEGACY buffered _doFetch host-call; redirect the legacy `name==='fetch'` host call to _doFetchStream-backed drain (or simply raise/remove the cap there and let .text()/.json() reassemble from streamed chunks). No CF primitive needed for any of the five.
- **CF primitive:** NONE (pure shim fixes). Note: workerd's host-side Headers.getSetCookie() (real Web platform API, already in the runtime) is the wired-existing capability used to recover set-cookie at the boundary — no new binding, no wrangler change.
- **mechanism:** All five are shim-layer edits across the JSON host<->VM boundary. (a)+(b) live entirely in the engine JS shim (bodyToBytes/inferContentType/fetch()), pure data transforms — the host fetch already accepts a body+content-type, it just receives a correctly-framed multipart buffer and a redirect mode instead of dropped fields. (c) is a two-sided edit: host reads the multi-valued set-cookie via the real workerd Headers.getSetCookie() (which exists precisely because forEach collapses cookies), ships it as setCookie:string[] in the existing result JSON, and the engine reconstitutes it into a per-Headers array exposed via getSetCookie(). (d) reuses the ALREADY-BUILT host streamRead park/resume chunk pump (the same one Response.body uses) to feed IncomingMessage 'data' events incrementally instead of buffering — zero new host effect, just a different consumer of an existing stream handle. (e) is a routing change: point the legacy buffered host-call at the existing streaming drain so the 2MB cap stops truncating. End to end: no new host import, no new RPC, no park/resume primitive added — every fix is either a pure in-VM transform or a re-wire of the existing fetchStream/streamRead machinery.
- **determinism:** Mostly determinism-neutral. THE ONE HAZARD: a multipart boundary is normally random; a random boundary would break byte-identical seeded restore and per-cell idempotency-key replay. FIX: derive the boundary DETERMINISTICALLY from the existing per-(doId,cell,fetch-seq) fence (same material that mints the Idempotency-Key) or a content hash — then byte-identical replay is preserved and seeded sessions stay reproducible. Redirect-mode forwarding, set-cookie array, real IncomingMessage streaming, and the >2MB un-cap add ZERO entropy (fetch responses already add no entropy per the existing contract). No opt-in flag needed if the boundary is deterministic; keep urlencoded-FormData reachable only as explicit legacy behavior, default to correct multipart. · **snapshot:** Neutral. The Headers set-cookie array is a few extra strings in the heap (already snapshot-persisted like the rest of Headers._m); negligible bytes. No change to the snapshot/restore blit mechanism. The >2MB un-cap does NOT enlarge the steady-state heap because the WHATWG path already streams (chunks are drained transiently, not resident) — the existing 32MB SCRATCH high-water already accounts for large fetch bodies; routing the legacy call through streaming actually REDUCES peak residency vs holding a full base64 blob. No new monotonic-memory pressure, stays well below the ~24-28MB incompressible cliff. · **imports:** Zero. No new WASM import — all fixes are JS-shim transforms or re-wires of the existing host.fetchStream/host.streamRead effects, which already occupy their slots. The 12-import ceiling is untouched.
- **effort:** M · **risk:** low · **feasibility(self):** feasible
- **VERIFY [revise→feasible]:** All five fixes are pure shim edits over the JSON host<->VM boundary and over workerd's outbound fetch() — no new CF binding, no new WASM import, no new host RPC. (c) workerd's host-side Headers.getSetCookie() IS a real shipped Web Platform API (the DO-side r.headers is a workerd Response), so recovering multi-valued set-cookie at the boundary works as claimed — r.headers.forEach genuinely collapses duplicate set-cookie into one comma-joined string (the lossy line is kernel-glue.ts L2113), getSetCookie() is the canonical fix. (b) workerd fetch() supports redirect 'manual'|'error'|'follow'; ssrfSafeFetch ALREADY forces redirect:'manual' (kernel-glue.ts L827) and drives hops by hand, so branching on req.redirect is trivial. (d)+(e) reuse the ALREADY-BUILT streamRead park/resume pump (_doStreamRead) that Response.body/drainBody already consume — no new effect. CAVEAT on (e): '2MB truncation only bites the LEGACY buffered _doFetch' is HALF-RIGHT. Verified FETCH_MAX_BODY_BYTES defaults to 2MB (L578), truncates in _doFetch (L2103) and the socket path (L1928). Global fetch() routes through host.fetchStream->_doFetchStream (engine L1130) with NO 2MB cap on the chunked path (inline only when content-length known AND <=256KB, L2186) — so global fetch() is already >2MB-safe. BUT legacy host.fetch (name==='fetch') is STILL used directly by node:http/https client (engine L2065 r.arrayBuffer()), DNS-over-HTTPS (L2119), and use()/CDN loader (L2578/2612) — those are still truncated. Fix must re-route those callers or raise the cap there, not merely 'redirect the legacy host call'.
  - blockers: 1) DETERMINISM MECHANISM MISDESCRIBED. The proposal derives the multipart boundary from 'the per-(doId,cell,fetch-seq) fence (same material that mints the Idempotency-Key)'. That material (_fenceDoId/_curFetchSeq) lives HOST-SIDE (kernel-glue.ts L2076), NOT visible in-VM where bodyToBytes() runs (engine lib.rs L962). The boundary must be minted IN-VM. The correct in-VM deterministic source already exists: seeded crypto.getRandomValues (engine L367, routes through seeded WASI random_get) or a per-cell in-VM counter. Determinism is ACHIEVABLE but via in-VM seeded entropy, not the named host fence. 2) (d) IncomingMessage is the WEAKEST item: ClientRequest.end() calls fetch() then r.arrayBuffer() (engine L2065). To drive 'data' events from streamRead it must consume resp.body (the ReadableStream that already pulls streamRead), not arrayBuffer(). pause()/resume() are NO-OPS (L2046-2047) and the asyncIterator (L2050) yields the whole _body in one shot — honoring real backpressure means rewriting all three; this item alone is ~M, not 'emit per chunk'. 3) ssrfSafeFetch drops body+forces GET after the first non-GET hop (L848), so multipart POSTs that 307/308-redirect lose their body — pre-existing; redirect-mode 'manual'/'error' exposes but doesn't worsen it. 4) Real multipart must preserve Blob/File .type and File .name in per-part framing — verify File carries a name (plain Blob does not). 5) Snapshot/import/monotonic-memory claims CHECK OUT: zero new imports (12-ceiling untouched), set-cookie array is a few heap strings, routing legacy >2MB through streaming REDUCES peak residency vs a base64 blob, staying clear of the ~24-28MB incompressible cliff. Effort is honest-to-slightly-light at M (IncomingMessage backpressure rewrite is the cost driver); 'feasible' stands.
  - evidence: apps/kernel/src/kernel-glue.ts:578 — FETCH_MAX_BODY_BYTES defaults to 2*1024*1024 (the 2MB legacy cap), apps/kernel/src/kernel-glue.ts:2113 — r.headers.forEach((v,k)=>headers[k]=v) is the lossy boundary that getSetCookie() must replace, apps/kernel/src/kernel-glue.ts:827 — ssrfSafeFetch already forces redirect:'manual' and drives hops manually (so req.redirect branch is trivial), apps/kernel/src/kernel-glue.ts:2076 — _curFetchSeq/_fenceDoId fence material is HOST-SIDE only, NOT visible in-VM, apps/kernel/engine/src/lib.rs:962-975 — bodyToBytes flattens FormData to urlencoded in-VM (no boundary); runs where host fence is invisible

### FETCH EGRESS CAP SILENT

- **findings:** The single finding: per-cell host-call guard (default 64, config.maxHostCallsPerCell) silently caps host.fetch — eval "continues" because the drain loop exits while status===STATUS_HOST_CALL, the parked fetch promise is never settled, and _readResult() returns the previous cell's stale RESULT buffer. Same silent-cap class also covers maxStreamOps/maxWsOps/maxFsOps loop ceilings (all share the identical bail pattern at kernel-glue.ts:1667).
- **root cause:** pure-impl-gap · **fix:** Pure host-side fix in the eval drain loop (apps/kernel/src/kernel-glue.ts ~L1667-1689). Change the loop from "exit silently when guard exceeds maxHostCalls" to "exit the SERVICE branch but settle the parked promise with a typed reject". Concretely: keep the `while (status === STATUS_HOST_CALL)` outer condition but move the budget checks INSIDE: when a host call would push guard past maxHostCalls (or streamOps/wsOps/fsOps past their caps), instead of calling _serviceHostCall, synthesize `res = {ok:false, error:"HostCallLimitError: cell exceeded maxHostCallsPerCell (64); chain across cells, raise config.maxHostCallsPerCell, or use host.fetchStream for fan-out"}` and feed it via eval_resume. The engine's resume path (lib.rs:3568 __settleHost) turns ok:false into `rej(new Error(error))` — a catchable typed rejection at the cell's `await host.fetch(...)`. The cell then runs its own catch/finally; if it ignores the error and parks again, the next park gets the same typed reject (idempotent), and the loop still terminates because guard keeps climbing and we hard-break after a small slack (e.g. maxHostCalls + N) to bound the reject-storm. NO new CF binding, NO determinism change by default, NO WASM import. Secondary (optional, separate effort): document/raise the default for fan-out and point users at the already-wired host.fetchStream (true streaming, own maxStreamOps budget of 4096) for high-fan-out cases — that path already exists (kernel-glue.ts _doFetchStream) and is not subject to the 64 guard.
- **CF primitive:** NONE (pure host-side fix). Optional fan-out adjunct: Cloudflare Queues (already-available menu primitive) for genuine async fan-out beyond a single cell, but NOT needed to close the finding — the silent-drop is fixed in-process.
- **mechanism:** End-to-end: (1) VM cell does `await host.fetch(...)` for the 65th time; the host Proxy (BOOTSTRAP) calls __hostCall, parking HOSTCALL_REQ and returning a promise whose resolve/reject are stashed in globalThis.__settleHost; engine returns STATUS_HOST_CALL. (2) Host drain loop (kernel-glue.ts) sees guard would exceed maxHostCalls. (3) INSTEAD of bailing silently, host writes a synthesized `{ok:false,error:"HostCallLimitError: ..."}` to scratch and calls ex.eval_resume(). (4) Engine run(resume=true) at lib.rs:3565 reads the payload and evals `__settleHost(false, error)` -> `rej(new Error("HostCallLimitError: ..."))`. (5) The cell's await rejects; user catch handles it or it propagates to __cellError -> finalize_cell produces a real `{ok:false,valueType:"error"}` frame. (6) _readResult() now returns THAT frame, not a stale buffer. The drop is now a typed, catchable, observable error. Loop bounding: after emitting the limit-reject we still allow the loop to continue servicing the settlement-driven microtasks, but any FURTHER host-call park is answered with the same synthesized reject; a hard slack break (maxHostCalls + small constant) guarantees termination so a catch-and-retry-loop cell cannot spin forever (it trips the cell instruction budget anyway since rejections cost bytecode).
- **determinism:** None by default — preserves seeded byte-identical restore. The synthesized HostCallLimitError is a deterministic function of (cell source, config.maxHostCallsPerCell), adds zero entropy, and fires at the exact same host-call ordinal on every replay/restore. It is recorded in the E6 oplog as a normal host result (this._cellHostResults.push(res)), so engine-migration replay reproduces it identically. No clock/RNG/crypto touched. Not opt-in — strictly better than the current silent drop. (The earlier silent bail was ALREADY non-deterministic-adjacent in effect because the stale RESULT depended on prior cell state; the fix makes the boundary crisp and reproducible.) · **snapshot:** None. The fix only changes how a parked promise settles within a cell turn; it does not alter heap layout, the snapshot manifest, or the dump/restore path. The rejection settles the pending promise so the job queue drains clean BEFORE finalize_cell — actually marginally IMPROVES snapshot hygiene vs the silent bail, which left an unresolved __settleHost closure and a parked HOSTCALL_REQ in the heap that could persist into the image. No new manifest columns, no size change, no used-heap delta. · **imports:** Zero. No new WASM import — the fix lives entirely in the JS glue drain loop and reuses the existing eval_resume import (already one of the 12). host.fetchStream (the fan-out adjunct) is also already wired and consumes no additional import slot.
- **effort:** S · **risk:** low · **feasibility(self):** ship-now
- **VERIFY [confirmed→ship-now]:** N/A — this is a pure in-process JS-glue fix; no Cloudflare API is invoked. The proposal correctly claims "CF primitive: NONE." I verified there is no DO/R2/AE/Worker-Loader/Queues call on the fixed path; the optional Queues adjunct is correctly flagged as NOT needed to close the finding and is out of scope. No async-vs-sync, no fetch round-trip, no consistency surface involved. The only "API" used is the engine's existing eval_resume WASM export — already one of the 12 imports (confirmed kernel-glue.ts:1689 + engine lib.rs:3458), so zero new import slots, no 12-import-ceiling pressure.
  - blockers: No fatal blocker — the core mechanism is verified correct and the fix is ship-now. But three real gotchas the design must address: (1) MISLEADING ERROR-MESSAGE GUIDANCE: the synthesized message points users at host.fetchStream "for fan-out," but fetchStream is the WRONG tool for high request-fan-out. I confirmed (kernel-glue.ts:1672) that only streamRead/streamWrite/socket.read/socket.write increment streamOps (4096 budget); the fetchStream OPEN call itself (name "fetchStream", serviced at :1772) is NOT in isStreamOp, so it counts against `guard` (the 64 cap). 64 separate fetchStream opens still trip the same fence. fetchStream gives ONE connection many chunk reads (big bodies), not many requests. The remediation message should instead say "chain across cells or raise config.maxHostCallsPerCell" and drop the fetchStream fan-out advice (or only mention it for large single bodies). (2) FOUR INDEPENDENT BUDGETS, NOT ONE: the loop guard is `guard<=maxHostCalls && streamOps<=maxStreamOps && wsOps<=maxWsOps && fsOps<=maxFsOps` (:1667). The fix must synthesize a budget-specific typed reject for WHICHEVER cap a given park would exceed (StreamOpLimit/WsOpLimit/FsOpLimit), and must check the cap BEFORE classifying+incrementing — the proposal's "move checks inside" must replicate the exact isStreamOp/isWsOp/isFsOp classification (req.name read via _readHostCall) so it rejects the right park against the right counter. (3) HARD SLACK-BREAK MANDATORY + must still call _readResult after: the bounded reject-storm break must fall through to a genuine finalize. If a catch-and-repark cell never lets the cell promise settle, the loop hard-breaks on slack but the VM is STILL parked → _readResult returns stale again. The break path must force one final finalize (e.g. resume with the typed error then, if still parked after slack, emit a synthesized {ok:false,valueType:"error"} frame from the glue itself rather than reading the engine RESULT). The proposal's claim that "it trips the cell instruction budget anyway" is only sometimes true: host-call round-trips cost ~0 budget (confirmed FIX #14 comment at lib.rs:3462 — budget is NOT refilled but rejection handling is cheap bytecode), so a tight catch/repark could chew slack faster than budget — the slack constant is the real fence and must be small + the fallthrough frame must be glue-synthesized.
  - evidence: apps/kernel/src/kernel-glue.ts:1667-1689 — the drain loop: while(status===STATUS_HOST_CALL && guard<=maxHostCalls && streamOps<=maxStreamOps && wsOps<=maxWsOps && fsOps<=maxFsOps); on exceed it falls out and `return this._readResult()` with the VM still parked (the silent-drop bug, confirmed), apps/kernel/src/kernel-glue.ts:2810-2815 — _readResult reads the engine RESULT buffer (result_ptr/result_len); with no final eval_resume->finalize_cell it returns the prior/stale buffer contents (confirms 'stale RESULT returned'), apps/kernel/engine/src/lib.rs:755 — host Proxy: globalThis.__settleHost = (ok,val)=>{ ok?res(val):rej(new Error(val)); } — feeding ok:false yields a catchable typed rejection at the cell's await (mechanism confirmed), apps/kernel/engine/src/lib.rs:3563-3570 — run(resume): reads HOSTCALL_RES payload, clears HOSTCALL_REQ, evals f(p.ok, p.ok?p.value:(p.error||'host error')) — exactly the path the fix relies on; eval_resume({ok:false,error}) -> rej(new Error(error)), apps/kernel/engine/src/lib.rs:3458-3473 — eval_resume reads scratch payload into HOSTCALL_RES then run(true); FIX #14: budget NOT refilled, so a catch/repark loop's termination relies on the glue slack-break, not solely on budget

### TLS/SOCKET FIDELITY

- **findings:** net.Socket.setKeepAlive/setNoDelay no-op; net.Socket.setTimeout inert (no idle fire); tls getPeerCertificate()={}; getProtocol()='TLSv1.3' nominal; getCipher()=nulls; alpnProtocol=false; authorized always true / no rejectUnauthorized / checkServerIdentity never fails; no mTLS client cert/key; no settable SNI servername.
- **root cause:** hard-platform-limit · **fix:** SPLIT the finding by item, do not paper over with one stub class. (1) SNI: document as ALREADY-CORRECT — cloudflare:sockets derives SNI automatically from the connect-address hostname; host-sockets.mjs already forwards the resolved hostname, so the right server cert is requested. No code change; just stop reporting it as a gap and set sock.servername (already done) to the connect host so the VM-visible value matches what's on the wire. (2) net.Socket.setTimeout: PROMOTE from inert stub to a real idle-timeout in the per-socket read loop (pure-impl, host-independent) — track lastActivity host-side per handle; when socket.read parks past `timeout` ms with no bytes, the shim emits 'timeout' (Node semantics: fires the event, does NOT auto-destroy). This is honest behavior the substrate CAN deliver because the read loop already polls. (3) keepalive/setNoDelay: DOCUMENT as hard platform limit — cloudflare:sockets SocketOptions exposes only {secureTransport, allowHalfOpen}; there is no TCP-level KEEPALIVE/NODELAY knob. Keep the no-op returning `this` (Node tolerates a runtime that ignores these hints). (4) getPeerCertificate/getProtocol/getCipher/alpnProtocol/authorized/mTLS: DOCUMENT as hard platform limit — TLS is terminated by the workerd runtime, neither the VM nor the host JS ever receives the cert chain, negotiated protocol, cipher, ALPN result, nor can it present a client cert (no mTLS). Change the LIE to an honest signal: getProtocol() should return null (unknown) not a fabricated 'TLSv1.3'; getPeerCertificate() returns {} with a documented caveat; alpnProtocol stays false; authorized stays true ONLY because the runtime already validated the chain server-side (document this is runtime-validated, not VM-validated, and rejectUnauthorized:false is unsupported because you cannot opt OUT of validation). (5) ESCAPE HATCH for callers that genuinely need real Node TLS (mTLS, client certs, cert pinning, ALPN h2): route through the already-bound Tier-2 Container (engram-sandbox) which is a full Linux/Node process with real OpenSSL — expose it as an explicit, capability-gated alternative in the net/tls module docstring + the env-surface excluded-table reason string, not as an in-VM capability.
- **CF primitive:** NONE for the in-VM fix (pure host-side impl + doc honesty). Containers/@cloudflare/sandbox (ALREADY BOUND as engram-sandbox, Tier-2) is the documented escape hatch for real Node TLS / mTLS / ALPN — additive, no new binding.
- **mechanism:** All TLS/socket I/O already flows VM net.js/tls.js shim -> host-call socket.open/read/write/startTls/close -> kernel-glue _socketOpen (SSRF + config.fetch allowlist) -> host-sockets.mjs makeSocketHost -> cloudflare:sockets connect(). The live Socket lives only in DO memory (handleId token in the heap), severs on hibernation as ECONNRESET — unchanged. FIX WIRING: (a) setTimeout idle: host-sockets.mjs stamps st.lastRead = Date.now() on each reader.read() that yields bytes; the VM-side per-socket read loop (net.js `turn()`) already recurses on every poll — add a VM-side elapsed check against this.timeout using the immediate-microtask clock is NOT enough (no wall clock in VM), so the idle timer must be HOST-side: socket.read returns an extra {idleMs} field; when idleMs >= the timeout the shim passed at connect (carried in openOpts.idleTimeoutMs), host returns {timedOut:true} and the shim emits 'timeout'. Zero new host call, piggybacks the existing read envelope. (b) getProtocol/getPeerCertificate honesty: pure VM-side edit in tls.js — return null/{}. (c) SNI: already on the wire (connect hostname == SNI); only the docstring/excluded-reason strings in engine/src/lib.rs change. (d) Container escape: documentation pointer to existing host.sandbox.* effect; no kernel change.
- **determinism:** SNI/getProtocol/getPeerCertificate honesty edits: ZERO entropy, byte-identical restore unaffected (pure constant returns / wire-hostname already deterministic). setTimeout idle-fire: introduces a WALL-CLOCK-dependent event ('timeout' fires based on real elapsed ms), which IS non-deterministic — therefore it MUST be opt-in: only arms when the cell explicitly calls socket.setTimeout(ms>0) (default timeout=0 stays inert, preserving current deterministic behavior). Socket I/O is already a host-mediated effect that adds no entropy to seeded clock/RNG; the idle event is the one new non-determinism and is caller-gated, so default seeded byte-identical sessions are unchanged. Container escape is wholly outside the seeded VM (real OpenSSL, real clock) — explicitly a determinism opt-out, same posture as existing host.sandbox.*. · **snapshot:** None. The live Socket and its handle state are DO-memory only and already excluded from the WASM linear-memory image (sever-on-wake = ECONNRESET). The new per-handle lastRead/idleMs timestamps live in the same non-snapshotted host Map. No new heap state, no manifest column, image size unchanged. · **imports:** None. socket.* is a JS host-call effect routed over the existing __hostCall WS bridge (kernel-glue), NOT a WASM import — it does not touch the 12-import ceiling. The Container escape is a DO-side fetch, also zero WASM imports.
- **effort:** S · **risk:** low · **feasibility(self):** feasible
- **VERIFY [revise→feasible]:** CONFIRMED via CF docs + code. cloudflare:sockets connect() SocketOptions exposes ONLY {secureTransport:'off'|'on'|'starttls', allowHalfOpen:bool} — NO SNI/servername param, NO TCP keepalive, NO TCP_NODELAY, NO read timeout/abort. SocketInfo exposes only remoteAddress/localAddress (may be null). NO peer certificate, ALPN, negotiated protocol, or cipher is ever surfaced to host JS or the VM (TLS terminates inside workerd). So items (3) keepalive/NODELAY=hard-limit and (4) cert/protocol/cipher/ALPN/mTLS=hard-limit are FACTUALLY CORRECT. SNI: connect() derives SNI from the connect-address hostname (no separate knob), and host-sockets.mjs:147-152 passes {hostname,port} to connect() — so SNI-on-wire is correct for the default case; item (1) mechanism is right. The DO calls connect() directly (host-sockets.mjs makeSocketHost); no fetch round-trip needed.
  - blockers: ITEM (2) setTimeout idle-fire mechanism is FALSE as written — REJECT this sub-claim, rest is fine. Proposal asserts 'the read loop already polls' and host can 'piggyback {idleMs} on the existing read envelope' with 'zero new host call'. It does NOT poll: host-sockets.mjs read() does `await reader.read()` which BLOCKS indefinitely on an idle socket — it never returns while no bytes arrive, so there is NO envelope to stamp idleMs onto and NO opportunity to return {timedOut:true}. To time-bound an idle read you MUST wrap reader.read() in Promise.race against a NEW host-side timer — not zero-cost. Compounding: workerd freezes wall-clock in-turn and a fired host timer needs the event loop to yield on real I/O to advance. Honest impl is feasible but materially different (race + real DO-side timer), so effort is NOT 'S' for item 2 (S holds for doc-honesty items 1/3/4/5 only). SECONDARY: item (1) 'set sock.servername (already done)' is mis-attributed — servername IS set, but in tls.js:53 (VM-side cosmetic), NOT host-sockets; and the real residual gap (caller-supplied options.servername DIFFERING from the connect host is silently dropped, audit:701) is glossed. Item (4) honesty edits have REAL code to fix: tls.js:55 getProtocol returns hardcoded 'TLSv1.3', tls.js:51 authorized=true, tls.js:60 alpnProtocol=false — the actual LIEs; null/{} is correct, zero-entropy, byte-identical restore. Snapshot/imports/single-realm claims hold (socket state is DO-memory Map, never in WASM image; socket.* is a __hostCall WS effect not a WASM import — 12-import ceiling untouched). Item 5 container escape is real (host.sandbox.* live).
  - evidence: apps/kernel/src/host-sockets.mjs:147-152 — connect() called with only {secureTransport, allowHalfOpen}; no servername/keepalive/timeout possible, apps/kernel/src/host-sockets.mjs:read() — `await reader.read()` blocks indefinitely on idle, no poll/abort: refutes 'read loop already polls' / 'piggyback idleMs', apps/kernel/stdlib-src/shims/tls.js:51,55,60 — authorized=true, getProtocol()='TLSv1.3', alpnProtocol=false: the real LIEs to make honest, apps/kernel/stdlib-src/shims/tls.js:53 — sock.servername set VM-side (refutes 'already done' in host-sockets), apps/kernel/stdlib-src/shims/net.js:519-521 — setNoDelay/setKeepAlive/setTimeout no-ops; net.js:7-13 explicit 'NO INACTIVITY TIMER, parks forever' warning

### STREAMS

- **findings:** #24 (Writable.write always true / unpipe no-op / no highWaterMark), #33 (stream/web returns Node streams not WHATWG; WritableStream/TransformStream absent; ReadableStream.tee/pipeThrough/pipeTo throw), plus the audit's stub-unacceptable entries at lib.rs:1751/1741/2163 and the createReadStream/createWriteStream "no hwm backpressure" notes (lib.rs:1718-1746,2163).
- **root cause:** pure-impl-gap · **fix:** Pure-JS, additive, in the same BOOTSTRAP blob — NO CF primitive, NO host import. Three parts: (1) Add a proper WHATWG layer: complete EngramRS (ReadableStream) with highWaterMark/size queuing-strategy accounting, controller.desiredSize, tee(), pipeTo(dest,{signal}), pipeThrough({writable,readable}), and a byte-vs-count strategy; add new WritableStream (queue + desiredSize + backpressure-aware writer.ready promise + abort/close) and TransformStream (paired readable/writable wired through a transformer with backpressure between sides); add ByteLengthQueuingStrategy/CountQueuingStrategy globals. Model backpressure with a resolved-promise/microtask 'ready' gate keyed on (queued size >= highWaterMark) — never wall-clock. (2) Fix the alias: B['stream/web']/B['node:stream/web'] must export {ReadableStream, WritableStream, TransformStream, ByteLengthQueuingStrategy, CountQueuingStrategy} (the WHATWG classes off globalThis), NOT __stream. Keep require('stream') -> __stream (Node) unchanged. (3) Repair the Node __stream module: implement real highWaterMark accounting in Writable (write() returns false when buffered length >= hwm=16384 default / 16 objectMode, emit 'drain' when it drains below hwm), make Readable.push() return false at hwm so pipe()'s already-present pause/resume branch becomes live, and implement Readable.prototype.unpipe(dest) to actually remove the 'data'/'end' listeners installed by pipe() (store them on a _pipes list). Add Duplex.fromWeb/toWeb + Readable.fromWeb/toWeb + Writable.fromWeb bridges so Node<->WHATWG interop works (pure JS adapters over the two impls).
- **CF primitive:** NONE (pure fix). The existing host.streamRead/streamWrite park-resume plumbing in kernel-glue.ts is reused unchanged for the ONE real-I/O case (fetch body); the new WHATWG WritableStream/TransformStream and Node hwm logic are in-heap pure JS over arrays+microtasks with no new boundary crossing.
- **mechanism:** All stream classes live in the single pure-JS BOOTSTRAP string eval'd by rquickjs (lib.rs:197 / installed at lib.rs:3199), re-run on every fresh instance and on cold restore (WIRED-guarded), so the surface is reconstructed deterministically post-blit — identical to how EngramRS already survives today. Backpressure is a state-machine over an in-heap queue: each stream tracks queuedSize; controller.desiredSize = highWaterMark - queuedSize; a producer's writer.ready / Readable.push() return value is computed from that and the 'drain'/ready promise resolves on queueMicrotask when a consumer dequeues. NO host call, NO wall-clock, NO timer is involved in the pure in-heap streams. The only host-backed stream remains Response.body's pull()->host.streamRead (lib.rs:1050) which is untouched; the new pipeTo/pipeThrough simply consume that existing ReadableStream through the new pure machinery, so 'relation to fetch streaming' is: fetch streaming already depends on EngramRS, and this fix makes that same ReadableStream teeable/pipeThrough-able and lets a fetch body be piped into a pure TransformStream (e.g. a gunzip transform written in cell JS) entirely in-heap.
- **determinism:** ZERO impact, determinism-by-default preserved. Backpressure/queue draining settle exclusively on queueMicrotask + resolved promises (the rquickjs job queue), never Date.now/performance.now/setTimeout — same discipline the existing __stream and EngramRS already follow (lib.rs:1719-1720 comment). No entropy, no clock read introduced. Seeded byte-identical restore is unaffected: these are deterministic synchronous/microtask state machines. NOT opt-in — safe to default-on. · **snapshot:** Neutral-to-positive. The classes are BOOTSTRAP code re-installed on each instance, not snapshotted heap data, so they add ~0 to the persisted image (BOOTSTRAP string is part of the engine binary, not the heap). At runtime a hwm-bounded stream now CAPS in-heap buffering at highWaterMark instead of the current unbounded accumulation (Writable.write always-true today buffers without limit until tick-budget/OOM) — so this REDUCES the chance of a pipe/transform pipeline driving the monotonic linear-memory toward the ~24-28MB incompressible cliff. An in-flight stream's queue is ordinary heap; if a snapshot is taken mid-pipeline it restores like any other live object (BOOTSTRAP re-installs the prototypes; the queue array is in the blitted heap), preserving the existing live-state fidelity guarantee. Source size: BOOTSTRAP grows ~3-5KB of JS, well under the 500KB injected-source cap. · **imports:** NONE. Consumes zero WASM import slots. No new host effect is added — the fix is in-heap JS, and the single real-I/O path (host.streamRead/streamWrite) already exists and is unchanged. Stays well under the 12-import ceiling.
- **effort:** M · **risk:** low · **feasibility(self):** feasible
- **VERIFY [confirmed→feasible]:** N/A in the literal sense — the fix uses NO Cloudflare API (the proposal correctly claims "CF primitive: NONE"). The only CF-touching path it relies on (host.streamRead/streamWrite park-resume for the fetch body) already exists in kernel-glue.ts (_doStreamRead at 2204-2257, dispatch at 1773-1775) and is explicitly left UNCHANGED — verified: one-chunk-per-pull IS the host-boundary backpressure. So there is no new CF round-trip, no new import, no DO-callability question, no fetch-to-self. The relevant reality is the engram-internal runtime, and the fix is pure in-heap JS over the rquickjs job queue (microtasks), which is real and supported. 12-import ceiling untouched (imports=0 confirmed; no new boundary crossing). Determinism claim HOLDS: existing __stream/EngramRS already settle exclusively on queueMicrotask + resolved promises with zero wall-clock (lib.rs:1719-1720 comment verified; grep shows no Date.now/performance.now/setTimeout in any stream code path), and the proposed classes follow the same discipline → seeded byte-identical restore unaffected. BOOTSTRAP re-install on every fresh instance + cold restore is WIRED-guarded (lib.rs:3195-3217), so the new pure-JS surface reconstructs deterministically post-blit exactly like EngramRS does today, adding ~0 to the persisted image.
  - blockers: No hard blockers; all three diagnosed bugs are real and the fix is mechanically sound. Caveats: (1) The "reduces the ~24-28MB monotonic cliff" claim is only PARTIALLY true — hwm caps PIPE()/pipeTo-driven buffering, but the existing Readable async-iterator path (lib.rs ~1735) pushes unboundedly into st.buffer regardless of flowing state, so a slow `for await` consumer still accumulates; this fix does not make all pipelines cliff-safe. (2) Node hwm semantics are subtle: write() returns false when buffered>=hwm but STILL accepts the chunk, and 'drain' must fire exactly once when draining below hwm — get needDrain/emit('drain') ordering wrong and a producer awaiting 'drain' deadlocks; needs a real test matrix. (3) tee()/pipeThrough over the host-backed Response.body ReadableStream (lib.rs:1050) means both branches pull a SINGLE upstream host-side cursor → tee must internally buffer for the lagging branch, which itself grows heap (the pure-microtask model handles ordering but not lagging-branch memory). (4) Mid-pipeline snapshot/restore is fine for plain array/Uint8Array queues, but if a queuing-strategy size() or transformer closure is involved it restores like any live closure — fine, provided the BOOTSTRAP-reinstalled prototypes are byte-identical across engine versions (the existing engine-hash restore guard already covers this). (5) Effort=M is honest-to-slightly-optimistic: WritableStream + TransformStream + correct WHATWG backpressure gate + Node hwm/drain + unpipe listener bookkeeping + 5 fromWeb/toWeb bridges + tee lagging-buffer is a meaningful spec-conformance surface; call it M-to-M+.
  - evidence: apps/kernel/engine/src/lib.rs:2163 — alias bug confirmed: B['stream/web']=__stream exports Node EventEmitter streams under the WHATWG name (wrong API shape), apps/kernel/engine/src/lib.rs:1741 — Readable.prototype.unpipe = function(){return this;} is a no-op; pipe at :1740 installs unremovable anonymous self.on('data') closures, apps/kernel/engine/src/lib.rs:1751 — Writable.prototype.write always returns true, needDrain never set true anywhere → pipe's `if(ok===false)` backpressure branch is dead code, apps/kernel/engine/src/lib.rs:~1730 — Readable.prototype.push always returns true, no highWaterMark accounting, apps/kernel/engine/src/lib.rs:266-313 — EngramRS is the ONLY ReadableStream; no WritableStream/TransformStream/QueuingStrategy globals; no desiredSize/tee/pipeTo/pipeThrough

### PROCESS MODEL

- **findings:** (1) process.env unwired — config.env not read, __processEnv never set, always {}. (2) process.memoryUsage returns all-zeros despite engine exporting used_heap()/buffer_bytes(). (3) process.exit — ALREADY DONE (catchable ProcessExit). (4) process.hrtime/uptime — ALREADY DONE (seeded clock). (5) signals (kill/on('exit')/SIG*) — HARD LIMIT (no real process lifecycle; listeners stored, never auto-fired).
- **root cause:** unwired-existing-capability · **fix:** Two small glue-side wirings, no engine change required for env, trivial engine tweak for memoryUsage. (A) process.env: add `env?: Record<string,string>` to KernelConfig; in _applyConfig (kernel-glue.ts:1216) read cfg.env and, in a new idempotent _seedProcessEnv() (modeled on _applyFsProvider:1160 / _seedStdlibMeta:1176), eval `globalThis.__processEnv = (<sanitized JSON>); if(globalThis.process) globalThis.process.env = Object.assign(globalThis.process.env||{}, globalThis.__processEnv);0` into the VM. Call it from createFresh AND restore (both already run _applyConfig + the seed helpers). Coerce all values to strings (Node env semantics), drop a deny-list of dangerous keys if desired. On a FRESH session env reflects config; on a cold-woken session the heap already holds any in-VM mutations the cell made (process.env is a plain heap object), and re-seeding merges config defaults back without clobbering cell writes (Object.assign target wins is configurable — use seed-as-base, cell-mutations-win by seeding only missing keys on restore). (B) process.memoryUsage: engine already has used_heap()/buffer_bytes(); change lib.rs:814 to return live values via the in-VM __memUsage() that the glue seeds at create/restore the same way (`globalThis.__memSnap={heapUsed:<used_heap>,rss:<buffer_bytes>}`), OR — cleaner — expose used_heap to the VM by writing the current scalar into a global on each eval_begin so memoryUsage() returns {rss:bufferBytes,heapTotal:bufferBytes,heapUsed:usedHeap,external:0,arrayBuffers:0}. Keep it a per-create/restore SNAPSHOT scalar (not live per-call) to avoid any host-import round-trip.
- **CF primitive:** NONE (pure glue/engine fix; config.env is plain structured data already round-tripped through the snapshot manifest's configJson)
- **mechanism:** End-to-end: client sends {t:create,config:{env:{FOO:"bar"},...}}. lib.rs create_critical persists config JSON in meta.config (same path as rngSeed/clock/fetch/extensions — already validated+stored). Glue _applyConfig(configJson) parses it; new _seedProcessEnv() encodes cfg.env to a JSON string and runs a single eval_begin into the live VM (using the existing _writeScratch + scratch_ptr + eval_begin shim, budget ~200k, makes ZERO host calls so never parks) that sets globalThis.__processEnv and merges it into process.env. The engine bootstrap (lib.rs:776-782) already reads __processEnv, so ordering is robust either way. Because process.env is a plain heap object, all cell mutations live in the WASM linear memory and survive snapshot/restore for free. On restore, _applyConfig + _seedProcessEnv re-run from the persisted configJson (restore() at line 1346 already calls _applyConfig and the seed helpers), so config-provided env is re-asserted as a base without clobbering cell writes. For memoryUsage, glue seeds globalThis.__memSnap={heapUsed:used_heap(),rss:buffer_bytes()} at create/restore (and optionally refreshes it at the top of each eval via a 1-line scalar write before eval_begin); lib.rs:814 reads that global instead of hardcoded zeros.
- **determinism:** ZERO determinism break, default-safe. config.env is static structured input (no entropy), identical across restore because it round-trips through the persisted configJson — byte-identical seeded restore preserved. memoryUsage seeded from used_heap() is the deterministic rquickjs allocator tally (already used for size-admission), and buffer_bytes() is page-count — both reproducible for a seeded session given identical eval history; to stay byte-identical, seed it as a per-create/restore snapshot scalar (or refresh per-eval BEFORE the cell runs) rather than letting a cell observe a non-reproducible live value mid-turn. No opt-in flag needed; this strengthens fidelity without touching the seeded clock/RNG regime. · **snapshot:** Negligible/positive. process.env and __processEnv are plain heap objects already captured by the WASM-linear-memory snapshot — env values add a few hundred bytes to the heap at most (cap env total size, e.g. <=64KB, to stay far below the ~24-28MB monotonic cliff). No new manifest column needed: env rides inside the existing meta.config configJson that is already persisted and re-applied on restore. memoryUsage seeding writes one tiny global. No effect on dump/restore ceilings. · **imports:** ZERO — consumes NO WASM import slot. Both fixes are pure in-VM globalThis writes via the existing eval_begin/scratch shim; used_heap()/buffer_bytes() are already-compiled engine EXPORTS (host calls them, not imports), and process.env/memoryUsage are pure JS heap closures. The 12-import ceiling is untouched.
- **effort:** XS · **risk:** low · **feasibility(self):** ship-now
- **VERIFY [confirmed→ship-now]:** No CF API is involved — correctly stated as "CF primitive: NONE". Pure glue+engine work. config.env rides inside the already-validated, already-persisted meta.config string (persist_config in apps/kernel/src/lib.rs has a 512KB ceiling; the extensions manifest already lives there at a 64KB cap), round-tripped through configJson on every create/restore. No DO/R2/AE/Worker-Loader call; no async-vs-sync or fetch round-trip. The CF-adjacent facts that matter (WASM-linear-memory monotonic snapshot, 12-import ceiling, single realm) are all UNTOUCHED: process.env is a plain heap object that snapshots for free, and used_heap()/buffer_bytes() are already-compiled engine EXPORTS (host-callable, zero import slots).
  - blockers: Confirmed; three honest caveats, none fatal. (1) ORDERING is correct but load-bearing — BOOTSTRAP (engine/src/lib.rs:776-782) runs LAZILY inside ensure_wired() on the FIRST eval_begin, and the glue seed helpers are themselves eval_begin calls, so they trigger BOOTSTRAP first. On FRESH: BOOTSTRAP sees __processEnv=undefined and sets p.env={}, so _seedProcessEnv MUST run as a SEPARATE eval AFTER and merge via Object.assign(p.env, cfg.env) — exactly the path proposed. Do NOT rely on pre-setting __processEnv before create(); BOOTSTRAP hasn't run yet at that instant. (2) RESTORE re-runs BOOTSTRAP on the blitted heap (WIRED is a fresh-instance thread-local = false), but the existing `if(!p.env)` guard (lib.rs:780) preserves cell mutations; _seedProcessEnv on restore must seed ONLY-missing keys (or seed-as-base/cell-wins) to avoid clobbering — proposal states this. (3) HONESTY NIT: the memoryUsage half is NOT strictly "no engine change" — p.memoryUsage at lib.rs:814 is a CLOSURE baked into the BOOTSTRAP string. Making it read used_heap/buffer_bytes requires EITHER an engine recompile+redeploy (cargo build, must run in-thread per the documented 180s-watchdog deploy lesson) OR the glue re-seeds p.memoryUsage via an eval each create/restore (pure glue, no recompile — the cleaner XS path, listed as the proposal's "OR cleaner" option). Env half is pure glue/XS; memoryUsage half is glue-only XS via re-seed, or S via engine edit. (4) memoryUsage must be a per-create/restore SNAPSHOT scalar (or refreshed BEFORE the cell runs), not a live per-call round-trip, to stay byte-identical for seeded sessions — proposal states this. used_heap() returns -1 if CTX is None (pre-create); guard. Net: effort XS, ship-now, determinism preserved, snapshot/import ceilings untouched. Cap env total (<=64KB) as proposed.
  - evidence: apps/kernel/engine/src/lib.rs:776-782 (process.env reads globalThis.__processEnv, plain heap object, `if(!p.env)` guard), apps/kernel/engine/src/lib.rs:814-815 (process.memoryUsage hardcoded zeros — the stub to fix; it is a closure baked into BOOTSTRAP), apps/kernel/engine/src/lib.rs:3106-3115 (used_heap()=memory_used_size, buffer_bytes()=cur_pages*65536 — engine EXPORTS, no import slot; used_heap returns -1 when CTX None), apps/kernel/engine/src/lib.rs:3193-3217,3433-3435 (BOOTSTRAP applied lazily via ensure_wired() on first eval_begin — the ordering constraint), apps/kernel/src/kernel-glue.ts:1216-1252 (_applyConfig parses configJson)

### MODULE SYSTEM

- **findings:** (1) top-level import/export in a CELL unsupported; (2) dynamic import('pkg') / import('./x') unsupported (no rquickjs Loader/Resolver wired); (3) require() relative-path './x.js' does not read the VFS, only falls through to basename cache
- **root cause:** unwired-existing-capability · **fix:** Surface-wire the THREE gaps onto machinery that already exists and is already deterministic + snapshot-safe — no new CF primitive, no new WASM import. (A) CELL import/export desugar: extend the host-side repl-transform.ts to rewrite depth-0 statement-start `import`/`export` into the existing async loader instead of bailing. `import X from 'p'` / `import {a,b} from 'p'` / `import * as ns from 'p'` -> `const {default:X}=await use('p')` etc.; bare `import 'p'` -> `await use('p')`; relative `import x from './m'` -> `await use('./m')` (routes to the new VFS branch in B). `export const f=...`/`export default ...`/`export {a}` -> assign the value to a per-cell `globalThis.__exports` object AND keep the binding (notebook cells already persist top-level bindings as globals, so `export` just additionally records the name->value into a snapshot-persisted module-namespace object keyed by an optional cell module id). This is the same depth-0 token-scanner that already rewrites `const`->global; flip the two existing bail points (repl-transform.ts ~L192/L208 `pendingAsyncOrExport`) to emit a rewrite. Because the cell now contains `await`, route it through the existing async-function-body wrap (wrapAsyncCompletion) already used for top-level await. (B) require() relative VFS: in the engine require() sync resolver (lib.rs ~L2303), when `name` starts with './' or '../' or '/', resolve it via the SAME `__wsResolveAbs` clamp already used by fs, look up a synchronously-available source: first an in-heap module cache `globalThis.__filemods[absPath]`, else throw the existing actionable MODULE_NOT_FOUND telling the caller to `await use('./x')` first (sync host IO is impossible — documented limit). `use()` gains a relative-path branch: if name starts with ./ ../ or /, it host-parks `host.vfs.read(absPath)` (the already-wired VFS host effect over R2 /workspace) instead of host.fetch(esm.sh), evals it as CJS/ESM through the identical frame/__esmEval path, and caches under BOTH the package cache `__mods` and `__filemods[absPath]` so a subsequent sync `require('./x')` resolves. (C) dynamic import(): two layers — (c1) pure in-VM: a BOOTSTRAP-installed `globalThis.import = (s)=>use(s)` is NOT enough because native `import()` is syntax, not a callable; instead wire rquickjs `Runtime::set_loader` with a custom Resolver+Loader pair whose Loader, on a cache miss, PARKS a host call (reuse the __HOSTCALL park/eval_resume cycle) to fetch the module source (esm.sh for bare specifiers, host.vfs.read for relative), then returns it as a `Module` — making native `import('p')` and `import('./x')` resolve through the loader. (c2) cheaper fallback if set_loader+park integration is too invasive: a host-side source rewrite turning `import(expr)` call-expressions into `globalThis.use(expr)` (regex/token rewrite in repl-transform; import() is already a function-call shape so this is a clean substitution and preserves the Promise contract). Ship (A)+(B)+(c2) first (all pure source/JS, XS-S); treat the native set_loader path (c1) as the follow-up for true `import()` fidelity.
- **CF primitive:** NONE (pure fix). Reuses already-bound effects: host.fetch (mediated egress, already on the fetch allowlist for esm.sh/jsdelivr) and the existing R2-backed /workspace VFS host effect. No new binding.
- **mechanism:** End-to-end: a CELL like `import _ from 'lodash'; export const f = _.chunk` is scanned by the existing depth-0 host-side transform; `import` rewrites to `const _ = (await use('lodash')).default ?? await use('lodash')`, `export const f=...` rewrites to a normal global assignment PLUS `globalThis.__exports['f']=f` (a plain heap object that snapshots like any value). Because the rewritten cell contains `await`, the existing wrapAsyncCompletion path runs it as an async function body. `use('lodash')` is the already-shipped loader: it `__HOSTCALL('fetch', url)` -> Rust parks the request, status=HOST_CALL, control returns to the JS shim, shim does the real `fetch()` and calls `eval_resume` settling the VM Promise; the fetched bundle is evaluated (CJS frame or `__esmEval` -> real `Module::declare/eval/finish`), its namespace copied to a plain object, cached in `globalThis.__mods`. For a RELATIVE specifier, `use('./m')` instead parks `host.vfs.read('/workspace/m.js')` (R2), same eval/cache path, additionally caching by absolute path in `globalThis.__filemods` so sync `require('./m')` then resolves from the heap cache. Native `import()` (follow-up c1) routes through `Runtime::set_loader` whose Loader parks the same host fetch/vfs call on a miss and returns a compiled Module. Every loaded module is a plain heap object -> rides the existing W5+W4+E6 snapshot, so cold-restore needs no re-fetch (already proven for `use()` today).
- **determinism:** No new nondeterminism, no opt-in flag needed by default. The ONLY entropy source is the network fetch, which is identical to today's `use()` — already mediated through host.fetch and oplog-recorded, so seeded byte-identical restore is PRESERVED (a cold-restored session re-reads the cached heap module object, never re-fetches). Compilation/eval is pure. Relative-import VFS reads are deterministic given the same /workspace contents (R2 read, already part of the mediated effect set). Native set_loader (c1) park reuses the same oplog-recorded host call, so it stays deterministic too. Net: determinism-by-default UNCHANGED. · **snapshot:** Positive/neutral. Loaded modules are plain heap objects (the `use()` path already deliberately materializes a PLAIN-OBJECT copy of the namespace precisely because the QuickJS frozen module-namespace exotic was observed to drop on W4 byte-delta restore — that lesson is reused). `__exports`, `__mods`, `__filemods` are ordinary heap objects -> snapshot-persist, no re-fetch on cold wake. Caveat: module source counts toward the live heap and the ~18-20MB dump ceiling / ~24-28MB monotonic snapshot cliff exactly like today's `use()`; the existing MAX_STDLIB_SOURCE_BYTES-style source caps and used-heap tripwire apply unchanged. The native set_loader path must keep the same plain-object materialization (do NOT retain the frozen module-namespace exotic in the heap) to avoid the W4 drop. · **imports:** Zero WASM import slots consumed. (A) and (c2) are host-side source rewrites (pure JS/TS in repl-transform, runs in workerd). (B) reuses the existing VFS host effect and the existing __HOSTCALL park (no new C export). The native loader (c1) uses rquickjs Runtime::set_loader, which is internal engine wiring driven through the EXISTING pending_host_call/eval_resume ABI — it adds no new `extern "C"` import to the 12-import ceiling. Well clear of 12.
- **effort:** M · **risk:** medium · **feasibility(self):** feasible
- **VERIFY [revise→feasible]:** No new CF primitive is invoked, so the real test is the engram-internal ABI. (A)+(c2) are pure host-side source rewrites in repl-transform.ts running in workerd: real and sound. (B)'s loader path (host.fetch -> Rust HOSTCALL park -> shim does real fetch() -> eval_resume settles the VM Promise) is exactly how use() works today and is genuinely deterministic (oplog-recorded, esm.sh/R2 on the existing fetch allowlist, loaded modules materialized as plain heap objects that ride the W5+W4+E6 snapshot — verified in lib.rs use() L2561 and __esmEval L2957). The c1 set_loader claim CONTRADICTS reality: QuickJS module resolution inside Module::eval is SYNCHRONOUS, and the host-call boundary is a FULL UNWIND back to workerd (HOSTCALL_REQ set, JS pump returns, async work, eval_resume). A Runtime::set_loader Loader must return a Module synchronously and CANNOT park/await. The engine's own __esmEval proves it: it requires a self-contained bundle and treats any residual import as rquickjs::Error::WouldBlock it explicitly cannot satisfy (L2971-2975). So c1 as described ('Loader parks the same host fetch/vfs call and returns a compiled Module') is NOT reusable through the pending_host_call/eval_resume ABI.
  - blockers: 1) FALSE PREMISE in (B): proposal says use('./m') parks host.vfs.read('/workspace/m.js') over 'the already-wired VFS host effect over R2 /workspace'. That API does NOT exist. The DEFAULT /workspace fs is the IN-HEAP SYNC VFS (globalThis.__vfs, lib.rs L2343), NOT R2. The host-backed R2 path is host.__fs({op,...}) and is ONLY active when config.fs.provider != 'vfs' (default 'vfs', L2408; glue L131). For a default session there is no async R2 read to park — but there IS a synchronous in-heap readFileSync, which actually SIMPLIFIES (B): relative require('./m') can resolve SYNCHRONOUSLY from globalThis.__vfs.files, making the use()-pre-fetch dance unnecessary in the common case. The proposal mis-modeled the data plane. (For the opt-in host-backed-R2 provider, host.__fs IS async/parkable so use('./m') works there, but sync require('./m') still can't — as admitted.) 2) c1 (native import()) is effectively INFEASIBLE on the current ABI (sync loader vs async-unwind host boundary), not merely 'invasive' — demote from follow-up to research-needed/likely-reject-as-described; it needs a different mechanism (pre-resolve all imports before eval, or a sync in-heap module-source cache the loader reads — never a parked host call). 3) (A) export must flip BOTH bail sites: transformCell L192 AND wrapAsyncCompletion's STMT_LEAD set (L423 lists export/import); proposal names only L192/L208 and misses the wrapAsyncCompletion one, so an export-tail cell would lose its completion-value rewrite. 4) __exports/module-namespace persistence MUST use the same plain-object materialization use() does (L2621-2642 deliberately copies out of the frozen QuickJS namespace exotic because it DROPS on W4 restore) — noted but load-bearing. 5) import-rewrite to globals must honor RESERVED_HOST_GLOBALS shadow rule (L44) or it clobbers crypto/fetch/host. 6) Source counts toward the ~18-20MB dump ceiling / ~24-28MB monotonic cliff like use() — correctly flagged. Net: ship (A)+(B-corrected-to-sync-in-heap)+(c2); drop/redesign c1.
  - evidence: apps/kernel/engine/src/lib.rs:2957-2998 (__esmEval requires self-contained bundle; residual import=WouldBlock — proves sync module resolution can't park), apps/kernel/engine/src/lib.rs:2561-2696 (use() loader: host.fetch park, plain-object materialization, __mods cache), apps/kernel/engine/src/lib.rs:2336-2408 (in-heap SYNC __vfs is the DEFAULT; host.__fs/R2 only when provider!=vfs — refutes 'VFS host effect over R2' premise), apps/kernel/engine/src/lib.rs:2300-2334 (require() sync resolver; 'VM has no SYNC host IO, require cant fetch'), apps/kernel/engine/src/lib.rs:3221-3236, 3457-3631 (HOSTCALL park = full unwind to workerd, eval_resume settles Promise — not re-entrant; incompatible with sync Loader)

### TypeScript namespace/module silent ReferenceError miscompile + decorator SyntaxError; const-enum is a false-positive (already correct)

- **findings:** Audit finding #4 (TS namespace/module silent miscompile → runtime ReferenceError), #32 (TS decorators passed verbatim → QuickJS SyntaxError, SESSION-SUMMARY.md:30 false advertisement), and the const-enum REVISED line (confirm acceptable, no change). Plus the stale guard comment at kernel-glue.ts:1531 and stale docs/TS-REPL.md (written for the removed ts-blank-space path).
- **root cause:** pure-impl-gap · **fix:** Add a tiny pre-strip TypeScript construct DETECTOR in kernel-glue.ts (a new `rejectUncompilableTS(src)` helper called at the top of stripTypes, before the sucrase call) that scans for value-emitting/codegen-requiring TS constructs sucrase cannot lower — namespace/module declarations and decorators — and throws a typed `TypeScriptError` (clean reject) instead of letting sucrase silently mangle them. EMPIRICALLY CONFIRMED via sucrase@3.35.1: `namespace N{export const x=1} N.x` → sucrase emits just `console.log(N.x)` with the body fully erased (even WITH the `imports` transform added) → runtime ReferenceError on N; `@sealed class C{}` → emitted verbatim → QuickJS-ng SyntaxError. There is NO sucrase config knob that fixes either: sucrase's Transform union is jsx|typescript|flow|imports|react-hot-loader|jest (no namespace codegen, no decorator transform). Switching transformers (e.g. esbuild WASM, real tsc) is infeasible — it would add a second heavy transpiler to the host bundle (esbuild needs WASM, eating into nothing-to-do-with-the-12-import-VM-ceiling but bloating the ~400-line glue) for two rarely-used legacy constructs. The const-enum case needs NO change: empirically sucrase lowers `const enum E{A=1}` to a runtime `var E;(function(E){...})(E||(E={}))` IIFE that the REPL top-level-hoist persists across cells — the only loss is cross-file numeric inlining, which is meaningless in a single-realm REPL where every cell is one file. Detector should be a scoped lexical scan (skip strings/comments/template literals) keyed on the `namespace`/`module` contextual keyword at statement position and a leading `@ident` decorator before `class`/`export class`/class members; throw `TypeScriptError` with an actionable message ("TS `namespace`/`module` and decorators require code generation sucrase does not perform; refactor namespace N{...} to `const N = {...}` or an object literal"). Also: (a) fix the STALE guard comment at kernel-glue.ts:1531 (it still claims enum/namespace/parameter-properties are rejected — that was the removed ts-blank-space path; enum/const-enum now lower fine, only namespace/module/decorators reject); (b) delete the false "decorators" claim at docs/SESSION-SUMMARY.md:30; (c) rewrite docs/TS-REPL.md's reject table to the post-sucrase reality (enum/const-enum = SUPPORTED via IIFE lowering; namespace/module/decorators = rejected). Parameter-properties (`constructor(private x)`) are a separate sub-case — verify empirically and add to the detector if sucrase also mis-handles them (sucrase's `imports`-transform normally lowers them, but with disableESTransforms the behavior should be checked; if broken, same reject path).
- **CF primitive:** NONE (pure host-side JS fix in kernel-glue.ts; no CF binding)
- **mechanism:** End-to-end: a cell arrives at KernelDO.evalCode → stripTypes(src) (host-side, in workerd, sucrase inlined by esbuild). NEW: stripTypes first calls rejectUncompilableTS(src), a pure lexical scanner (no AST, no extra dep) that walks the source skipping string/template/comment spans and flags (1) a `namespace`/`module` identifier in statement position followed by an identifier and `{`, and (2) a `@`-decorator token immediately preceding `class` or a class-member. On a hit it throws an Error tagged name="TypeScriptError". stripTypes already wraps sucrase in try/catch and re-tags as TypeScriptError; the new throw rides the SAME path. evalCode's existing catch around the strip (kernel-glue.ts:1535-1545) serializes `{ok:false, valueType:"error", error:{name:"TypeScriptError", message, stack:""}}` back over the WS frame — socket stays alive, the eval mutex is released, the next eval works. No engine.wasm change, no WASI import, no host-call. The detector runs entirely host-side before any byte enters the QuickJS scratch buffer. This converts both finding #4 (silent ReferenceError) and #32 (SyntaxError surfacing as a confusing QuickJS parse error mid-eval) into one clean, typed, documented reject at the boundary — matching how the kernel already handles other un-erasable TS.
- **determinism:** ZERO. The fix is a pre-eval source-shape gate that either rejects (cell never reaches the VM) or passes the byte-identical source through to the unchanged sucrase strip. It does not touch the seeded clock/RNG/crypto, adds no entropy, and runs deterministically on source text. Seeded byte-identical snapshot/restore is unaffected. Not opt-in and need not be — rejecting un-compilable TS is strictly safer than today's silent miscompile and is gated by the existing `config.typescript` flag (a `{typescript:false}` session skips stripTypes entirely, so the detector is also skipped, preserving the raw-passthrough escape hatch). · **snapshot:** NONE. The detector runs before any VM eval; a rejected cell mutates no QuickJS heap state, so no snapshot is taken or altered. An accepted cell produces byte-identical heap to today (same source reaches sucrase). No new manifest columns, no heap-size change, no effect on the monotonic-buffer cliff or W4 byte-delta restore. · **imports:** NONE. Pure host-side JS in kernel-glue.ts (workerd side, never WASM). Consumes zero of the 12 WASM import slots — it never crosses into engine.wasm. Adds no dependency (lexical scan is hand-written; sucrase is already bundled).
- **effort:** S · **risk:** low · **feasibility(self):** ship-now
- **VERIFY [revise→feasible]:** No CF API is involved — "CF primitive: NONE, pure host-side JS in kernel-glue.ts" is ACCURATE and verified. stripTypes runs in workerd; sucrase is inlined by esbuild (package.json:20 -> resolved 3.35.1). The detector never crosses into engine.wasm, so it consumes 0 of the 12 WASM-import slots; touches no DO SQLite, R2, AE, or Worker Loader; takes/alters no snapshot or manifest. The reject path is real: stripTypes is called at kernel-glue.ts:1536 INSIDE the try/catch at 1535-1545 that serializes {ok:false,valueType:"error",error:{name:"TypeScriptError",...}} over the WS frame; strip runs BEFORE this._ex()/mutex acquisition, so the socket stays alive and the mutex is never taken — as claimed. config.typescript gate is real (tsEnabled 973/1041/1241; {typescript:false} skips strip and therefore the detector -> raw-passthrough escape hatch preserved). Determinism=ZERO / snapshot=NONE are correct: rejected cell never reaches the VM; accepted cell sends byte-identical source to the unchanged sucrase call.
  - blockers: DIAGNOSIS is 100% correct, reproduced under the ACTUAL prod config (transforms:["typescript"],disableESTransforms:true — NOT the "imports" transform the writeup speculates about; that detail is moot/wrong but harmless): namespace/module bodies fully erased leaving console.log(N.x) -> ReferenceError; @decorator (class AND member) emitted verbatim -> QuickJS SyntaxError; const-enum AND plain enum lower to a working IIFE (false-positive confirmed). EMPIRICAL CORRECTION: parameter-properties (constructor(private a)) lower CORRECTLY under prod config (constructor( a){;this.a=a;}) — they are NOT broken, so MUST NOT be added to the detector (proposal left this open; resolve to: exclude). REVISE (not confirm) because the proposed IMPLEMENTATION — a hand-rolled char scanner skipping strings/comments, keyed on "namespace/module keyword at statement position + identifier + {" — is the weak point and is mis-scoped as ship-now/S. In a Node-SHAPED REPL, `module`/`namespace` are realistic plain identifiers: verified sucrase compiles `let module={};module.x=1`, `const namespace=5`, `function namespace(x){}`, `{module(){}}` as valid JS. A naive statement-position char scanner risks FALSE-REJECTING these — a regression strictly worse than the rare silent miscompile. The robust fix costs barely more and adds zero deps: sucrase's PUBLIC getFormattedTokens(src,cfg) (already bundled) flags the namespace/module DECLARATION name token with isType set vs an ordinary identifier (identifierRole=2, no isType), and emits a distinct "@" token Label for decorators — keying on isType + the @-label eliminates the false-positive class the char scanner must re-derive by heuristic. DOC SCOPE understated: SESSION-SUMMARY.md:30 falsely lists "decorators" SUPPORTED (delete-correct). docs/TS-REPL.md is FAR more wrong than admitted — it currently claims enum (79), const-enum (80), namespace/module (81-82) AND parameter-properties are ALL rejected (dead ts-blank-space 266/267 path); post-sucrase reality: enum/const-enum/param-props SUPPORTED, only namespace/module/decorators rejected — and decorators were never in that table. Rewrite is bigger than "rewrite the reject table". Stale comment is at kernel-glue.ts:1529-1533 (confirmed stale). No existing detector exists anywhere (repl-transform.ts + kernel-glue.ts clean) -> the gap is real and these silently miscompile today.
  - evidence: apps/kernel/src/kernel-glue.ts:28 sucrase import, apps/kernel/src/kernel-glue.ts:44-53 stripTypes uses transforms:["typescript"],disableESTransforms:true (NOT imports), apps/kernel/src/kernel-glue.ts:1529-1533 stale comment claims enum/namespace/parameter-properties rejected, apps/kernel/src/kernel-glue.ts:1535-1545 existing TypeScriptError catch envelope the detector rides, apps/kernel/src/kernel-glue.ts:973/1041/1241 tsEnabled / config.typescript gate

### util.inspect / deepStrictEqual / TextDecoder fidelity

- **findings:** util.inspect delegates to globalThis.__preview (lib.rs:1552/2701), ignoring most opts (showHidden/getters/maxArrayLength/maxStringLength/sorted; no symbol or non-enumerable keys; hardcoded 100/4096 caps). isDeepStrictEqual + assert.deepStrictEqual/deepEqual route through globalThis.__deepEqual (lib.rs:1612) which uses only Object.keys (skips Symbol + non-enumerable own props), misses boxed primitives, one-sided cycle guard. TextDecoder (lib.rs:409) ignores its label arg: every encoding decodes as UTF-8; no fatal/ignoreBOM/stream, no ICU.
- **root cause:** pure-impl-gap · **fix:** Pure-JS edits to the boot shim strings in apps/kernel/engine/src/lib.rs; no CF primitive, no new WASM import. (A) inspect: deepen __preview to honor the opts bag (maxArrayLength/maxStringLength/showHidden/getters/sorted plus depth), include Symbol keys via getOwnPropertySymbols and non-enumerable own props via getOwnPropertyNames when showHidden, eval getters when getters:true, keep 100/4096 as defaults. (B) __deepEqual: add getOwnPropertySymbols to the key-set, compare non-enumerable own props, unwrap boxed primitives via valueOf+toStringTag, two-sided cycle guard. (C) TextDecoder: keep UTF-8 default, add small pure-JS paths for labels Node guarantees without ICU (utf-16le/utf-16/ucs-2, utf-16be, latin1/iso-8859-1, windows-1252, ascii) plus fatal+ignoreBOM+stream option surface. Declare full ICU (~100 legacy multibyte: shift_jis/gbk/big5/euc) a hard platform limit (multi-MB ICU blows the snapshot cliff); route true legacy-multibyte to the existing Tier-2 sandbox container as an opt-in escape hatch.
- **CF primitive:** NONE (pure-impl fix). ICU legacy-multibyte escape hatch is the existing Tier-2 Container engram-sandbox, already wired, no new binding.
- **mechanism:** All three shims are JS source strings concatenated into the QuickJS realm at boot and re-evaluated identically on cold-restore (boot source runs before the snapshot blit, so behavior is fixed across hibernate/resume). inspect already calls __preview(x,depth); pass the opts object through and branch on fields (Symbol and non-enum enumeration is pure reflection, no host call). __deepEqual runs synchronously in-VM; extending key-set, boxed-primitive, and cycle handling is pure in-VM JS. TextDecoder.prototype.decode picks a per-encoding path once in the constructor from a normalized label (single-byte ~256-entry consts, UTF-16 arithmetic). No __hostCall, no WASI fn, no WebAssembly import. Legacy multibyte: a cell calls s.sandbox.exec to decode in the container sharing the /workspace R2 plane.
- **determinism:** No break to seeded byte-identical restore. All three are pure functions of input (no clock/RNG/crypto/network entropy); the deepening is more correct but deterministic; single-byte/UTF-16 tables are static. Only non-deterministic path is the opt-in sandbox ICU escape hatch (real container), already outside engram determinism contract like host.fetch/sandbox.exec. · **snapshot:** Negligible-to-small: boot-time source, no per-cell heap growth. Source add ~1-2KB (inspect/deepEqual) plus ~few KB single-byte tables, well under the v0.7 500KB injected-source guard and nowhere near the 24-28MB cliff. Full ICU is rejected precisely because multi-MB baked data would push images toward the 18MB dump ceiling / WS-1006 cliff; staying small protects the envelope. · **imports:** Zero WASM import slots consumed. All edits are in-VM JS source; no new WASI/host import. The 12-import ceiling is untouched (decoding is arithmetic plus lookup tables).
- **effort:** M · **risk:** low · **feasibility(self):** feasible
- **VERIFY [confirmed→feasible]:** No new CF API is invoked for the core fix — correct. All three shims are JS source strings concatenated into the QuickJS realm at boot (engine/src/lib.rs: TextDecoder @409, __deepEqual @1612, __preview @2701, util.inspect @1552) and re-evaluated identically on cold-restore (boot runs before the snapshot blit), so behavior is fixed across hibernate/resume — mechanism accurate. Reflection primitives the fix needs (getOwnPropertyNames/getOwnPropertySymbols/getOwnPropertyDescriptor) are pure-VM and ALREADY used in this same shim (lib.rs:2584,2688) → no host call, no import slot, 12-import ceiling untouched. The ICU legacy-multibyte escape hatch is the REAL, already-wired Tier-2 path: s.sandbox.exec is implemented end-to-end (SDK index.ts:1270-1274 → kernel sandbox_frame lib.rs:1873 → engram-sandbox container /exec), gated on config.sandbox:true, sharing the /workspace R2 plane — no new binding. CF reality check passes: container exec is an async out-of-band fetch round-trip (sandbox_frame is async, NOT under self.mutex), so legacy-multibyte decode via the sandbox is opt-in + non-deterministic + explicitly outside the determinism contract — consistent with how host.fetch already works. Hot path touches no CF primitive at all, so no consistency/limit concern.
  - blockers: (1) SIGNATURE-CHANGE BLAST RADIUS: __preview is currently (v, depth) with 9 callsites (console.dir/table @1329-1332, mime bundle @2803/2808, displayJSON @2842, assert inspectShort @1647, eval preview @3281) plus util.inspect @1552. Threading an opts bag MUST be backward-compatible (duck-type the 2nd arg number|opts as util.inspect @1552 already does, or add an optional 3rd arg) or every callsite regresses console.log formatting. Design says 'pass the opts object through' and understates this. (2) getters:true EVALUATES getters → can throw, have side effects, or be an in-cell DoS; each getter eval must be try/catch-wrapped and respect the cell tick-budget. It is NOT a pure function of input as the determinism claim implies (a getter can read Date.now/Math.random) — but since it is opt-in and OFF by default, seeded byte-identical restore is preserved for the default path. Claim should be narrowed to 'default path deterministic; getters:true escapes the contract like sandbox.exec'. (3) deepStrictEqual Set fix (the load-bearing correctness win, replacing b.has(v) @1634 with element-wise recursion) is O(n*m) — bound it against the tick budget for large Sets. (4) windows-1252 != latin1: CP-1252 has the 0x80-0x9F C1 remap (€‚ƒ at 0x80,0x82,0x83); it needs a real 256-entry table, NOT 'latin1 arithmetic' — the design lumps them. (5) utf-16be surrogate-pair handling for astral planes is not mere LE arithmetic-symmetry; handle it. (6) fatal mode must actually THROW TypeError on malformed sequences (current decoder silently substitutes); verify no regression in StringDecoder @1709 / Buffer.toString which depend on the decoder. (7) Snapshot-cliff claim is HONEST but for a different reason than stated: the 500KB MAX_SOURCE_BYTES guard (lib.rs:1602) is on CELL source, NOT the baked boot shim — the shim grows the wasm DATA segment / initial heap directly, so the real ceiling is the ~18MB dump guard / 24-28MB monotonic cliff. ~1-2KB code + a few ~256-entry single-byte tables stays negligible against that; full-ICU rejection is correct. (8) Out of scope (state it): util.inspect.custom / Symbol.for('nodejs.util.inspect.custom') hooks (audit @1528) are NOT addressed — inspect parity stays partial for libs with custom inspect.
  - evidence: apps/kernel/engine/src/lib.rs:409-425 (TextDecoder: label stored, never consulted, UTF-8 only), apps/kernel/engine/src/lib.rs:1612-1642 (__deepEqual Object.keys-only; Set b.has(v) reference-identity bug @1634), apps/kernel/engine/src/lib.rs:1552 (util.inspect threads only depth), apps/kernel/engine/src/lib.rs:2701-2760 (__preview signature (v,depth); 100/4096 defaults; no Symbol/non-enum/getters), apps/kernel/engine/src/lib.rs:2584,2688 (getOwnPropertyNames already used in-VM — reflection present, no host call)

### DETERMINISM-vs-REALISM POLICY

- **findings:** All gaps rooted in seeded determinism: Date.now/new Date() epoch (1.7e12 base), performance.now, process.hrtime/uptime, dayjs.now()/moment-now, fs mtime/ctime (derive from seeded clock), Math.random, crypto.getRandomValues, crypto.randomUUID, node:crypto randomBytes/randomFillSync/randomInt, nanoid/uuid (route through seeded getRandomValues). One policy knob covers the whole cluster.
- **root cause:** engram-determinism-choice · **fix:** Promote the EXISTING partial `config.clock:"real"` into a coherent two-axis realism policy with one umbrella: (1) CLOCK axis — already shipped (clockSeed seeded from real wall clock at create, per-eval monotonic re-anchor via ex.set_clock); leave as-is, this already fixes Date/performance/hrtime/uptime/dayjs.now/mtime transitively. (2) ENTROPY axis — NEW `config.crypto:"real"`: when set, the engine `__rand` and WASI `random_get` draw from a glue-provided real-entropy source (workerd `crypto.getRandomValues`) instead of the seeded LCG/mulberry32, so Math.random + all of node:crypto + nanoid/uuid become true-CSPRNG. (3) UMBRELLA — `config.nondeterministic:true` is sugar that sets BOTH clock:"real" and crypto:"real" unless the granular keys override. All three are OFF by default (seeded byte-identical stays the default contract). Wire the entropy source the SAME way clock:real is wired: a host callback the glue installs at create AND restore, persisted via config_json. No new WASM import slot, no per-cell host round-trip.
- **CF primitive:** workerd Web Crypto global (globalThis.crypto.getRandomValues) — a runtime global, NOT a wrangler binding. Zero wrangler.jsonc config, zero env binding, zero quota. The CF "real Web Crypto" menu item. (The clock half reuses the DO host wall clock Date.now, already in use.)
- **mechanism:** CLOCK (done): _applyConfig seeds clockSeed=Date.now()-CLOCK_EPOCH_BASE_MS when clock=="real"; per-eval the glue calls ex.set_clock(realNow-base) monotonically (never backward) so inter-cell elapsed time is real; within a turn workerd freezes the clock so the +1ms/call step persists (documented floor). ENTROPY (new, ~symmetric): engine gains an entropy MODE flag set by create()/a new set_entropy_mode(real:bool) export (mirrors set_clock). In seeded mode __rand keeps the LCG and random_get keeps mulberry32 (unchanged byte-path). In real mode, both pull bytes from a glue-installed real source. Two viable bridges, both import-free: (A) PREFERRED — fill a small ring buffer of real-random bytes from the GLUE at the start of each eval turn (glue calls crypto.getRandomValues into a wasm scratch region via the existing setMem/scratch plumbing) and have __rand/random_get consume from it; refilled per turn so it never blocks and needs no synchronous host-call from inside the VM. (B) host-call __hostCall('crypto.random',n) round-trip (async, one await) — rejected: adds latency + only works at await points. Use (A): pure synchronous reads from a per-turn-refilled buffer, no new WASM import, no park/resume. The engine still increments RNG_CALLS so the counter semantics survive, but the values are real. Bytes are sourced host-side exactly like clock:real sources Date.now host-side.
- **determinism:** BREAKS seeded byte-identical restore — BY DESIGN and ONLY when explicitly opted in. clock:"real" already documents this. crypto:"real" extends it to entropy: a real-RNG session is non-reproducible across runs, so byte-identical replay/restore comparison is meaningless for it (the heap will differ between two runs even from the same source). Default (no flag) is UNCHANGED: seeded LCG/mulberry32, byte-identical across restore, the existing determinism-by-default contract holds. The flag is the explicit trade. AE should log a `configRealism` blob (none/clock/crypto/both) alongside the existing config so determinism status is observable per op. · **snapshot:** Snapshot/restore MECHANISM is unaffected — the heap still snapshots and restores identically (it's just an ArrayBuffer blit). What changes: in real mode the restored session does NOT byte-match a re-run, and the entropy/clock counters (clock_calls/rng_calls in the manifest) no longer let you replay to the same bytes — but the kernel never replays (heap-snapshot, no journal replay), so this costs nothing operationally. The realism flags persist in config_json (same path as clock/rngSeed today) so a cold restore re-enters the same mode. set_clock re-anchor + entropy-buffer refill both re-arm on restore via the existing create/restore config-apply path. No new manifest column strictly required (config_json carries it); optionally surface configRealism for queryability. · **imports:** ZERO. Consumes NO WASM import slot. The clock path reuses host Date.now (glue-side). The entropy path reuses workerd's crypto.getRandomValues (a glue-side global) writing into the EXISTING wasm scratch/setMem region — the same memory plumbing already present. __rand/random_get remain the same wasi_snapshot_preview1 / engine callbacks they are today (just reading a different byte source); no new function added to the import object. The 12-import ceiling is untouched.
- **effort:** S · **risk:** low · **feasibility(self):** ship-now
- **VERIFY [revise→feasible]:** YES on the CF-primitive level — but it is the LEAST load-bearing part of the claim. globalThis.crypto.getRandomValues IS a runtime global in workerd, available synchronously inside a DO with zero binding/quota (the kernel already calls crypto.subtle.digest host-side at kernel-glue.ts:2078 and kernel-glue.mjs:23704, proving Web Crypto is present in this exact context). So a glue-side real-entropy source needs no wrangler config, no env binding, no fetch round-trip, no async — that part of the proposal is accurate. The clock half reuses Date.now() host-side, also correct. The CF-reality problem is NOT the API; it is that the proposal mis-describes engram's OWN internals, and one of those (set_clock) does not exist in the shipped engine.
  - blockers: 1) FALSE PREMISE — clock:real is NOT "already shipped and working." The per-eval monotonic re-anchor depends on ex.set_clock(), which has NO definition in apps/kernel/engine/src/lib.rs (export surface at lines 3072-3171 has create/reattach/clock_calls/rng_calls/set_counters/used_heap/buffer_bytes/run_gc/scrub_arena — NO set_clock). The glue call is wrapped in try/catch with the literal comment "engine lacks set_clock (older build) -> leave clock as-is" (kernel-glue.ts:1571). So today clock:real only sets the CREATE-TIME base (clockSeed=Date.now()-base, kernel-glue.ts:1233) giving a real-ish starting year, but then the VM clock advances +1ms/call from that frozen base and DRIFTS from real wall time — it does NOT track real inter-cell elapsed time as the proposal claims. The CLOCK axis is NOT a no-op-to-implement freebie; it needs the set_clock export actually built. 2) WRONG SOURCE TARGETED — the proposal says make "__rand/random_get consume from a glue buffer." But __rand is a Rust closure (Function::new, engine lib.rs:2923) executing INSIDE wasm with NO glue access; it reads thread-local RNG via lcg(). random_get (the WASI import at kernel-glue.ts:317) IS glue-side JS and trivially swappable — BUT random_get only feeds rquickjs/std internals. EVERY user-facing entropy consumer — Math.random (lib.rs:199 binds Math.random→__rand), the crypto.getRandomValues shim (lib.rs:367-368 calls __rand), node:crypto/nanoid/uuid (lib.rs:1800-1802 + 1858 all route through that shim → __rand) — flows through __rand, NOT random_get. So swapping random_get alone (the easy, import-free part) achieves almost nothing user-visible. 3) ASYMMETRY the proposal denies — clock:real pushes ONE scalar/eval (real time is a single number, clock is deterministically +1ms-monotone within a turn). Entropy needs a FRESH value on EVERY __rand call, of which there are unbounded many per cell, fired deep inside synchronous wasm execution. Bridge (A) "fill a per-turn ring buffer of N real bytes and have __rand consume it" therefore must (a) make __rand read engine linear memory (a real code change to a Rust closure, NOT "the same way set_clock is wired"), (b) pick a fixed N — if a cell draws more than N random bytes in one turn the buffer is exhausted and you must either block (no host re-entry available mid-cell without a WASM import — the 12-slot ceiling / single sequential host-call boundary bites here) or silently fall back to seeded (a correctness lie). The proposal hand-waves refill "per turn so it never blocks" but a single Math.random()-heavy loop blows any fixed per-turn budget. 4) NEW EXPORT REQUIRED — bridge (A) needs set_entropy_mode + an engine-side read of the scratch ring; that is engine Rust work + a rebuild, not glue-only. effort=S is dishonest; it's S-to-M with an engine rebuild and the unbounded-draw design question unsolved.
  - evidence: apps/kernel/engine/src/lib.rs:3072-3171 (no_mangle export surface — set_clock ABSENT), apps/kernel/src/kernel-glue.ts:1557-1571 (clock:real re-anchor wrapped in try/catch, comment 'engine lacks set_clock (older build)'), apps/kernel/src/kernel-glue.ts:1233 (clockSeed=Date.now()-CLOCK_EPOCH_BASE_MS — create-time base only), apps/kernel/engine/src/lib.rs:2923-2932 (__rand is a Rust Function::new closure, RNG/lcg thread-local, no glue access), apps/kernel/engine/src/lib.rs:199 (Math.random = __rand)

## Appendix — CF Primitive Inventory (16)


### Durable Object Alarms
- offers: Real wall-clock scheduling that survives hibernation. After a DO hibernates (WebSocket closed, in-memory state gone), a previously set alarm fires at the scheduled time, waking the DO and invoking the `alarm()` handler. The alarm is persisted in DO storage — it is not an in-memory timer. Provides guaranteed at-least-once execution with automatic retry on uncaught exceptions (exponential backoff, 2
- binds: No env binding needed — accessed via `this.ctx.storage.setAlarm(timestampMs)` / `getAlarm()` / `deleteAlarm()` from inside the DO class. The DO class must implement an `async alarm(alarmInfo?: {retryCount: number, isRetry: boolean})` method. No wrangler.jsonc entry beyond the DO class declaration it
- limits: 1. SINGLE ALARM PER DO INSTANCE — `setAlarm()` replaces any existing alarm; you cannot schedule two independent timers on one DO without a hand-rolled priority queue in storage. 2. FACETS CANNOT SET ALARMS — confirmed platform hard constraint ('Facets currently cannot set alarms'); idle-TTL / keep-warm scheduling MUST live on the SupervisorDO, not on KernelFacets. This is already the architecture in engram-cloud/supervisor.ts. 3. ALARM HANDLER WALL TIME = 15 minutes max. 4. AT-LEAST-ONCE, NOT EX
- good_for: Warm-flush dirty-checkpoint trigger (ALREADY IN USE): engram-kernel sets an alarm on mark_dirty() with warmFlushIdleMs (default 15min); alarm fires flush_critical() to snapshot the live QuickJS heap to SQLite before the DO evicts — this is the primary use and it works.; Idle-TTL eviction for facets (ALREADY IN USE via supervisor): since facets cannot alarm, the SupervisorDO arms its single alarm on each keep-warm heartbeat and uses it to scan for sessions past their TTL and call facets.abort() — supervisor-alarm-driven per the current cloud/supervisor.ts architecture.; Adaptive keep-warm scheduling: the supervisor alarm can implement EWMA-predicted next-activity and heartbeat only hot sessions rather than all sessions, reducing DO-wake costs.; Deferred async work that must not rely on an incoming request: e.g., post-eval R2 GC (pruning stale snapshot chunks) or AE flush — schedule via alarm to guarantee completion even if no client is connected.
- cost: No separate billing line for alarms. Alarm handler invocation counts as a normal DO request (billed against DO request quota on Workers Paid). Storage reads/writes inside the handler are billed as sta

### Dynamic Worker Loader (Worker Loader binding)
- offers: Spins up fresh V8 isolates at runtime from arbitrary JS/CJS/Python/text/data/JSON source strings. Two modes: load() (always-fresh, no cache) and get(id, codeCallback) (best-effort warm-cache by ID). Each child isolate is a full Workers runtime with its own event loop, import.meta, and env. Supports per-isolate network isolation (globalOutbound: null blocks fetch+connect entirely, or route through 
- binds: wrangler.jsonc: { \"worker_loaders\": [{ \"binding\": \"LOADER\" }] }. In code: env.LOADER.get(id: string, () => Promise<WorkerCode>): WorkerStub or env.LOADER.load(code: WorkerCode): WorkerStub. WorkerCode shape: { compatibilityDate: string, mainModule: string, modules: Record<string, string | {js}
- limits: 1. NO documented WASM module type in the public API (js/cjs/py/text/data/json only); runtime WebAssembly.compile is blocked inside child isolates ('Wasm code generation disallowed by embedder') — the only confirmed path is the undocumented {wasm: ArrayBuffer} module type proven in engram's v1-facet spike. 2. NO guarantee of isolate reuse: get() may cache by ID but eviction is platform-controlled with no TTL or guarantee; warm reuse is best-effort. Callbacks may fire multiple times for the same I
- good_for: Tier-1 content-addressed hash-worker registry (already live in engram): run user-registered JS snippets in full network isolation with a prefix-scoped VFS gateway as the only I/O surface — exactly the current use. globalOutbound:null + env:{VFS:stub} is the canonical deny-all-then-offer-capabilities pattern.; VM isolation for untrusted eval: instead of running untrusted user code inside the QuickJS WASM realm (single-realm, no JS-level isolation), delegate to a fresh V8 isolate — stronger sandbox than QuickJS's JS realm. Does NOT fix the snapshot-cliff (child isolate state is not snapshotted).; Tier-2 computation offload: CPU-heavy tasks that would exhaust the DO's single-threaded turn budget (e.g., large bundle compilation, crypto, image processing) can run in a child isolate with a hard cpuMs cap, keeping the kernel DO responsive.; Codemode / AI-generated tool execution: load() creates a fully disposable one-shot isolate per LLM-generated code fragment — no state leakage between calls, clean for agent tool-use patterns.
- cost: $0.002 per unique Worker loaded per day (distinct codeId × calendar day), plus standard Workers Paid CPU/request fees ($0.02/million requests; CPU billed per ms above free tier). During open beta (as 

### Cloudflare Queues (async producer/consumer, batching, retries, delay-seconds)
- offers: Durable, at-least-once async message delivery between Workers/DOs. A producer sends up to 128 KB per message (100 msgs / 256 KB per sendBatch call). A push-based consumer Worker receives batches (up to 100 msgs, up to 60s wait). Per-message or per-batch delaySeconds (0–86400s = 24h) defers delivery. Dead-letter queue on exhausted retries (up to 100 attempts). Queue-level backlog up to 25 GB, reten
- binds: Producer (DO or Worker sends): wrangler.jsonc `queues.producers[].queue` + `.binding` → env.MY_QUEUE in DO/Worker scope. API: `await env.MY_QUEUE.send(body, {contentType:'json'|'text'|'bytes'|'v8', delaySeconds:N})` or `await env.MY_QUEUE.sendBatch([{body,delaySeconds},...])`. Consumer (Worker recei
- limits: 1. AT-LEAST-ONCE ONLY — no exactly-once, no ordering guarantee. Duplicate delivery is possible on retry/failure paths. 2. ONE ACTIVE CONSUMER per queue (one push-consumer Worker OR one HTTP pull consumer, not both). 3. DO CANNOT be a push consumer — only plain Workers export the queue() handler. A DO can be a producer (env.MY_QUEUE.send from inside a DO method). 4. 128 KB message size ceiling including metadata; 256 KB / 100 msgs per sendBatch. 5. delaySeconds max = 86400 (24 h); queue-wide deli
- good_for: Async work offload from a DO response path — e.g. post-eval side-effects (R2 write, AE telemetry flush, sandbox cleanup) queued from inside a DO method without blocking the WS frame reply.; Deferred snapshot compaction jobs — a DO that hits the snapshot-size cliff can enqueue a 'compact this session' task with a delaySeconds back-off; a consumer Worker calls the DO later to trigger compaction when load is lower.; Dead-letter / error audit trail — failed eval batches or broken sessions can be routed to a DLQ for later inspection without losing the event.; Rate-limit / backpressure on sandbox Tier-2 invocations — enqueue sandbox.exec requests with delaySeconds when the container is busy; consumer drains at a controlled rate.
- cost: Paid plan pricing: $0.40 per million operations (writes + reads each count as one operation; a sendBatch of 10 msgs = 10 write ops). No egress charge. Storage for backlog: included up to 25 GB per que

### D1 (serverless SQLite DB, separate from DO SQLite)
- offers: A managed, serverless SQLite-compatible relational database that lives OUTSIDE any Durable Object, queryable from Workers/DOs over a network hop. Provides: full SQL (prepared statements, batch, exec, transactions), Time Travel (restore to any minute in the last 30 days), global read replicas (public beta, April 2025) with a Sessions API for sequential consistency via bookmarks, an HTTP API for ext
- binds: wrangler.jsonc: add a top-level "d1_databases" array entry: { "binding": "MY_D1", "database_name": "my-db", "database_id": "<uuid>" }. In the Worker/DO TypeScript env: env.MY_D1 is a D1Database object. API: env.MY_D1.prepare(sql).bind(...).run() | .all() | .first(); env.MY_D1.batch([...stmts]); env.
- limits: 1. NETWORK HOP — NOT colocated: every D1 query from a DO crosses a network boundary, adding latency (tens to hundreds of ms depending on region distance). DO SQLite (this.ctx.storage.sql) is zero-latency by comparison. This is the primary gotcha for engram's hot eval path.
2. Single-writer, sequential: D1 is single-threaded, one query at a time. A 1ms query yields ~1000 qps; a 100ms query yields ~10 qps. Not suitable for high-frequency concurrent writes.
3. Limits: max row/blob 2 MB; SQL stateme
- good_for: Cross-session / cross-tenant metadata store: storing per-tenant config, API keys, usage records, and metering rows that must be queryable across many DO instances simultaneously — something DO SQLite cannot do (each DO's SQLite is isolated to that DO).; Usage metering and billing queries: the v1.2 /usage endpoint currently reads per-DO AE data; D1 could hold normalized billing rows joinable across tenants, queryable by the cloud supervisor without opening each individual DO.; Snapshot manifest index across tenants: a global catalog of which sessions have snapshots, their sizes, last-eval timestamps, and engine hashes — enabling cross-shard queries the supervisor currently cannot do with per-facet SQLite.; Worker-Loader content-addressed registry metadata: the Tier-1 hash→worker mapping is currently in-DO; D1 would allow the registry to be shared across all kernel DO instances and queried/pruned globally.
- cost: Pricing (Workers Paid): $0.001 per 1M rows read, $1.00 per 1M rows written. Storage: $0.75/GB-month beyond the 5 GB included. Free tier: 5 million rows read/day, 100K rows written/day, 5 GB storage. L

### Workers KV (eventually-consistent global key-value store)
- offers: A globally-distributed, low-latency key-value store backed by a tiered cache (edge → regional → central). Stores arbitrary values up to 25 MiB (string, ArrayBuffer, ReadableStream) with optional per-key TTL (expiration/expirationTtl, minimum 60s) and up to 1024 bytes of JSON metadata per key. Reads from hot (cached) keys return in ~500µs–10ms at the edge; p99 improved to under 5ms as of 2025 after
- binds: wrangler.jsonc: { "kv_namespaces": [{ "binding": "KV", "id": "<namespace-id>" }] }. Accessed inside any Worker or DO handler as env.KV.get(key, opts), env.KV.put(key, value, opts), env.KV.delete(key), env.KV.list(opts). For DO access the same binding flows through the DO env object — no special DO-s
- limits: 1. EVENTUAL CONSISTENCY ONLY: writes propagate to other colos within up to 60+ seconds; read-after-write at a DIFFERENT colo is NOT guaranteed even immediately after a put(). No compare-and-swap, no transactions, no atomic read-modify-write. 2. WRITE RATE: hard 1 write/second per key — a second write to the same key within 1s returns HTTP 429. Engram's per-cell checkpoint would violate this if two cells write the same manifest key within 1s. 3. VALUE SIZE: 25 MiB max (fine for most snapshots; th
- good_for: R2-alternative for small snapshot metadata/manifests: KV metadata (1024b) could store chunk counts, usedHeap, generation, engine hash for a session — but only if a single session's DO is the sole writer (1 write/s constraint is fine for per-cell checkpoints that are seconds apart). NOT a replacement for the primary snapshot body (consistency lag would break cold-restore correctness).; Global feature-flag / config distribution: per-account or per-tenant runtime config (stdlib allowlist, fetch allowlist, cellBudgetTicks defaults) that changes rarely — KV's up-to-60s lag is acceptable for config that changes on the order of minutes.; Content-addressed Tier-1 worker registry cache: the Worker-Loader hash→source registry already uses LOADER, but KV could back a read-through cache of compiled worker sources keyed by sha256, with a long cacheTtl. Cold misses hit central store once; hot reads at edge are ~1ms. Write-once-read-many is the ideal KV pattern.; SDK/auth token lookup: API-key → tenant mapping for engram-cloud's per-tenant auth layer — written rarely (on key issue), read on every WS connect. Eventual consistency (60s lag) is acceptable for auth tokens that don't rotate frequently.
- cost: Free tier: 100k reads/day, 1k writes/day, 1 GB storage. Paid (Workers Paid, which engram already has): $0.50/million reads, $5/million writes, $0.50/GB-month storage — reads are cheap, writes cost 10×

### Containers / @cloudflare/sandbox (real Linux container, Tier-2 muscle)
- offers: A full Linux container (Debian-based, docker.io/cloudflare/sandbox base image) running alongside a Durable Object. Provides real child_process / exec, real Node.js (nodejs_compat on the Worker side), threads, arbitrary binaries, Git, file system, persistent Python/JS interpreter contexts, WebSocket tunnels to in-container services, and background process control. The container's HTTP API is fronte
- binds: wrangler.jsonc stanza (standalone worker, NOT inside the kernel worker): { "containers": [{ "class_name": "Sandbox", "image": "./Dockerfile", "instance_type": "standard-1", "max_instances": 5 }], "durable_objects": { "bindings": [{ "name": "Sandbox", "class_name": "Sandbox" }] }, "migrations": [{ "t
- limits: 1. SEPARATE WORKER: the Sandbox DO cannot be co-located inside the kernel worker (wrangler constraint — containers stanza requires its own worker with the Sandbox class). The kernel calls the sandbox over HTTP, adding one network hop (~10-50ms RTT on same-region CF infra). 2. COLD START: container provisioning timeout defaults to 30s (instanceGetTimeoutMS); portReadyTimeoutMS defaults to 90s. A sleeping/evicted container may take seconds to wake. 3. SLEEP/EVICT: default sleepAfter is 10 minutes 
- good_for: snapshot-cliff escape hatch: offload heap-intensive computation (large array builds, data transforms) to the container instead of doing it in the QuickJS VM, keeping the WASM linear memory below the ~24-28MB incompressible cliff; real child_process / threads: operations that need real Node.js child_process, worker_threads, native addons, or arbitrary binaries (e.g. ffmpeg, git, Python with numpy) that are impossible inside QuickJS; real fs / POSIX semantics: the container sees the shared R2 workspace as a real POSIX filesystem via mountBucket, giving true readdir/stat/inode semantics that the in-kernel VFS shim cannot provide; git operations: s.sandbox.git() already exposed in engram — clone, commit, push from a cell without any QuickJS shim
- cost: Requires Workers Paid plan ($5/month base). Billing starts when a request hits the container (or manual start) and stops after sleepAfter timeout (default 10 min). Billed in 10ms increments. CPU: $0.0

### R2 multipart upload + streaming GET/PUT + conditional writes
- offers: Resumable multipart upload (createMultipartUpload / uploadPart / complete) for objects up to ~5 TB in up to 10,000 parts; streaming GET via ReadableStream body on R2ObjectBody (pipe directly, no full buffer needed); streaming PUT via ReadableStream value to put() or uploadPart(); conditional reads and writes via R2Conditional (onlyIf: { etagMatches, etagDoesNotMatch, uploadedBefore, uploadedAfter 
- binds: wrangler.jsonc: [[r2_buckets]] binding = \"SNAPSHOTS\", bucket_name = \"engram-snapshots\". Accessed in Worker/DO via env.SNAPSHOTS (type R2Bucket). Methods: env.SNAPSHOTS.put(key, body, opts?), .get(key, opts?), .head(key), .delete(key|key[]), .list(opts?), .createMultipartUpload(key, opts?), .resu
- limits: 1. PART SIZE FLOOR: minimum part size for multipart is 5 MiB (except the final part). Engram snapshot chunks are ~64 KB SQLite rows — too small for multipart parts; multipart is only useful for the R2 overflow path (>2 MB gz), where you'd accumulate a full 5 MiB buffer before calling uploadPart. 2. CONCURRENT WRITE RATE: max 1 write/second to the same key; higher rates return HTTP 429. Per-session DO is single-threaded so this is only a risk if multiple DO instances (or a DO + supervisor) race t
- good_for: snapshot-cliff: streaming GET (ReadableStream piped directly without full buffer) reduces the ~597 ms R2 readMs for large images (>2 MB gz) — the dominant owned latency cost identified in v0.4 cold-start research. Pipe R2ObjectBody.body directly into a TransformStream gunzip + blit loop rather than await arrayBuffer() to cut time-to-first-byte and reduce peak Worker heap usage during restore.; snapshot-cliff: multipart upload for the R2 overflow path (>2 MB gz snapshots) lets a large snapshot be written in resumable 5 MiB+ parts, so a crash mid-write does not leave a corrupt partial object — each part is independently written and the object only becomes visible on complete(). This improves the atomicity of the R2 overflow write path currently done as a single put().; snapshot coherence / atomic swap: conditional put with onlyIf: { etagDoesNotMatch: '*' } implements create-if-not-exists; onlyIf: { etagMatches: '<prior-etag>' } implements compare-and-swap for snapshot rotation. Useful for the engine-migration journal (EXP-9 pattern) and for ensuring a supervisor-level manifest key is only written once on cold-boot races. Returns null (not throw) on precondition failure — must be checked.; R2 overflow path durability: byte-range GET (R2GetOptions.range) allows fetching only the manifest header or a specific chunk of a large snapshot without downloading the entire image — could support partial restore or incremental chunk validation without full R2 GET cost.
- cost: R2 pricing (as of 2026): Class A operations (put, createMultipartUpload, uploadPart, completeMultipartUpload, list) = $4.50 per million requests; Class B operations (get, head) = $0.36 per million req

### workerd real Web Crypto (globalThis.crypto / crypto.subtle / crypto.getRandomValues)
- offers: A full W3C Web Crypto implementation backed by the host OS CSPRNG (not seeded, not deterministic). Available as a global in every Worker and Durable Object JS execution context. Provides: crypto.getRandomValues(TypedArray) — fills with true hardware-entropy random bytes; crypto.randomUUID() — v4 UUID from CSPRNG; crypto.subtle — full SubtleCrypto interface: encrypt/decrypt (AES-CTR/CBC/GCM/KW), si
- binds: No wrangler binding or env.X needed. crypto is a global on the Worker/DO JS execution context — accessed as globalThis.crypto, crypto.subtle, etc., exactly like a browser. Zero wrangler.jsonc configuration required. nodejs_compat flag optionally exposes node:crypto on top (which delegates randomByte
- limits: 1. CSPRNG ONLY — no seeded/deterministic mode. There is no API to seed crypto.getRandomValues. This is by design (security). 2. In engram today ALL VM-visible crypto (Math.random, crypto.getRandomValues, randomUUID, node:crypto randomBytes) is wired to the seeded LCG (__rand in engine/src/lib.rs), not to the host's real Web Crypto. The host real crypto.subtle is intentionally NOT forwarded into the QuickJS heap — the in-VM shim is pure-JS and covers only digest/sha256/sha1/md5 synchronously (lin
- good_for: Host-side token/key generation (API keys, session tokens, HMAC signing of frames) — use crypto.getRandomValues or crypto.randomUUID directly in kernel-glue.ts or lib.rs JS dispatch, no plumbing needed.; Host-side HMAC verification for Tier-1 Worker-Loader content-addressed registry (sign/verify worker hashes before loading).; DigestStream for streaming R2 snapshot integrity checks — pipe the snapshot body through a DigestStream(SHA-256) without buffering the full image a second time.; crypto.subtle.timingSafeEqual for constant-time API key comparison in the DO dispatch path (already trivially available).
- cost: Free — no billing unit, no quota. Counted against CPU time only (crypto ops are fast; hardware-accelerated). No per-invocation charge. No subrequest consumed. Available on all plans (Free and Paid). L

### Hibernatable WebSockets + WS auto-response/serializeAttachment API (Cloudflare DO)
- offers: Allows a Durable Object to drop out of memory (hibernate) while keeping client WebSocket connections alive on Cloudflare's edge. Three sub-capabilities: (1) state.acceptWebSocket(ws, tags?) — registers a WS with the hibernation runtime instead of pinning the DO in memory; incoming messages wake the DO, call webSocketMessage(ws,msg), then re-hibernate on idle. (2) ws.serializeAttachment(value) / ws
- binds: No env.X binding — it is a method surface on the DurableObjectState object (this.ctx / state arg passed to the DO constructor). No wrangler.jsonc config beyond the standard DO class declaration (new_sqlite_classes or durable_objects.bindings). The DO class must implement webSocketMessage(ws, message
- limits: 1. SERVER-SIDE ONLY: hibernation is unsupported for outgoing WebSockets (DO as client). Engram's kernel-glue WS connection FROM the DO to external services cannot hibernate. 2. CONSTRUCTOR RE-RUNS ON EVERY WAKE: the full JS constructor fires on each incoming message after eviction. Any in-memory state not in SQLite or an attachment is gone. The constructor must be cheap and idempotent. Engram's kernel-glue.ts constructor already re-instantiates the WASM engine on cold restore — that path is pres
- good_for: COST: eliminates DO billable Duration (GB-s) during idle REPL sessions — the single largest operational cost item; WS stays open so SDK reconnect logic is not triggered on normal idle; KEEP-WARM POLICY: autoResponse ping/pong means the SDK heartbeat (currently a plain 'ping' cell or WS message) can be served at the edge without waking the DO, so the keep-warm interval can be much longer without paying CPU time — relevant to the adaptive keep-warm design noted in CLAUDE.md; SESSION METADATA SURVIVAL: serializeAttachment(16KB) can hold session-id + apiKey + current cell index + last-eval timestamp per socket, allowing the DO constructor to skip a SQLite read on the common wake path (attach the fast-path auth/routing info directly to the socket handle); SOCKET IDENTITY ON RE-WAKE: getWebSockets(tag) lets the DO recover all live socket handles after re-initialization without the client reconnecting — critical for the supervisor DO's PROXY model (facet-held sockets broken, supervisor holds + RPCs) since the supervisor can re-acquire its sockets by tag after an unexpected eviction
- cost: Billing unit is DO active Duration (GB-s) — charged only while the DO is in memory executing. With hibernatable WS: DO sleeps when idle (no setTimeout/fetch pending), Duration billing stops, WS connec

### Cron Triggers / Scheduled handlers
- offers: Periodic invocation of a Worker's `scheduled(controller, env, ctx)` handler on a cron schedule (Quartz-style 5-field, UTC only, minimum 1-minute granularity). The handler receives the full `env` bindings object — including Durable Object namespaces, KV, R2, AE, Container, and Worker-Loader — so it can call into any bound resource. `ctx.waitUntil()` extends execution past the handler return. `contr
- binds: wrangler.jsonc: `"triggers": { "crons": ["*/5 * * * *", "0 * * * *"] }`. No env.X binding — the scheduled handler is an export on the same Worker module (`export default { scheduled(controller, env, ctx) {...} }`). All existing bindings (KERNEL_DO, SNAPSHOTS R2, AE, LOADER, sandbox Container) are av
- limits: 1. MINIMUM INTERVAL IS 1 MINUTE — no sub-minute scheduling; for finer granularity use DO alarms. 2. NO RETRY ON FAILURE — if the handler throws or times out, Cloudflare does NOT retry; next fire is at the next scheduled time. Must implement own idempotency/retry. 3. JITTER IS REAL AND UNDOCUMENTED — Cloudflare deploys scheduled work on underutilized infrastructure; actual fire time can drift by an unspecified amount from the cron wall-clock second. No latency SLA. 4. UTC ONLY — no timezone confi
- good_for: Supervisor-level keep-warm heartbeats: cron every 1 min can enumerate active sessions in KV/DO and send a lightweight ping fetch/RPC to each DO stub, preventing eviction without DO alarms on facets (facets CANNOT set alarms — this is the direct workaround). This is the highest-value engram use.; Stale-key R2 pruning: scheduled scan of engram-snapshots R2 bucket to delete orphaned snapshot chunks — the task that is listed as remaining-to-GA but blocked on a manual token.; AE / metering rollup: periodic flush or aggregation of Analytics Engine counters into KV or DO SQLite for billing/usage dashboards (engram-cloud /usage endpoint).; Adaptive keep-warm policy enforcement: a cron handler can read session cadence from AE or KV and decide which DO instances to warm vs. let evict, implementing the 'predict cadence, decide when NOT to warm' strategy noted in CLAUDE.md.
- cost: Each scheduled invocation counts as one Worker request against the daily request quota (100K/day free; paid = requests billing). On Paid Workers, scheduled invocations are billed at the same per-reque

### Service Bindings + Worker-to-Worker RPC (WorkerEntrypoint / JsRpc)
- offers: Lets any Worker or DO call typed async methods on another named Worker (or named WorkerEntrypoint export) as if it were a local function call. The callee extends WorkerEntrypoint and exposes public methods; the caller gets a proxy stub via env.BINDING. Bidirectional: caller can pass function callbacks and RpcTarget subclass instances back as stubs. Supports structured-cloneable values, ReadableStr
- binds: wrangler.toml / wrangler.jsonc:
  [[services]]
  binding = "MY_SVC"          # → env.MY_SVC in the caller
  service = "target-worker"   # worker name on the same CF account
  entrypoint = "MyEntrypoint" # optional: named WorkerEntrypoint export

Caller usage: await env.MY_SVC.myMethod(arg1, arg2)
Ca
- limits: 1. STUBS DO NOT SURVIVE WS HIBERNATION. A stub obtained in one execution context is invalid after the DO hibernates and resumes — you must re-acquire it on each wake. This is the single biggest constraint for engram's always-alive DO pattern.
2. 32-Worker-invocation chain limit per incoming request. Each service binding call consumes one slot; hitting 32 throws. Deep recursive cell→service→service chains will exhaust this quickly.
3. 32 MiB max serialized RPC payload. Snapshot blobs (~1–45 MB ra
- good_for: Tier-1 hash-worker registry: the VfsGateway WorkerEntrypoint already uses this pattern (env.LOADER). Service bindings generalize it for typed RPC instead of raw fetch — no engram problem fixed, already in use.; Offloading CPU-heavy deterministic transforms (zlib compress/decompress, sucrase TS transpile) to a dedicated stateless WorkerEntrypoint worker, keeping those off the DO's single-threaded budget. This would reduce DO CPU time per cell.; Sandbox capability brokering: the existing engram-sandbox Tier-2 fetch could be wrapped as a named WorkerEntrypoint with typed methods (exec, git, expose) instead of raw HTTP — better type safety, same zero-network-hop perf, no new capability.; Streaming large R2 payloads to/from the DO via ReadableStream over RPC — relevant for snapshot blobs above 2 MB if you want to avoid direct R2 GET latency (though R2 binding from the DO directly is simpler).
- cost: No additional per-call billing. Both Workers' CPU time is attributed to the originating request's bill. Subrequest quota: max 32 Worker invocations per incoming request chain (each service binding cal

### Cloudflare Workflows (durable multi-step execution)
- offers: Durable, hibernating multi-step execution engine built on Workers. Core API: step.do(name, fn, retryConfig) persists step result to durable storage; step.sleep(name, duration) hibernates free for up to 1 year; step.waitForEvent(desc, {type, timeout}) suspends until an external event arrives via instance.sendEvent() — default 24h timeout, configurable 1s–365d. Automatic retry per step (up to 10,000
- binds: wrangler.jsonc: top-level \"workflows\" array — [{\"binding\": \"MY_WORKFLOW\", \"class_name\": \"MyWorkflow\", \"name\": \"my-workflow\"}]. Class must extend WorkflowEntrypoint and be exported from the same Worker (or a named script_name for cross-worker). At runtime: env.MY_WORKFLOW.create({id?, p
- limits: HARD LIMITS (Paid): max 10,000 steps/instance (configurable to 25,000); max 1 MiB step result payload; max 1 GiB persisted state/instance; max 30s CPU/step (configurable to 5 min); max 10,000 subrequests/instance (configurable to 10M); state retained 30 days then deleted; 50,000 concurrent running instances; 300 create/sec account-wide. WHAT IT CANNOT DO: (1) Cannot snapshot or restore arbitrary in-memory interpreter state — persists only serialisable step RESULTS (JSON), not live heap/closure/P
- good_for: Tier-3 wake-loop replacing hand-rolled R2-poll: a Workflow sleeps N minutes, checks for pending work, dispatches to a DO, sleeps again — durably and free with no alarm-budget overhead; Agent orchestration above the kernel: long-running SPLIT/MAP/REDUCE pipelines live in a Workflow that calls engram-kernel DO per cell; each step.do() invokes the kernel and persists the cell result; workflow survives multi-hour pauses between agent turns; Human-in-the-loop approval gates: waitForEvent gives a clean durable pause point for a human to approve the next agent action before the kernel evaluates it — no polling, no separate state machine; Tenant lifecycle management for engram-cloud: one Workflow per tenant session handles DO facet provisioning, keep-warm heartbeat (step.sleep + ping), and teardown on expiry without consuming supervisor DO alarm budget
- cost: Three billing axes: (1) CPU ms — 30M ms/month included on Paid, then $0.02/M ms; sleep and waitForEvent idle time is FREE (zero CPU billed while paused). (2) Requests — 10M/month included, then $0.30/

### Cache API (caches.default / caches.open())
- offers: A per-datacenter, HTTP-response-shaped key-value store backed by Cloudflare's CDN cache layer. You store a Response object under a URL key (cache.put(request, response)), retrieve it (cache.match(request)), or delete it (cache.delete(request)). Useful as a fast, free-tier edge cache for fetch responses — avoids re-fetching the same upstream URL on repeated requests within the same datacenter. Name
- binds: No env.X binding and no wrangler.jsonc entry needed or available. Accessed as a runtime global: `caches.default` or `await caches.open('name')`. Nothing to declare in wrangler.jsonc. Works in standard Workers on custom domains. DISABLED for Workers for Platforms (namespaced scripts). No documented c
- limits: DATA-CENTER LOCAL ONLY: cache.put/match/delete operate only on the single datacenter handling the current request. No replication, no global consistency, no cross-DC invalidation. cache.delete() purges only the local DC. GET ONLY: only GET-method Request keys are accepted; put() throws on other methods. RESPONSE SHAPE REQUIRED: values must be Response objects, not arbitrary buffers/strings. Must set correct Cache-Control/Expires headers or the response is not stored (413 returned if Cache-Contro
- good_for: FETCH RESPONSE MEMOIZATION: caching results of host.fetch() calls to the same upstream URL within the same DC, avoiding repeated network round-trips for deterministic external resources — but only for GET responses, only within the same datacenter colocation, and only if the Cache API is actually accessible from a DO handler (unconfirmed).; STDLIB BUNDLE DISTRIBUTION: if engram fetches versioned stdlib bundles from a stable upstream URL on hot paths, Cache API could serve as a fast-path read-through cache in front of R2 — the R2 GET is the dominant cold-wake latency contributor at 5MB; a DC-local cache hit could short-circuit it for repeat warm fetches.; WORKER-LOADER HASH REGISTRY READS: Tier-1 hash-worker source blobs are content-addressed and GET-fetchable by URL; Cache API could short-circuit repeat loads of the same hash blob at the DC level if the LOADER binding internally does a cacheable fetch.
- cost: Included in Workers Paid plan — no separate line-item charge for Cache API operations. Cache API calls consume the same subrequest quota as fetch() (10,000/invocation on Paid as of Feb 2026). Cached o

### Hyperdrive (Cloudflare)
- offers: A globally-distributed connection pooler + optional query cache that sits between your Worker/DO and an external Postgres (or MySQL/compatible) database. It terminates the TCP+TLS handshake at a PoP close to the Worker, keeps a warm pool of real connections near the origin DB, and returns read-query results from an in-network cache (default TTL 60 s, max 1 h, stale-while-revalidate 15 s). Net effe
- binds: wrangler.jsonc: [[hyperdrive]] binding = "HYPERDRIVE" id = "<hyperdrive-config-id>". Accessed in Worker/DO as env.HYPERDRIVE.connectionString (returns a postgres:// URL with Hyperdrive as the proxy endpoint). You pass that string to any Postgres driver (pg, postgres.js). Requires nodejs_compat flag 
- limits: 1. TRANSACTION MODE ONLY: the pooler runs pgBouncer-style transaction-mode pooling. A single connection is held for the duration of one transaction, then returned. Session-level state (SET, prepared statements outside a transaction, advisory locks, LISTEN/NOTIFY, COPY) does NOT persist across queries. Each new client.query() may land on a different backend connection.

2. NO SESSION AFFINITY: you cannot pin a session to one backend connection. This makes stateful Postgres features (temp tables, 
- good_for: External durable relational store for engram metadata (session registry, tenant records, usage/billing rows) that outgrows DO SQLite's per-DO isolation — e.g., a SupervisorDO that needs cross-tenant query capability without serializing through one DO.; Backing store for the engram-cloud multi-tenant supervisor's session/auth/metering tables if those need to be queryable across DOs (DO SQLite is per-DO, not cross-queryable).; Read-heavy caching of stable reference data (e.g., a content-addressed Worker-Loader registry manifest or a module catalog) that the kernel reads at cold-start — cache hit at sub-5 ms is a meaningful cold-start improvement vs R2 GET (~597 ms measured).; Offloading append-only AE-like metering/audit rows to Postgres if Analytics Engine SQL API is insufficient for complex cross-tenant queries.
- cost: Workers Paid plan required (same as engram already is). Hyperdrive itself: free tier available (10 configs, ~20 origin connections). Paid plan: 25 configs, ~100 origin connections. You pay for the ext

### Workers AI / Vectorize
- offers: Workers AI: serverless GPU inference bound as env.AI — run embedding models (@cf/baai/bge-large-en-v1.5 → 1024-dim, @cf/baai/bge-small-en-v1.5 → 384-dim, @cf/baai/bge-base-en-v1.5 → 768-dim, plus Qwen3/BGE-M3/EmbeddingGemma), text generation (llama-3.1-8b-instruct etc.), summarization, classification, ASR, translation, image tasks. Streaming via { stream: true }. Vectorize: a managed approximate-n
- binds: Workers AI — wrangler.jsonc: add top-level `"ai": { "binding": "AI" }`. Exposed as `env.AI` (type `Ai`). Call: `await env.AI.run("@cf/baai/bge-large-en-v1.5", { text: [...] })` returns `{ data: Float32Array[] }`. Limit: ONE ai binding per Worker project. Vectorize — wrangler.jsonc: `"vectorize": [{ 
- limits: 1. EVENTUAL CONSISTENCY on Vectorize: inserted/upserted/deleted vectors are typically unavailable for query for several seconds after mutation — you cannot upsert then immediately query in the same cell and expect to see the new vector. 2. Workers AI rate limits are LOW: text embeddings 3000 RPM (BGE-Large-EN 1500 RPM); text generation 300 RPM; these are account-wide, not per-DO. A burst eval loop generating embeddings per cell will hit limits fast. 3. ONE ai binding per Worker — you cannot have
- good_for: Semantic cell search / notebook history indexing: embed cell source+output text at checkpoint time → store in Vectorize → later query 'find the cell where I computed X' without replaying state. Does not fix any kernel primitive but adds a retrieval layer on top.; Snapshot manifest semantic search: embed session descriptions or exported variable names into Vectorize so a supervisor can find the closest live session to a user's intent without full cold-restore.; Tier-1 hash-worker content routing: embed the source of a registered hash-worker at registerWorker() time, then at invokeWorker() time embed the task description and query Vectorize to find the best matching worker hash — turns the content-addressed registry into a semantic registry.; Context window pre-filtering for RLM / ouru: if a session's host.ctx grows large, embed each stored context chunk and query Vectorize to retrieve only the top-K relevant chunks before slicing into the LLM prompt — reduces tokens sent without needing to replay state.
- cost: Workers AI: billed per neuron (Cloudflare's internal compute unit). Embedding models (BGE family) are among the cheapest. Text generation (llama-3.1-8b) is more expensive and billed per token. Free ti

### Browser Rendering (Cloudflare Workers Browser Binding)
- offers: A managed headless Chromium instance reachable from a Worker or Durable Object via a `Fetcher`-typed env binding. Supports Puppeteer v22 (forked as `@cloudflare/puppeteer`) and Playwright for full CDP automation: page navigation, screenshot/PDF, JS evaluation in-page, DOM interaction, network interception. Also exposes a `.quickAction()` REST shortcut (compat date >= 2026-03-24) for one-shot page-
- binds: wrangler.jsonc: `"browser": { "binding": "MYBROWSER" }` — no id/namespace needed, just a name. TypeScript env type: `MYBROWSER: Fetcher`. Accessed from a DO exactly the same as from a plain Worker: `puppeteer.launch(this.env.MYBROWSER)`. Also requires `"compatibility_flags": ["nodejs_compat"]` and c
- limits: HARD LIMITS (Paid plan, which engram already requires): 120 concurrent browser sessions per account; 1 new session/second launch rate; 60-second inactivity timeout per session (extendable to 10 min via keep_alive ms); Quick Actions 10 req/s. Free plan: 10 min/day total, 3 concurrent, 1 session/20s — irrelevant for engram. WHAT IT CANNOT DO: (1) The Chrome process runs OUTSIDE the DO's isolate — it is a remote Chromium over CDP/WebSocket, NOT an in-process JS engine. You cannot snapshot its heap,
- good_for: Fetching and rendering real-world web content from inside a cell (host.fetch returns raw HTML/JSON; browser binding returns rendered DOM, screenshots, post-JS-execution content — closes the 'JS-rendered page' gap that host.fetch cannot reach); Generating screenshots or PDFs as artifacts from an engram cell — the cell calls a Tier-1 hash-worker or sandbox endpoint that drives the browser binding, result stored in R2 /workspace; Scraping/crawling tasks where target pages require JS execution (SPAs, auth flows) — same pattern via Tier-1 worker registry; Sandboxed page-preview or OG-image generation as a kernel capability exposed via host.browser or similar host call
- cost: Billing unit is browser-session-hours (active Chromium time). Paid plan required (engram is already on Workers Paid). No published per-hour rate in public docs as of 2026-06; metered via dashboard. Ra
